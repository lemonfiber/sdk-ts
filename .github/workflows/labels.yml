# Keep this repo's labels in sync with the canonical set in the spec.
name: labels
on:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * 1"
permissions:
  contents: read
jobs:
  sync:
    permissions:
      contents: read
      issues: write
    uses: lemonfiber/spec/.github/workflows/label-sync.yml@2eada7417e11209ebfa323e7d422b02d727da85e # main
