# Gate this repo's PRs on zero new SonarCloud issues.
#
# The scan is SonarCloud Automatic Analysis against `lemonfiber_sdk-ts`; there
# is no scan job here. This reads the summary that analysis posts and enforces
# the standard the free plan's own quality gate cannot (Q-R64), through the
# shared reusable in spec so every repo gates identically.
name: sonar
on:
  pull_request:
permissions:
  contents: read
  pull-requests: write
jobs:
  gate:
    uses: lemonfiber/spec/.github/workflows/sonar-gate.yml@61f5205b9db1699ba58dc007b23924a7e566de76 # main
    with:
      allowed-open: 0
    secrets:
      sonar-token: ${{ secrets.SONAR_TOKEN }}
