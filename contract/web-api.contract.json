{
  "api_version": 1,
  "kinds": {
    "admission": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Admitted"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Admitted": {
          "description": "A session opened, and the moment it stops being one.\n\nThe ending is carried rather than left for a client to work out, because a client\nthat guessed would be a second opinion about who is admitted — and the two would\ndisagree on the day somebody's clock is wrong.",
          "type": "object",
          "properties": {
            "token": {
              "description": "The secret this session is carried by, sent in the header the per-run token is.",
              "type": "string"
            },
            "until": {
              "description": "When it stops being one, written as every other instant this product writes.",
              "type": "string"
            }
          },
          "required": [
            "token",
            "until"
          ]
        }
      }
    },
    "adoption": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/AdoptReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "AdoptReport": {
          "description": "What adopting a setup already here came to, or would come to.",
          "type": "object",
          "properties": {
            "back_up": {
              "description": "The host paths those services keep their data in, so a backup can be taken of\nexactly the right thing.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "project": {
              "description": "The project lemonfiber would manage, where exactly one could be adopted.",
              "type": [
                "string",
                "null"
              ]
            },
            "refusal": {
              "description": "Why nothing was done, where nothing was.",
              "type": [
                "string",
                "null"
              ]
            },
            "stance": {
              "description": "Where the act stands.",
              "$ref": "#/$defs/Stance"
            },
            "upgrades": {
              "description": "The services whose databases a newer version would upgrade, and whose data\ntherefore has to be backed up before anything opens it.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/CarryingReport"
              }
            }
          },
          "required": [
            "stance",
            "upgrades",
            "back_up"
          ]
        },
        "CarryingReport": {
          "description": "What adopting one existing service would come to.",
          "type": "object",
          "properties": {
            "backup_first": {
              "description": "Whether its database must be backed up before lemonfiber opens it.",
              "type": "boolean"
            },
            "because": {
              "description": "What that means for this service's data, in the operator's terms.",
              "type": "string"
            },
            "existing": {
              "description": "The version standing here now.",
              "type": "string"
            },
            "ours": {
              "description": "The version lemonfiber pins.",
              "type": "string"
            },
            "refused": {
              "description": "Whether lemonfiber will not do this at all.",
              "type": "boolean"
            },
            "service": {
              "description": "The service, by the name lemonfiber runs it under.",
              "type": "string"
            },
            "verdict": {
              "description": "Which of the two is the later, in one word.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "existing",
            "ours",
            "verdict",
            "because",
            "backup_first",
            "refused"
          ]
        },
        "Stance": {
          "description": "Where a proposed change stands once it has been read against what is in force.",
          "oneOf": [
            {
              "description": "The setting already holds what was asked for. Nothing to apply, nothing to\nconfirm, and the file is not touched — a write of the same value would move\nthe file's own timestamp and read afterwards as an edit somebody made.",
              "type": "string",
              "const": "unchanged"
            },
            {
              "description": "Staged and not applied: nothing has been written. Either it is consequential\nand nobody has said yes to it yet, or it was rehearsed.\n\nThe default, because nothing having happened yet is what every act starts as,\nand a report built and not filled in should say that rather than claim it acted.",
              "type": "string",
              "const": "pending"
            },
            {
              "description": "Turned away. It cannot be applied safely, the file is exactly as it was, and\nthe refusal says why.",
              "type": "string",
              "const": "blocked"
            },
            {
              "description": "Written.",
              "type": "string",
              "const": "applied"
            }
          ]
        }
      }
    },
    "alerts": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/AlertReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "AlertReport": {
          "description": "What the operator will be told about, and what changing it came to.",
          "type": "object",
          "properties": {
            "changed": {
              "description": "Whether this call changed the answer.",
              "type": "boolean"
            },
            "exceptions": {
              "description": "Events set apart from the preset, quietest name first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/ExceptionReport"
              }
            },
            "means": {
              "description": "What that preset means, in the operator's terms.",
              "type": "string"
            },
            "preset": {
              "description": "The preset in force for events with no exception of their own.",
              "type": "string"
            },
            "rehearsed": {
              "description": "Whether it only reported what it would have written.",
              "type": "boolean"
            }
          },
          "required": [
            "preset",
            "means",
            "exceptions",
            "changed",
            "rehearsed"
          ]
        },
        "ExceptionReport": {
          "description": "One event kind the operator set apart from the preset.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "The kind of event, by the name a finding gives it.",
              "type": "string"
            },
            "wanted": {
              "description": "Whether it is heard about, whatever the preset would say.",
              "type": "boolean"
            }
          },
          "required": [
            "kind",
            "wanted"
          ]
        }
      }
    },
    "archives": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Listing"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Listing": {
          "description": "The archives this machine has kept.",
          "type": "object",
          "properties": {
            "archives": {
              "description": "Each one by the name it was written under, newest first.\n\nThe name is the whole of what another surface needs: it is what a restore\nasks for, and it carries the moment the archive was taken and what it\ncovers, because that is how a capture names one.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "archives"
          ]
        }
      }
    },
    "backup": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Report"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Report": {
          "description": "What a capture produced.",
          "type": "object",
          "properties": {
            "path": {
              "description": "Where the archive was written.",
              "type": "string"
            },
            "pruned": {
              "description": "The older backups retention pruned, oldest first.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "scope": {
              "description": "What the backup covers.",
              "$ref": "#/$defs/Scope"
            },
            "sensitive": {
              "description": "Whether it carries credentials, and so must be handled as sensitive.",
              "type": "boolean"
            }
          },
          "required": [
            "path",
            "scope",
            "sensitive",
            "pruned"
          ]
        },
        "Scope": {
          "description": "How much of the stack a backup covers.\n\nWhole-stack is the common case, but restoring one service is often what is\nactually wanted — one \\*arr's configuration mangled while the rest is fine —\nso the scope is recorded in the archive and honoured on the way back.",
          "oneOf": [
            {
              "description": "Every service's configuration, plus lemonfiber's own and the stack.",
              "type": "object",
              "properties": {
                "scope": {
                  "type": "string",
                  "const": "whole_stack"
                }
              },
              "required": [
                "scope"
              ]
            },
            {
              "description": "One named service's configuration alone.",
              "type": "object",
              "properties": {
                "name": {
                  "description": "The service whose configuration this covers.",
                  "type": "string"
                },
                "scope": {
                  "type": "string",
                  "const": "service"
                }
              },
              "required": [
                "scope",
                "name"
              ]
            }
          ]
        }
      }
    },
    "bandwidth": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Sharing"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Answer": {
          "description": "How one download client answered about the limits on it.",
          "oneOf": [
            {
              "description": "It answered, in both directions.",
              "type": "object",
              "properties": {
                "answered": {
                  "type": "string",
                  "const": "held"
                },
                "down": {
                  "description": "What became of the download limit.",
                  "$ref": "#/$defs/Held"
                },
                "period": {
                  "description": "Which side of the household's day it says it is on, where it keeps the\nhours itself.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/Period"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "up": {
                  "description": "And of the upload one.",
                  "$ref": "#/$defs/Held"
                }
              },
              "required": [
                "answered",
                "down",
                "up"
              ]
            },
            {
              "description": "It did not answer, and this is what it said.\n\nIts own line rather than an absence, because a client nobody could reach is\na client whose limits are unknown, and an unknown limit rendered as no\nlimit is the report reading better than the stack is.",
              "type": "object",
              "properties": {
                "answered": {
                  "type": "string",
                  "const": "silent"
                },
                "said": {
                  "description": "What went wrong, in the words of whatever refused.",
                  "type": "string"
                }
              },
              "required": [
                "answered",
                "said"
              ]
            }
          ]
        },
        "Cap": {
          "description": "A monthly allowance, and what to do at the end of it.",
          "type": "object",
          "properties": {
            "exceeded": {
              "description": "What happens when it is reached, chosen when the cap was declared.",
              "$ref": "#/$defs/WhenExceeded"
            },
            "monthly": {
              "description": "The allowance, in bytes.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "monthly",
            "exceeded"
          ]
        },
        "Capacity": {
          "description": "What the line was measured to carry.",
          "type": "object",
          "properties": {
            "down": {
              "description": "Bytes a second down.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "source": {
              "description": "Where the figure came from.",
              "$ref": "#/$defs/Source"
            },
            "taken": {
              "description": "When it was taken, in seconds since the epoch.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "through_tunnel": {
              "description": "Whether the path it was measured over goes through the VPN tunnel.",
              "type": "boolean"
            },
            "up": {
              "description": "Bytes a second up.\n\nMeasured apart from the download, because a home connection is asymmetric\nand a single figure for both would make every upload share far larger than\nthe operator meant.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "down",
            "up",
            "source",
            "taken",
            "through_tunnel"
          ]
        },
        "Held": {
          "description": "One direction on one client: what it was asked for, took, and is doing.",
          "type": "object",
          "properties": {
            "accepted": {
              "description": "What it reports as in force.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "asked": {
              "description": "What it was asked to hold to, in bytes a second, where anything was.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "moving": {
              "description": "What it is moving right now, where it reported a figure.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "verdict": {
              "description": "What that adds up to.",
              "$ref": "#/$defs/Verdict"
            }
          },
          "required": [
            "verdict"
          ]
        },
        "Holding": {
          "description": "One download client, and what became of the limits it was given.",
          "type": "object",
          "properties": {
            "answer": {
              "description": "What it said.",
              "$ref": "#/$defs/Answer"
            },
            "client": {
              "description": "The client, by the name the stack knows it under.",
              "type": "string"
            },
            "pulling": {
              "description": "Whether it is fetching at all, where a declared cap made that a question.\n\nAbsent on a stack with no cap rather than assumed to be fetching: asking\nevery client whether it has stopped, on a stack where nothing would ever\nstop it, is traffic spent on a figure nothing would act on.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Pulling"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "client",
            "answer"
          ]
        },
        "Limit": {
          "description": "How much of the line something may take.",
          "oneOf": [
            {
              "description": "Nothing holds it back.",
              "type": "object",
              "properties": {
                "as": {
                  "type": "string",
                  "const": "unlimited"
                }
              },
              "required": [
                "as"
              ]
            },
            {
              "description": "A proportion of what the line was measured to carry, in whole per cent.",
              "type": "object",
              "properties": {
                "as": {
                  "type": "string",
                  "const": "share"
                },
                "at": {
                  "type": "integer",
                  "format": "uint8",
                  "maximum": 255,
                  "minimum": 0
                }
              },
              "required": [
                "as",
                "at"
              ]
            },
            {
              "description": "A figure in bytes a second, as it was given.",
              "type": "object",
              "properties": {
                "as": {
                  "type": "string",
                  "const": "absolute"
                },
                "at": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                }
              },
              "required": [
                "as",
                "at"
              ]
            }
          ]
        },
        "Metered": {
          "description": "What the stack itself moved in a calendar month, and what that leaves out.",
          "type": "object",
          "properties": {
            "down": {
              "description": "Bytes pulled down, as far as the clients count them.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "excludes": {
              "description": "What this count does not include, always said.",
              "type": "string"
            },
            "incomplete": {
              "description": "What is known to be missing from the count itself, where anything is.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "month": {
              "description": "The month, as the client that dated the figures dates them.",
              "type": "string"
            },
            "up": {
              "description": "Bytes given back.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "month",
            "down",
            "up",
            "excludes",
            "incomplete"
          ]
        },
        "Period": {
          "description": "Which side of the household's day a moment falls on.",
          "oneOf": [
            {
              "description": "People are up and using the line, so the stack is held back.",
              "type": "string",
              "const": "active"
            },
            {
              "description": "The house is asleep and the line is the stack's.",
              "type": "string",
              "const": "quiet"
            }
          ]
        },
        "Pulling": {
          "description": "Whether a client is fetching at all, where a cap made it a question.\n\nApart from the limits above rather than folded in with them, because it answers\na different question and a client held to a crawl is not a client that stopped.\nA word that named both would be the vocabulary this whole path exists to avoid.",
          "oneOf": [
            {
              "description": "It is fetching, or would start on the next thing handed to it.",
              "type": "string",
              "const": "fetching"
            },
            {
              "description": "Nothing is moving and nothing new would start.",
              "type": "string",
              "const": "stopped"
            }
          ]
        },
        "Reached": {
          "description": "Where a month stands against a declared cap.",
          "oneOf": [
            {
              "description": "Comfortably inside it.",
              "type": "string",
              "const": "within"
            },
            {
              "description": "Close enough that there is still time to do something.",
              "type": "string",
              "const": "warning"
            },
            {
              "description": "Reached, and the declared behaviour applies.",
              "type": "string",
              "const": "exceeded"
            }
          ]
        },
        "Reading": {
          "description": "One direction's limit, as declared and as it comes to.",
          "type": "object",
          "properties": {
            "limit": {
              "description": "The limit as it was expressed.",
              "$ref": "#/$defs/Limit"
            },
            "resolved": {
              "description": "What it comes to against the measured line.",
              "$ref": "#/$defs/Resolved"
            },
            "says": {
              "description": "The limit and the line it was measured against, in one sentence.\n\nCarried rather than left to each surface, so the rule that a share is never\nshown without the figure it is a share of is kept in one place instead of\nthree.",
              "type": "string"
            }
          },
          "required": [
            "limit",
            "resolved",
            "says"
          ]
        },
        "Resolved": {
          "description": "What a limit comes to once it is weighed against a measured line.",
          "oneOf": [
            {
              "description": "Nothing holds it back.",
              "type": "object",
              "properties": {
                "is": {
                  "type": "string",
                  "const": "unlimited"
                }
              },
              "required": [
                "is"
              ]
            },
            {
              "description": "This many bytes a second.",
              "type": "object",
              "properties": {
                "bytes_per_second": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                },
                "is": {
                  "type": "string",
                  "const": "at"
                }
              },
              "required": [
                "is",
                "bytes_per_second"
              ]
            },
            {
              "description": "A proportion was asked for and nothing has measured the line.\n\nDeliberately not folded into [`Self::Unlimited`]. \"Half of an unknown\nnumber\" resolving to \"no limit at all\" is the shape of a setting an\noperator believes is in force while the stack takes the whole line.",
              "type": "object",
              "properties": {
                "is": {
                  "type": "string",
                  "const": "unmeasured"
                }
              },
              "required": [
                "is"
              ]
            }
          ]
        },
        "Restraint": {
          "description": "Where the line stands.",
          "oneOf": [
            {
              "description": "Nothing is configured.",
              "type": "string",
              "const": "unlimited"
            },
            {
              "description": "Limits are in force, with no schedule to switch them.",
              "type": "string",
              "const": "limited"
            },
            {
              "description": "Inside the household's active hours, so the limits apply.",
              "type": "string",
              "const": "scheduled-active"
            },
            {
              "description": "Outside them, so the line is the stack's.",
              "type": "string",
              "const": "scheduled-quiet"
            },
            {
              "description": "A temporary override is lifting the limits, and will expire.",
              "type": "string",
              "const": "overridden"
            },
            {
              "description": "The month is close enough to a declared cap to say so.",
              "type": "string",
              "const": "cap-warning"
            },
            {
              "description": "The cap has been reached and the declared behaviour applies.",
              "type": "string",
              "const": "cap-exceeded"
            }
          ]
        },
        "Rhythm": {
          "description": "The hours the household is awake, declared once for every download client.",
          "type": "object",
          "properties": {
            "from": {
              "description": "When the household's day starts.",
              "type": "string"
            },
            "to": {
              "description": "When it ends, which may be the next morning.",
              "type": "string"
            }
          },
          "required": [
            "from",
            "to"
          ]
        },
        "Sharing": {
          "description": "How the line is shared, and what that costs.",
          "type": "object",
          "properties": {
            "acting": {
              "description": "What a spent cap is doing to the figures above, where one is spent.",
              "type": [
                "string",
                "null"
              ]
            },
            "applied": {
              "description": "Whether this run wrote the limits to the clients or only read them.",
              "type": "boolean"
            },
            "cap": {
              "description": "The monthly cap, where one was declared.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Cap"
                },
                {
                  "type": "null"
                }
              ]
            },
            "capacity": {
              "description": "What the line was measured to carry.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Capacity"
                },
                {
                  "type": "null"
                }
              ]
            },
            "cautions": {
              "description": "What is worth knowing about that reading before trusting it.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "clients": {
              "description": "What each download client was asked and what it is doing about it.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Holding"
              }
            },
            "down": {
              "description": "The download limit.",
              "$ref": "#/$defs/Reading"
            },
            "means": {
              "description": "What that means for the household.",
              "type": "string"
            },
            "metered": {
              "description": "What the stack itself moved this month.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Metered"
                },
                {
                  "type": "null"
                }
              ]
            },
            "ratio": {
              "description": "What throttling the upload costs, where an upload limit is in force.",
              "type": [
                "string",
                "null"
              ]
            },
            "reached": {
              "description": "Where the month stands against it.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Reached"
                },
                {
                  "type": "null"
                }
              ]
            },
            "respite": {
              "description": "The override, where one is running or has just run out.",
              "$ref": "#/$defs/Standing"
            },
            "respite_says": {
              "description": "What the override amounts to, in words.",
              "type": [
                "string",
                "null"
              ]
            },
            "restraint": {
              "description": "Where the line stands.",
              "$ref": "#/$defs/Restraint"
            },
            "rhythm": {
              "description": "The household's hours, where any were declared.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Rhythm"
                },
                {
                  "type": "null"
                }
              ]
            },
            "untouched": {
              "description": "What is outside every limit here.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "up": {
              "description": "The upload limit, which is declared apart and defaults lower.",
              "$ref": "#/$defs/Reading"
            },
            "zone": {
              "description": "The zone the clients read those hours in, where the stack says.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "restraint",
            "means",
            "cautions",
            "down",
            "up",
            "respite",
            "clients",
            "untouched",
            "applied"
          ]
        },
        "Source": {
          "description": "Where a figure for the line came from.",
          "oneOf": [
            {
              "description": "The operator gave it, presumably off the plan they pay for.",
              "type": "string",
              "const": "declared"
            },
            {
              "description": "The fastest the stack has been seen to move, which is what it achieved\nrather than what the line is sold as.",
              "type": "string",
              "const": "observed"
            }
          ]
        },
        "Standing": {
          "description": "Where a respite stands against the clock.",
          "oneOf": [
            {
              "description": "None was asked for.",
              "type": "object",
              "properties": {
                "standing": {
                  "type": "string",
                  "const": "none"
                }
              },
              "required": [
                "standing"
              ]
            },
            {
              "description": "In force, with this long left.",
              "type": "object",
              "properties": {
                "seconds": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                },
                "standing": {
                  "type": "string",
                  "const": "in-force"
                }
              },
              "required": [
                "standing",
                "seconds"
              ]
            },
            {
              "description": "It ran out, this long ago. Said once, then cleared.",
              "type": "object",
              "properties": {
                "seconds": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                },
                "standing": {
                  "type": "string",
                  "const": "expired"
                }
              },
              "required": [
                "standing",
                "seconds"
              ]
            }
          ]
        },
        "Verdict": {
          "description": "What became of one limit, in one direction, on one client.",
          "oneOf": [
            {
              "description": "Nothing was asked for, so there is nothing to hold to.",
              "type": "string",
              "const": "unasked"
            },
            {
              "description": "This client has no such direction to limit at all.\n\nApart from a limit that was ignored, and deliberately: a Usenet client does\nnot upload, so an upload limit on one is not a setting it refused.",
              "type": "string",
              "const": "nothing-to-limit"
            },
            {
              "description": "It took the limit and is inside it.",
              "type": "string",
              "const": "holding"
            },
            {
              "description": "It reports a figure other than the one it was given.",
              "type": "string",
              "const": "ignored"
            },
            {
              "description": "It reports the right figure and is moving faster than it.",
              "type": "string",
              "const": "overrunning"
            }
          ]
        },
        "WhenExceeded": {
          "description": "What to do when a declared cap is reached.",
          "oneOf": [
            {
              "description": "Stop fetching until the month turns over.",
              "type": "string",
              "const": "pause"
            },
            {
              "description": "Keep going, slowly, so what is half-finished can finish.",
              "type": "string",
              "const": "throttle"
            },
            {
              "description": "Carry on. Some caps cost money and some only cost speed.",
              "type": "string",
              "const": "continue"
            }
          ]
        }
      }
    },
    "beside": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/BesideReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "BesideReport": {
          "description": "What standing lemonfiber beside an existing setup came to, or would come to.",
          "type": "object",
          "properties": {
            "ports": {
              "description": "Where each service would listen instead, lowest original port first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/MovedReport"
              }
            },
            "refusal": {
              "description": "Why nothing was written, where nothing was.",
              "type": [
                "string",
                "null"
              ]
            },
            "stance": {
              "description": "Where the act stands.",
              "$ref": "#/$defs/Stance"
            },
            "written": {
              "description": "Where the Compose file that says so was written.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "ports",
            "stance"
          ]
        },
        "MovedReport": {
          "description": "Where one service would listen to run beside what is already here.",
          "type": "object",
          "properties": {
            "from": {
              "description": "The port it would ordinarily take.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "service": {
              "description": "The lemonfiber service being moved.",
              "type": "string"
            },
            "to": {
              "description": "The port it would take instead.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            }
          },
          "required": [
            "service",
            "from",
            "to"
          ]
        },
        "Stance": {
          "description": "Where a proposed change stands once it has been read against what is in force.",
          "oneOf": [
            {
              "description": "The setting already holds what was asked for. Nothing to apply, nothing to\nconfirm, and the file is not touched — a write of the same value would move\nthe file's own timestamp and read afterwards as an edit somebody made.",
              "type": "string",
              "const": "unchanged"
            },
            {
              "description": "Staged and not applied: nothing has been written. Either it is consequential\nand nobody has said yes to it yet, or it was rehearsed.\n\nThe default, because nothing having happened yet is what every act starts as,\nand a report built and not filled in should say that rather than claim it acted.",
              "type": "string",
              "const": "pending"
            },
            {
              "description": "Turned away. It cannot be applied safely, the file is exactly as it was, and\nthe refusal says why.",
              "type": "string",
              "const": "blocked"
            },
            {
              "description": "Written.",
              "type": "string",
              "const": "applied"
            }
          ]
        }
      }
    },
    "bundle": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Bundle"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Bundle": {
          "description": "What a support request said: what a bundle holds, and where it is if it exists.\n\nOne record with an absent path rather than two shapes, because the two answers\nare the same answer at two moments: both list what goes in the file and say how\nlarge it is, and only one of them has a file to point at. A caller reads whether\nthere is a path to know which it has.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "How large the file is, or would be.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "contents": {
              "description": "Everything it holds, gathered, redacted and read back.",
              "$ref": "#/$defs/Contents"
            },
            "path": {
              "description": "Where it was written, or nothing where a run that writes nothing described it.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "contents",
            "bytes"
          ]
        },
        "Contents": {
          "description": "Everything gathered for a bundle, and everything that could not be.",
          "type": "object",
          "properties": {
            "missing": {
              "description": "What could not be collected, named.\n\nNamed rather than passed over: a bundle from a machine whose diagnostics will not\nrun is exactly the bundle worth having, and a gap nobody mentions reads as an\nabsence of trouble rather than as an absence of information.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "pieces": {
              "description": "The files, in the order a reader would want them.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Piece"
              }
            },
            "taken": {
              "description": "Where and when it came from.",
              "$ref": "#/$defs/Taken"
            },
            "terms": {
              "description": "How it was made, and what its operator chose.",
              "$ref": "#/$defs/Terms"
            }
          },
          "required": [
            "pieces",
            "missing",
            "taken",
            "terms"
          ]
        },
        "Filenames": {
          "description": "Whether media filenames are shown as they are.\n\nReplaced unless asked otherwise. A library's contents are not a credential, but they\nare the one thing in a bundle that says something about the person rather than about\nthe machine, and replacing them costs a diagnostic a reader can still follow — the\nmarks keep two mentions of one file recognisable as one file.\n\nRead from the bare flag a surface carries rather than from a name of its own,\nbecause that is what both surfaces have: `--filenames` on a command line and a\n`filenames` in a request body are one word that is there or is not. Which way\nround it reads is decided here, once — a surface that read it the other way\nround would put a library's contents in a file people post in public.",
          "type": "boolean"
        },
        "Piece": {
          "description": "One file inside a bundle: the name it will carry, and what it holds.\n\nHeld in memory rather than written as it is gathered, because everything is read back\nbefore anything is written. A bundle that had already put one file on disk when it\nfound a credential in the next would have to be unwritten, and unwriting is the kind of\nthing that half-works.",
          "type": "object",
          "properties": {
            "body": {
              "description": "What it holds, already redacted.",
              "type": "string"
            },
            "name": {
              "description": "What it is called inside the bundle.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "body"
          ]
        },
        "Taken": {
          "description": "What a reader needs to know before reading a word of the bundle.\n\nAn operator pasting last week's bundle into this week's thread is the commonest way one\nof those threads goes wrong, and nothing in the contents tells either of them.",
          "type": "object",
          "properties": {
            "at": {
              "description": "When, as a service writes a moment.",
              "type": "string"
            },
            "lemonfiber": {
              "description": "The lemonfiber that wrote it.",
              "type": "string"
            },
            "stack": {
              "description": "The stack it was written from.",
              "type": "string"
            }
          },
          "required": [
            "lemonfiber",
            "stack",
            "at"
          ]
        },
        "Terms": {
          "description": "How a bundle was made: what was bounded, what was replaced, and what its operator asked\nto have shown as it is.\n\nCarried in the bundle rather than known only to the command that wrote it, because the\nperson who reads one is usually not the person who chose any of this.",
          "type": "object",
          "properties": {
            "filenames": {
              "description": "Whether media filenames were shown.",
              "$ref": "#/$defs/Filenames"
            },
            "revealed": {
              "description": "The settings the operator asked to have shown as they are.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "window": {
              "description": "How much of the logs was taken, said as it would be said aloud.",
              "type": "string"
            }
          },
          "required": [
            "window",
            "filenames",
            "revealed"
          ]
        }
      }
    },
    "clients": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Guidance"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Cause": {
          "description": "One thing that could be behind a symptom, and how to tell it from the others.",
          "type": "object",
          "properties": {
            "because": {
              "description": "What is wrong.",
              "type": "string"
            },
            "fix": {
              "description": "What to do about it.",
              "type": "string"
            },
            "tell": {
              "description": "How to tell this cause from the others under the same symptom.",
              "type": "string"
            }
          },
          "required": [
            "because",
            "tell",
            "fix"
          ]
        },
        "Device": {
          "description": "A kind of device somebody in the house might watch on.",
          "type": "object",
          "properties": {
            "caution": {
              "description": "What is worth knowing before starting, where anything is.",
              "type": [
                "string",
                "null"
              ]
            },
            "client": {
              "description": "What to use on it.",
              "type": "string"
            },
            "device": {
              "description": "What somebody would call the device they are holding.",
              "type": "string"
            },
            "instead": {
              "description": "What to do instead where this is a bad device to be stuck with.",
              "type": [
                "string",
                "null"
              ]
            },
            "support": {
              "description": "How well served it is.",
              "$ref": "#/$defs/Support"
            }
          },
          "required": [
            "device",
            "support",
            "client"
          ]
        },
        "Guidance": {
          "description": "The guidance in full, for a surface that shows all of it.",
          "type": "object",
          "properties": {
            "devices": {
              "description": "Every device, in the order somebody is likely to be holding one.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Device"
              }
            },
            "nothing_is_installed": {
              "description": "What this will not do for them.",
              "type": "string"
            },
            "only_at_home": {
              "description": "True of every device, said once.",
              "type": "string"
            },
            "straining": {
              "description": "Why playback here is likely to struggle before any app is chosen, or `None`\nwhere the preset in force asks for nothing this platform cannot serve.\n\nAbsent far more often than present, and it must be: a caution shown to\neverybody says nothing about anybody's machine, and a reader who meets one\nevery time stops reading it.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Straining"
                },
                {
                  "type": "null"
                }
              ]
            },
            "trouble": {
              "description": "What to do when it does not work, keyed by the symptom.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Trouble"
              }
            }
          },
          "required": [
            "devices",
            "trouble",
            "only_at_home",
            "nothing_is_installed"
          ]
        },
        "Straining": {
          "description": "Why playback here is likely to struggle, whatever app the household installs.\n\nPresent only where the preset in force asks for transcoding this platform cannot\ndo in hardware. It belongs to the guidance rather than to any one device: the\npreset and the platform decide it between them, and every device in the table\nmeets it.",
          "type": "object",
          "properties": {
            "caution": {
              "description": "What that preset asks of this machine, and what playback does where this\nmachine cannot give it.",
              "type": "string"
            },
            "instead": {
              "description": "What makes it stop.",
              "type": "string"
            },
            "preset": {
              "description": "The preset in force, under the name it was chosen by.",
              "type": "string"
            }
          },
          "required": [
            "preset",
            "caution",
            "instead"
          ]
        },
        "Support": {
          "description": "How well a device is served.",
          "oneOf": [
            {
              "description": "An official app that works. Most people, most of the time.",
              "type": "string",
              "const": "good"
            },
            {
              "description": "It works, with something worth knowing before starting.",
              "type": "string",
              "const": "workable"
            },
            {
              "description": "Poorly served. Said plainly, with somewhere else to go.",
              "type": "string",
              "const": "poor"
            },
            {
              "description": "No installation, works anywhere, and is never unavailable.",
              "type": "string",
              "const": "fallback"
            }
          ]
        },
        "Trouble": {
          "description": "Something somebody reports, and what is likely behind it.\n\nKeyed by the symptom rather than the cause: the person asking has the symptom,\nand which cause it is is the thing they cannot yet say.",
          "type": "object",
          "properties": {
            "causes": {
              "description": "What is likely behind it, most likely first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Cause"
              }
            },
            "symptom": {
              "description": "What somebody says is happening, in their words.",
              "type": "string"
            }
          },
          "required": [
            "symptom",
            "causes"
          ]
        }
      }
    },
    "config": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/ConfigReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Active": {
          "description": "One download still coming down when a reduction was asked for.",
          "type": "object",
          "properties": {
            "name": {
              "description": "What it is, as the client names it.",
              "type": "string"
            },
            "progress": {
              "description": "How far along, from zero to a hundred.",
              "type": "integer",
              "format": "uint8",
              "maximum": 255,
              "minimum": 0
            },
            "protocol": {
              "description": "Which client has it.",
              "type": "string"
            }
          },
          "required": [
            "protocol",
            "name",
            "progress"
          ]
        },
        "Change": {
          "description": "The difference between the configuration in force and the one proposed.\n\nOne setting, because one call changes one setting. What makes it a diff rather than\na value is `from`: an operator deciding whether to go ahead is deciding between two\nthings, and a report that showed only the new one would be asking them to remember\nthe old one correctly.",
          "type": "object",
          "properties": {
            "cost": {
              "description": "Whether applying it is cheap or consequential.",
              "$ref": "#/$defs/Cost"
            },
            "from": {
              "description": "What it holds now, withheld where it is a credential, and absent where the\nsetting holds nothing yet.",
              "type": [
                "string",
                "null"
              ]
            },
            "key": {
              "description": "The setting the change names.",
              "type": "string"
            },
            "to": {
              "description": "What it would hold, withheld the same way.",
              "type": "string"
            }
          },
          "required": [
            "key",
            "to",
            "cost"
          ]
        },
        "ConfigReport": {
          "description": "The answer to a configuration command.",
          "type": "object",
          "properties": {
            "changed": {
              "description": "Whether this command changed, or would change, a setting.",
              "type": "boolean"
            },
            "consequence": {
              "description": "What this change costs, where making it decides something with a\nconsequence — moving the library, turning port forwarding off, or naming a\nfront door. Stated for a change that is only staged as well as one that\nlanded, since the moment before it happens is the moment it is worth reading.\nAbsent for a read, and for a change to a setting nobody catalogued a cost for.",
              "type": [
                "string",
                "null"
              ]
            },
            "rehearsed": {
              "description": "Whether this was a rehearsal, so a change that `changed` reports was one\nthat *would* be made rather than one that was.",
              "type": "boolean"
            },
            "review": {
              "description": "The difference between the configuration in force and the one proposed, and\nwhere that proposal stands: applied, staged for a confirmation, turned away,\nor nothing to do. Absent for a read, which proposes nothing.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Review"
                },
                {
                  "type": "null"
                }
              ]
            },
            "settings": {
              "description": "The settings asked about — one for a lookup, all of them for a listing.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/SettingReport"
              }
            }
          },
          "required": [
            "settings",
            "changed",
            "rehearsed"
          ]
        },
        "Cost": {
          "description": "What changing a decision costs.",
          "oneOf": [
            {
              "description": "Applied with a restart of the services it affects, and nothing else moves.",
              "type": "string",
              "const": "cheap"
            },
            {
              "description": "Said and confirmed before it is applied: it may move data, invalidate library\npaths, or take a service away.",
              "type": "string",
              "const": "consequential"
            }
          ]
        },
        "Edited": {
          "description": "A setting changed outside lemonfiber since it last wrote one.\n\nBoth sides, so the operator chooses between them rather than being told one of\nthem lost. Values a listing withholds are withheld here too — a report a script\ncan log must not be the one place a password is printed.",
          "type": "object",
          "properties": {
            "found": {
              "description": "What the file holds now.",
              "type": "string"
            },
            "secret": {
              "description": "Whether either value was withheld rather than shown.",
              "type": "boolean"
            },
            "wrote": {
              "description": "What lemonfiber last wrote there.",
              "type": "string"
            }
          },
          "required": [
            "wrote",
            "found",
            "secret"
          ]
        },
        "Findings": {
          "description": "What a proposed change comes to on this machine.\n\nEmpty on every change that comes to nothing beyond its value, which is most of\nthem: a report full of empty lists about a timezone would teach the operator to\nskip the one that matters.",
          "type": "object",
          "properties": {
            "active": {
              "description": "What is still coming down, where a reduction would interrupt it.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Active"
              }
            },
            "edited": {
              "description": "The hand-edit found in the configuration file, where one was found.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Edited"
                },
                {
                  "type": "null"
                }
              ]
            },
            "keeps": {
              "description": "What this change leaves exactly as it is, said in full — because an operator\ndropping a way of downloading is weighing whether they lose what they built\nwith it.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "library": {
              "description": "The library paths the services hold, and what moving the data location does\nto each. Empty for every change that does not move it.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/LibraryPath"
              }
            },
            "opens": {
              "description": "What this change newly asks the operator for, in the order they meet it.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Opening"
              }
            },
            "stops": {
              "description": "What this change stops running, by service name.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "opens",
            "stops",
            "keeps",
            "library",
            "active"
          ]
        },
        "LibraryPath": {
          "description": "One library path a service files into, and what moving the data location does\nto it.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why it does or does not, in the operator's terms.",
              "type": "string"
            },
            "carried": {
              "description": "Whether the library at this path survives the move.",
              "type": "boolean"
            },
            "host": {
              "description": "The host directory it would resolve to after the move, where the move can\nresolve it at all.",
              "type": [
                "string",
                "null"
              ]
            },
            "path": {
              "description": "The path as that service holds it, which is a path inside its container.",
              "type": "string"
            },
            "service": {
              "description": "The service holding it.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "path",
            "carried",
            "because"
          ]
        },
        "Opening": {
          "description": "One thing a way of downloading newly asks the operator for.\n\nWhat is opened and nothing beside it: an operator adding Usenet is shown the\nUsenet provider and the settings its login is kept in, and never the tunnel,\nwhich they neither need nor asked about.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why this protocol needs it.",
              "type": "string"
            },
            "setting": {
              "description": "The setting its answer is kept in, where it is kept in one. Absent for an\naccount the operator has to go and obtain, which no setting holds.",
              "type": [
                "string",
                "null"
              ]
            },
            "what": {
              "description": "What it is, in the operator's terms.",
              "type": "string"
            }
          },
          "required": [
            "what",
            "because"
          ]
        },
        "Review": {
          "description": "A proposed change, read against what is in force, and where it stands.",
          "type": "object",
          "properties": {
            "change": {
              "description": "The difference, as it would be applied.",
              "$ref": "#/$defs/Change"
            },
            "findings": {
              "description": "What the change comes to on this machine, beyond the value it changes.\n\nFilled by whoever went and asked — the services where they file, the clients\nwhat they are fetching — and empty on every change that comes to nothing\nbeyond its value. Carried on a staged proposal as well as an applied one: a\nreview that withheld this until after the yes would be asking for a yes to\nsomething unstated.",
              "$ref": "#/$defs/Findings",
              "default": {
                "active": [],
                "keeps": [],
                "library": [],
                "opens": [],
                "stops": []
              }
            },
            "proof": {
              "description": "What proving the replacement credential came to, where one was proven.\n\nA replacement is proven against its live service before the credential it\nreplaces is discarded, so this is present for exactly those settings and\nabsent everywhere else.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Validation"
                },
                {
                  "type": "null"
                }
              ]
            },
            "refusal": {
              "description": "Why nothing was written, where nothing was and the reason is not simply that\nsomebody has yet to say yes.",
              "type": [
                "string",
                "null"
              ]
            },
            "stance": {
              "description": "Where the proposal stands.",
              "$ref": "#/$defs/Stance"
            }
          },
          "required": [
            "change",
            "stance"
          ]
        },
        "SettingReport": {
          "description": "One setting, as it is safe to show.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The setting's name.",
              "type": "string"
            },
            "secret": {
              "description": "Whether the value was withheld.",
              "type": "boolean"
            },
            "value": {
              "description": "Its value, or a note that it is set and withheld.",
              "type": "string"
            }
          },
          "required": [
            "key",
            "value",
            "secret"
          ]
        },
        "Stance": {
          "description": "Where a proposed change stands once it has been read against what is in force.",
          "oneOf": [
            {
              "description": "The setting already holds what was asked for. Nothing to apply, nothing to\nconfirm, and the file is not touched — a write of the same value would move\nthe file's own timestamp and read afterwards as an edit somebody made.",
              "type": "string",
              "const": "unchanged"
            },
            {
              "description": "Staged and not applied: nothing has been written. Either it is consequential\nand nobody has said yes to it yet, or it was rehearsed.\n\nThe default, because nothing having happened yet is what every act starts as,\nand a report built and not filled in should say that rather than claim it acted.",
              "type": "string",
              "const": "pending"
            },
            {
              "description": "Turned away. It cannot be applied safely, the file is exactly as it was, and\nthe refusal says why.",
              "type": "string",
              "const": "blocked"
            },
            {
              "description": "Written.",
              "type": "string",
              "const": "applied"
            }
          ]
        },
        "Validation": {
          "description": "What proving a credential against its live service established — never the\ninput, only the outcome.\n\nRead back as well as built. A surface that is not in this process asks setup to\nprove a credential and is told what came of it, so the four outcomes are tagged\nby name rather than distinguished by which field is present — the same reason an\nanswer carries the step it belongs to.",
          "oneOf": [
            {
              "description": "Proven working, carrying the capability observed while proving it.",
              "type": "object",
              "properties": {
                "observed": {
                  "description": "The observed fact — what the service did, not that it merely answered.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "valid"
                }
              },
              "required": [
                "outcome",
                "observed"
              ]
            },
            {
              "description": "The service answered and refused: the credential is wrong for it.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "What the service said, in terms the operator can act on.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "rejected"
                }
              },
              "required": [
                "outcome",
                "detail"
              ]
            },
            {
              "description": "Nothing usable answered, so nothing can be concluded about the credential.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "Why nothing usable came back.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "unreachable"
                }
              },
              "required": [
                "outcome",
                "detail"
              ]
            },
            {
              "description": "It authenticated, but cannot do the job it is for — exhausted, limited, or\notherwise unable.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "What it can no longer do, and why where the service says.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "degraded"
                }
              },
              "required": [
                "outcome",
                "detail"
              ]
            }
          ]
        }
      }
    },
    "credentials": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Inventory"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Held": {
          "description": "One credential, described without being disclosed.\n\nThere is deliberately no value here, and no field a value could be put in later\nwithout the change being visible in review.",
          "type": "object",
          "properties": {
            "advisory": {
              "description": "What is worth saying about this one, where anything is.",
              "type": [
                "string",
                "null"
              ]
            },
            "consumers": {
              "description": "Everything that authenticates with it. Named individually, because a\nconsumer left off this list is a consumer a rotation would silently strand.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "fingerprint": {
              "description": "A short likeness of the value, for telling two copies apart in a report.\n\nAbsent where there is no value to take one of. Never reversible and never a\nproof — see [`fingerprint`].",
              "type": [
                "string",
                "null"
              ]
            },
            "location": {
              "description": "Where the value lives, as a path or a description of one.",
              "type": "string"
            },
            "name": {
              "description": "What it is, in the operator's words — `qBittorrent web UI password`.",
              "type": "string"
            },
            "origin": {
              "description": "Who produced it.",
              "$ref": "#/$defs/Origin"
            },
            "setting": {
              "description": "The setting it is recorded under, which is a name and never a value.",
              "type": "string"
            },
            "state": {
              "description": "Where it stands.",
              "$ref": "#/$defs/State"
            }
          },
          "required": [
            "name",
            "setting",
            "consumers",
            "location",
            "origin",
            "state"
          ]
        },
        "Inventory": {
          "description": "The whole answer to a question about credentials.\n\nEvery ask answers with the inventory as it now stands, and adds what became of\nwhatever else was asked for. An operator who has just rotated something wants to\nsee the inventory that rotation produced, not a receipt they have to go and check\nagainst one.",
          "type": "object",
          "properties": {
            "held": {
              "description": "Every credential, whether or not it is present.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Held"
              }
            },
            "protection": {
              "description": "What keeping them in files does and does not protect against.",
              "$ref": "#/$defs/Protection"
            },
            "revealed": {
              "description": "One value, where one was asked for.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Revealed"
                },
                {
                  "type": "null"
                }
              ]
            },
            "rotated": {
              "description": "What became of a rotation, where one was asked for.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Rotation"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "held",
            "protection"
          ]
        },
        "Origin": {
          "description": "Who produced a credential, which decides what can be done about it.",
          "oneOf": [
            {
              "description": "The operator supplied it, from an account they hold somewhere else.",
              "type": "string",
              "const": "operator"
            },
            {
              "description": "The service minted it for itself and wrote it into its own configuration.",
              "type": "string",
              "const": "service"
            },
            {
              "description": "lemonfiber minted it, because the service offers nothing durable to read.",
              "type": "string",
              "const": "lemonfiber"
            }
          ]
        },
        "Propagation": {
          "description": "One consumer, and how far the rotation reached it.",
          "type": "object",
          "properties": {
            "consumer": {
              "description": "What authenticates with the credential.",
              "type": "string"
            },
            "reach": {
              "description": "How far the rotation reached it.",
              "$ref": "#/$defs/Reach"
            }
          },
          "required": [
            "consumer",
            "reach"
          ]
        },
        "Protection": {
          "description": "The honest account of what the credential store protects against.\n\nTwo lists and a sentence, carried as data rather than printed here, so the API\nserves the same words the terminal prints and neither can drift into a claim the\nother does not make.",
          "type": "object",
          "properties": {
            "against": {
              "description": "What it protects against.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "not_against": {
              "description": "What it does not protect against.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "summary": {
              "description": "What the storage is, before any claim about it.",
              "type": "string"
            }
          },
          "required": [
            "summary",
            "against",
            "not_against"
          ]
        },
        "Reach": {
          "description": "How far a rotation reached one consumer.",
          "oneOf": [
            {
              "description": "It now holds the replacement.",
              "type": "object",
              "properties": {
                "reach": {
                  "type": "string",
                  "const": "updated"
                }
              },
              "required": [
                "reach"
              ]
            },
            {
              "description": "It will hold the replacement once one more thing happens, and that thing is\nnamed. A consumer reading the value out of a container's environment has it\nfixed at the moment the container was created, so recording a new one is\nonly half of reaching it.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "What still has to happen, written as the command that does it.",
                  "type": "string"
                },
                "reach": {
                  "type": "string",
                  "const": "pending"
                }
              },
              "required": [
                "reach",
                "detail"
              ]
            },
            {
              "description": "It could not be updated. Named rather than dropped, because a consumer left\nholding the old value is the failure this list exists to surface.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "Why it could not be.",
                  "type": "string"
                },
                "reach": {
                  "type": "string",
                  "const": "failed"
                }
              },
              "required": [
                "reach",
                "detail"
              ]
            }
          ]
        },
        "Revealed": {
          "description": "One stored value, handed back because the operator asked for it and said so.\n\nSeparate from [`Held`] rather than a field on it, so that the inventory cannot\ncarry a value by accident: a surface that renders the inventory has nothing to\nrender, whatever it does.",
          "type": "object",
          "properties": {
            "name": {
              "description": "Which credential this is.",
              "type": "string"
            },
            "value": {
              "description": "The value, present only where the ask was confirmed.",
              "type": [
                "string",
                "null"
              ]
            },
            "warning": {
              "description": "What the operator is told before it appears, whether or not it appears.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "warning"
          ]
        },
        "Rotation": {
          "description": "What one rotation came to.",
          "type": "object",
          "properties": {
            "consumers": {
              "description": "Every consumer, and how far the rotation reached it.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Propagation"
              }
            },
            "credential": {
              "description": "Which credential was to be replaced.",
              "type": "string"
            },
            "settled": {
              "description": "What became of the replacement.",
              "$ref": "#/$defs/Settled"
            }
          },
          "required": [
            "credential",
            "settled",
            "consumers"
          ]
        },
        "Settled": {
          "description": "What became of a rotation.",
          "oneOf": [
            {
              "description": "The replacement was proven and is now the value in force.",
              "type": "object",
              "properties": {
                "observed": {
                  "description": "What the service did while proving it — an observation, never the value.",
                  "type": "string"
                },
                "settled": {
                  "type": "string",
                  "const": "replaced"
                }
              },
              "required": [
                "settled",
                "observed"
              ]
            },
            {
              "description": "The service answered and refused the replacement. Nothing was changed.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "What the service said, with any credential in it withheld.",
                  "type": "string"
                },
                "settled": {
                  "type": "string",
                  "const": "refused"
                }
              },
              "required": [
                "settled",
                "detail"
              ]
            },
            {
              "description": "Nothing usable answered, so the replacement could not be proven. Nothing\nwas changed, because an unproven replacement is not a better one.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "Why nothing could be concluded.",
                  "type": "string"
                },
                "settled": {
                  "type": "string",
                  "const": "unproven"
                }
              },
              "required": [
                "settled",
                "detail"
              ]
            },
            {
              "description": "Nothing in this stack holds a credential by that name.",
              "type": "object",
              "properties": {
                "known": {
                  "description": "The names that would have been accepted.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "settled": {
                  "type": "string",
                  "const": "unknown"
                }
              },
              "required": [
                "settled",
                "known"
              ]
            },
            {
              "description": "A replacement for this one does not come from here.\n\nEither the operator's provider issued it, in which case inventing one would\nproduce a credential no service has ever heard of; or the service that holds\nit offers no way to change it in place. Either way what is owed is a\nsentence saying where a replacement does come from, not an attempt.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "Where a replacement comes from, and what to do once it exists.",
                  "type": "string"
                },
                "settled": {
                  "type": "string",
                  "const": "elsewhere"
                }
              },
              "required": [
                "settled",
                "detail"
              ]
            }
          ]
        },
        "State": {
          "description": "Where a credential stands, as far as lemonfiber can tell without spending it.\n\nSix states rather than a boolean because the operator's next move differs for\neach: an absent credential is one to supply, a stale one is one to prove, an\ninvalid one is one to replace, and the two rotation states exist so a run\ninterrupted half-way through a replacement is legible rather than mysterious.",
          "oneOf": [
            {
              "description": "Required by what this stack runs, and not supplied.",
              "type": "string",
              "const": "absent"
            },
            {
              "description": "Present, and proven against the service it authenticates to.",
              "type": "string",
              "const": "active"
            },
            {
              "description": "Present, and never proven — or not proven since it was last written.",
              "type": "string",
              "const": "stale"
            },
            {
              "description": "Present, and refused by the service the last time it was offered.",
              "type": "string",
              "const": "invalid"
            },
            {
              "description": "A replacement is being proven; the existing value is still the one in force.",
              "type": "string",
              "const": "rotating"
            },
            {
              "description": "Replaced. What is left is the old value, waiting to be destroyed.",
              "type": "string",
              "const": "superseded"
            }
          ]
        }
      }
    },
    "dashboard": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Snapshot"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Address": {
          "description": "Where the front door is reached, and what is worth knowing about the address.",
          "type": "object",
          "properties": {
            "caution": {
              "description": "What is worth knowing about the address itself, where anything is. Absent\nfor one that keeps working on its own.",
              "type": [
                "string",
                "null"
              ]
            },
            "url": {
              "description": "The whole address, as it would be typed or followed.",
              "type": "string"
            }
          },
          "required": [
            "url"
          ]
        },
        "Affected": {
          "description": "One thing that is wrong, as the expanded summary lists it.",
          "type": "object",
          "properties": {
            "check": {
              "description": "The check that raised it.",
              "type": "string"
            },
            "downstream": {
              "description": "What is also wrong because of this, counted with it rather than again.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "remedies": {
              "description": "What to do about it, most likely first.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "severity": {
              "description": "How bad it is.",
              "$ref": "#/$defs/Severity"
            },
            "summary": {
              "description": "What is wrong, in one line.",
              "type": "string"
            }
          },
          "required": [
            "check",
            "severity",
            "summary",
            "remedies",
            "downstream"
          ]
        },
        "Alert": {
          "description": "One interruption: what happened, which way, and how much it matters.",
          "type": "object",
          "properties": {
            "affected": {
              "description": "Every check this alert speaks for, the first being [`Self::check`]. More\nthan one where the same event was grouped across several services.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "check": {
              "description": "The check this came from, so an alert and its condition cannot drift apart.\nWhere several were grouped, the first of them.",
              "type": "string"
            },
            "kind": {
              "description": "What kind of event it is, shared by every instance of it.",
              "type": "string"
            },
            "moment": {
              "description": "Which way it went.",
              "$ref": "#/$defs/Moment"
            },
            "remedies": {
              "description": "What to do about it, most likely first. An alert that says what happened\nand not what to do is a notification, which is a different and worse thing.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "severity": {
              "description": "How much it matters. A resolution takes the severity of what resolved,\nbecause \"the critical thing is over\" is itself worth the attention the\ncritical thing had.",
              "$ref": "#/$defs/Severity"
            },
            "summary": {
              "description": "What happened, in the words the condition was raised with.",
              "type": "string"
            }
          },
          "required": [
            "check",
            "kind",
            "moment",
            "severity",
            "summary",
            "remedies",
            "affected"
          ]
        },
        "Beside": {
          "description": "A service the household can reach that is not the front door, and why it is not.\n\nCarried rather than left out, because the decision is the useful part: an operator\nwho can see that the index over every service was considered and refused has been\ntold something, where one shown a single name has only been given an answer.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why it is not somewhere to begin.",
              "type": "string"
            },
            "facing": {
              "description": "What it is to the household.",
              "$ref": "#/$defs/Facing"
            },
            "service": {
              "description": "The service, by the name it shows itself under.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "facing",
            "because"
          ]
        },
        "Chosen": {
          "description": "How the front door came to be the one it is.",
          "oneOf": [
            {
              "description": "Worked out from what the stack declares, which is what a stack whose\noperator has named nothing answers.",
              "type": "object",
              "properties": {
                "chosen": {
                  "type": "string",
                  "const": "derived"
                }
              },
              "required": [
                "chosen"
              ]
            },
            {
              "description": "Named by the operator, by the id the stack declares it under, and it is the\ndoor.",
              "type": "object",
              "properties": {
                "chosen": {
                  "type": "string",
                  "const": "named"
                },
                "door": {
                  "type": "string"
                }
              },
              "required": [
                "chosen",
                "door"
              ]
            },
            {
              "description": "Named by the operator and refused. The worked-out door stands, and this\ncarries what was named and why it is not it.",
              "type": "object",
              "properties": {
                "chosen": {
                  "type": "string",
                  "const": "refused"
                },
                "door": {
                  "$ref": "#/$defs/Refusal"
                }
              },
              "required": [
                "chosen",
                "door"
              ]
            }
          ]
        },
        "Counted": {
          "description": "One of the two counts the request service keeps, and what it has left.",
          "type": "object",
          "properties": {
            "limit": {
              "description": "How many the period allows. Absent where nothing limits them, which is a\ndifferent answer from a limit of nought.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "period": {
              "description": "How long the period is, in the words a household says it in — absent where\nthe count runs from the beginning rather than over a window.",
              "type": [
                "string",
                "null"
              ]
            },
            "remaining": {
              "description": "How many more they may ask for. Absent where nothing limits them.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "used": {
              "description": "How many the period has already counted against them.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "used"
          ]
        },
        "Criticality": {
          "description": "How much a service's absence costs.\n\nSerialisable as well as readable, because it reaches an operator: a status\nreport that says a service is down without saying whether that matters\nleaves them to guess, and the manifest already holds the answer.",
          "oneOf": [
            {
              "description": "Its failure has consequences outside the machine.",
              "type": "string",
              "const": "critical"
            },
            {
              "description": "The form does not work without it.",
              "type": "string",
              "const": "core"
            },
            {
              "description": "The form works, badly.",
              "type": "string",
              "const": "important"
            },
            {
              "description": "Makes things better.",
              "type": "string",
              "const": "enhancing"
            },
            {
              "description": "Nice to have.",
              "type": "string",
              "const": "optional"
            }
          ]
        },
        "Duration": {
          "type": "object",
          "properties": {
            "nanos": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "secs": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "secs",
            "nanos"
          ]
        },
        "Estimate": {
          "description": "About how much room one request will want.\n\nCarried as a number and a word rather than as a rendered string, so a surface can\nput it in a column and still say what it is. The word is not decoration: a figure\nwith nothing hedging it is a promise, and this cannot keep one.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "About how many bytes it will take.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "measured": {
              "description": "Whether this was measured. Always false — nothing here has measured anything,\nand the field is present so that a surface rendering it cannot forget to say so.",
              "type": "boolean"
            }
          },
          "required": [
            "bytes",
            "measured"
          ]
        },
        "Facing": {
          "description": "What a service published to the local network is to the people in the house.",
          "oneOf": [
            {
              "description": "Where asking for something begins, and where what was asked for is followed.\nIt links onward to the library, which is what makes it a place to start\nrather than one more address to be sent.",
              "type": "string",
              "const": "asking"
            },
            {
              "description": "The library, where what arrived is watched. A place to start only where there\nis nothing to ask for, because somebody sent there has no way to ask.",
              "type": "string",
              "const": "watching"
            },
            {
              "description": "One kind of media and no other, reached from the library rather than instead\nof it.",
              "type": "string",
              "const": "shelf"
            },
            {
              "description": "An index over every service, including the ones nobody in the house should\nlearn exist.",
              "type": "string",
              "const": "operators"
            },
            {
              "description": "How the others are reached, rather than one of them.",
              "type": "string",
              "const": "carriage"
            },
            {
              "description": "Published to the household, and nothing here says what it is to them.",
              "type": "string",
              "const": "unstated"
            }
          ]
        },
        "FrontDoorReport": {
          "description": "The household's one front door: which service it is, where it stands, and what\nelse they can reach that is not it.",
          "type": "object",
          "properties": {
            "address": {
              "description": "The address to hand them, read from this machine at the moment of asking\nrather than remembered. Absent where there is no door, and where there is\none on a machine that will say neither what it is called nor where it is.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Address"
                },
                {
                  "type": "null"
                }
              ]
            },
            "beside": {
              "description": "Everything else the household can reach, and why none of it is the door.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Beside"
              }
            },
            "chosen": {
              "description": "How this came to be the door: worked out from what the stack declares, named\nby the operator, or named by them and refused.\n\nCarried as a state rather than left to the sentence beneath it, for the reason\nthe standing is: an operator whose setting was refused reads the sentence, and\na browser, a script or a dashboard reads this.",
              "$ref": "#/$defs/Chosen"
            },
            "facing": {
              "description": "What that service is to them. Absent for the same reason.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Facing"
                },
                {
                  "type": "null"
                }
              ]
            },
            "meaning": {
              "description": "What this comes to, in the words an operator would say it in — including,\nwhere there is no door, that there is none.",
              "type": "string"
            },
            "service": {
              "description": "The service the household begins at, by the name it shows itself under.\nAbsent where this stack publishes nothing they could begin at.",
              "type": [
                "string",
                "null"
              ]
            },
            "standing": {
              "description": "Where the front door stands.",
              "$ref": "#/$defs/Standing2"
            }
          },
          "required": [
            "standing",
            "chosen",
            "meaning",
            "beside"
          ]
        },
        "Hardlink": {
          "description": "Whether imports are hardlinking or copying — the difference between an import\nthat is free and one that doubles the disk it uses.",
          "oneOf": [
            {
              "description": "Imports hardlink, as they should.",
              "type": "string",
              "const": "linking"
            },
            {
              "description": "Imports copy, doubling space and slowing every import.",
              "type": "string",
              "const": "copying"
            },
            {
              "description": "It could not be established which.",
              "type": "string",
              "const": "unknown"
            }
          ]
        },
        "HouseholdMember": {
          "description": "One household member: who they are, what they may watch, when they were last\nseen, and everything they have asked for.",
          "type": "object",
          "properties": {
            "access": {
              "description": "What they may watch.",
              "$ref": "#/$defs/MemberAccess"
            },
            "asking": {
              "description": "What they may ask for, and what their period has left of it.\n\nAbsent where the request service holds no account for them, and where it could\nnot be asked — an unread answer is not an unlimited member, and reporting one as\nthe other would tell an operator their quota was never applied.",
              "anyOf": [
                {
                  "$ref": "#/$defs/MemberAsking"
                },
                {
                  "type": "null"
                }
              ]
            },
            "claimed": {
              "description": "Whether somebody has set a password on the account. False is an invitation\nnobody has taken up rather than a member who is not here.",
              "type": "boolean"
            },
            "last_seen": {
              "description": "When the media server last saw them, as it timestamps it. Absent where nobody\nhas ever signed in, which is exactly the unclaimed invitations.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "The member, by the name their account is held under.",
              "type": "string"
            },
            "requests": {
              "description": "What they asked for, newest first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/MemberRequest"
              }
            },
            "to_hand_over": {
              "description": "What this member would be told, in the words they would read it in.\n\nEverything a household member is owed at the moment of asking and cannot be\nshown where they ask: what happens to what they ask for, what their period has\nleft and when it makes room, roughly what a thing costs before they choose one,\nwhat is still waiting on an answer, and what was refused and why. Written to\nthem rather than about them, so it can be handed over as it stands.\n\nEmpty where there is nothing to tell them — a member the request service holds\nno account for has no standing to report and nothing waiting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "name",
            "requests",
            "access",
            "claimed",
            "to_hand_over"
          ]
        },
        "HouseholdReport": {
          "description": "Who is in the household, what each may watch, and what each has asked for.",
          "type": "object",
          "properties": {
            "allows": {
              "description": "What that policy allows in a period, in the words a household says it in.\n\nAbsent where nothing limits the household, which is not the same as a policy\nthat could not be read: that one leaves [`Self::policy`] absent too.",
              "type": [
                "string",
                "null"
              ]
            },
            "available": {
              "description": "Whether the household could be read at all. A false here is why the list is\nempty, and keeps an unread record from being mistaken for an empty house — the\nsame honesty a trace keeps about a silence it did not hear.",
              "type": "boolean"
            },
            "filtering": {
              "description": "What the limits on this household are and are not, where anybody carries one.\n\nAbsent on a household nobody has limited, because there is no claim to be modest\nabout. Present the moment there is one, because a parent who has set a limit is\nexactly the reader who might take it for a lock.",
              "type": [
                "string",
                "null"
              ]
            },
            "findings": {
              "description": "What could not be read, and anything else worth the operator's attention.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "members": {
              "description": "Everybody the media server holds an account for, in name order — including\nthose who have never asked for anything, and the invitations nobody has taken\nup yet.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/HouseholdMember"
              }
            },
            "policy": {
              "description": "What happens to what the household asks for where nobody chose otherwise for\none person. Absent where the request service could not be asked.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Policy"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "members",
            "available",
            "findings"
          ]
        },
        "MemberAccess": {
          "description": "What one member may watch, in the household's own words.\n\nRead off the media server's account rather than kept here: that is where access is\ndecided, and a second copy is a copy able to disagree.",
          "type": "object",
          "properties": {
            "administrator": {
              "description": "Whether the account administers the media server.",
              "type": "boolean"
            },
            "age_limit": {
              "description": "The highest rating they may watch, where the operator set a limit.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "disabled": {
              "description": "Whether the account is switched off — held, but unable to sign in.",
              "type": "boolean"
            },
            "every_library": {
              "description": "Every library, rather than a chosen few. The ordinary case.",
              "type": "boolean"
            },
            "libraries": {
              "description": "The libraries they may watch where it is not every one, by the names the\noperator gave them — or by the server's identifiers where the library list\ncould not be read, which a finding says.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "rated": {
              "description": "What that limit comes to in the certificates this media server names, in the\noperator's own country. Absent where no limit is set.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Rated"
                },
                {
                  "type": "null"
                }
              ]
            },
            "restriction": {
              "description": "What they are held to, in one word — including where what they may watch and\nwhat they may ask for disagree.",
              "$ref": "#/$defs/Restriction"
            },
            "unrated": {
              "description": "What becomes of content the media server has no rating for.\n\nSaid either way, because an unexplained absence is the thing this answers: a\nrestricted member missing half the library is either this setting or a defect,\nand an operator cannot tell which from silence.",
              "$ref": "#/$defs/Unrated"
            }
          },
          "required": [
            "every_library",
            "libraries",
            "unrated",
            "restriction",
            "administrator",
            "disabled"
          ]
        },
        "MemberAsking": {
          "description": "What one member may ask for, where the request service could be asked.\n\nBoth counts, because the service keeps them apart and folding them would report a\nhousehold as within its limit while the half that matters is spent: television is\ncounted a season at a time, so one ask for a six-season series spends six.",
          "type": "object",
          "properties": {
            "films": {
              "description": "Films, counted one to a request.",
              "$ref": "#/$defs/Counted"
            },
            "frees_up": {
              "description": "When the count next lets go of something, so one more becomes possible.\n\nAbsent where nothing limits them, and where the request service's own dates\ncould not be read — an invented one would be a promise about a day on which\nnothing happens. The period is a window that rolls rather than a month that\nends, so this is the moment their earliest counted request ages out.",
              "type": [
                "string",
                "null"
              ]
            },
            "policy": {
              "description": "What happens to what they ask for.",
              "$ref": "#/$defs/Policy"
            },
            "standing": {
              "description": "Where they stand against what their period allows, taken over both counts.",
              "$ref": "#/$defs/Standing3"
            },
            "television": {
              "description": "Television, counted one to a season.",
              "$ref": "#/$defs/Counted"
            }
          },
          "required": [
            "policy",
            "standing",
            "films",
            "television"
          ]
        },
        "MemberRequest": {
          "description": "One thing a household member asked for, and where it stands in their words.",
          "type": "object",
          "properties": {
            "estimate": {
              "description": "About how much room it will want, at the quality in force.\n\nA guess and labelled as one — see [`crate::asking::Estimate`]. Absent where the\nrequest service names a kind this build does not know, since there is nothing to\nguess the length of.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Estimate"
                },
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "description": "The number the request service files it under, which is how one is named again\nwhen somebody rules on it.",
              "type": "integer",
              "format": "int64"
            },
            "media": {
              "description": "What kind of thing it is — a series, a film — in the household's own words.\nAbsent where the request service names a kind this build does not know.",
              "type": [
                "string",
                "null"
              ]
            },
            "refused": {
              "description": "Why it was turned down, where it was turned down from here.\n\n**The request service keeps none**, so this is lemonfiber's own record and is\nsaid to be — a reason presented as delivered would end the operator's job at\nexactly the point it begins. Absent on a request nobody has refused, and on one\nrefused in the request service itself, where there are no words to report and\ninventing some would put them in somebody's mouth.\n\nWhether the words were carried to the person who asked is the record's own\n`told`, which is why the two travel together: what an operator does next turns\non it, and a reason read without it is a reason of unknown standing.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Refused"
                },
                {
                  "type": "null"
                }
              ]
            },
            "state": {
              "description": "Where the request stands, or absent where the request service reports a status\nthis build does not know rather than guessing it into the nearest word.",
              "anyOf": [
                {
                  "$ref": "#/$defs/State2"
                },
                {
                  "type": "null"
                }
              ]
            },
            "title": {
              "description": "What it is called, where the service filing it has been told about it and its\nlibrary could be read. Absent for a request no service holds yet — one still\nawaiting approval has been handed to nobody, so there is no title to find.",
              "type": [
                "string",
                "null"
              ]
            },
            "waiting_days": {
              "description": "How many whole days it has been waiting on somebody, where it is waiting at all\nand the service's own date could be read.\n\nOnly on the ones nobody has ruled on. A request already answered has not been\nwaiting since it was made, and a figure beside one would be counting the wrong\nthing.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "id"
          ]
        },
        "Moment": {
          "description": "Which way a condition went.\n\nBoth directions are worth saying and neither is worth saying twice. An operator\ntold a disk filled up and never told it was resolved goes on believing it — so\nresolution is an alert in its own right rather than the absence of one.",
          "oneOf": [
            {
              "description": "It started.",
              "type": "string",
              "const": "onset"
            },
            {
              "description": "It stopped, having previously started.",
              "type": "string",
              "const": "resolved"
            }
          ]
        },
        "Panel": {
          "description": "A panel's content, or the reason its source could not fill it.\n\nThe difference between \"this panel is up to date\" and \"this panel's source is\nunreachable\" is the whole of degrading honestly: an unavailable panel says so,\nin its own words, rather than showing stale data as current or blank data as\nzero — and the panels beside it stay live.",
          "oneOf": [
            {
              "description": "The source answered; here is the panel.",
              "type": "object",
              "properties": {
                "data": {
                  "$ref": "#/$defs/Vpn"
                },
                "panel": {
                  "type": "string",
                  "const": "ready"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            },
            {
              "description": "The source could not be reached, for this stated reason.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "object",
                  "properties": {
                    "reason": {
                      "description": "Why the panel could not be filled, in the operator's terms.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "reason"
                  ]
                },
                "panel": {
                  "type": "string",
                  "const": "unavailable"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            }
          ]
        },
        "Panel2": {
          "description": "A panel's content, or the reason its source could not fill it.\n\nThe difference between \"this panel is up to date\" and \"this panel's source is\nunreachable\" is the whole of degrading honestly: an unavailable panel says so,\nin its own words, rather than showing stale data as current or blank data as\nzero — and the panels beside it stay live.",
          "oneOf": [
            {
              "description": "The source answered; here is the panel.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Transfer"
                  }
                },
                "panel": {
                  "type": "string",
                  "const": "ready"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            },
            {
              "description": "The source could not be reached, for this stated reason.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "object",
                  "properties": {
                    "reason": {
                      "description": "Why the panel could not be filled, in the operator's terms.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "reason"
                  ]
                },
                "panel": {
                  "type": "string",
                  "const": "unavailable"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            }
          ]
        },
        "Panel3": {
          "description": "A panel's content, or the reason its source could not fill it.\n\nThe difference between \"this panel is up to date\" and \"this panel's source is\nunreachable\" is the whole of degrading honestly: an unavailable panel says so,\nin its own words, rather than showing stale data as current or blank data as\nzero — and the panels beside it stay live.",
          "oneOf": [
            {
              "description": "The source answered; here is the panel.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Queue"
                  }
                },
                "panel": {
                  "type": "string",
                  "const": "ready"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            },
            {
              "description": "The source could not be reached, for this stated reason.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "object",
                  "properties": {
                    "reason": {
                      "description": "Why the panel could not be filled, in the operator's terms.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "reason"
                  ]
                },
                "panel": {
                  "type": "string",
                  "const": "unavailable"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            }
          ]
        },
        "Panel4": {
          "description": "A panel's content, or the reason its source could not fill it.\n\nThe difference between \"this panel is up to date\" and \"this panel's source is\nunreachable\" is the whole of degrading honestly: an unavailable panel says so,\nin its own words, rather than showing stale data as current or blank data as\nzero — and the panels beside it stay live.",
          "oneOf": [
            {
              "description": "The source answered; here is the panel.",
              "type": "object",
              "properties": {
                "data": {
                  "$ref": "#/$defs/Storage"
                },
                "panel": {
                  "type": "string",
                  "const": "ready"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            },
            {
              "description": "The source could not be reached, for this stated reason.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "object",
                  "properties": {
                    "reason": {
                      "description": "Why the panel could not be filled, in the operator's terms.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "reason"
                  ]
                },
                "panel": {
                  "type": "string",
                  "const": "unavailable"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            }
          ]
        },
        "Panel5": {
          "description": "A panel's content, or the reason its source could not fill it.\n\nThe difference between \"this panel is up to date\" and \"this panel's source is\nunreachable\" is the whole of degrading honestly: an unavailable panel says so,\nin its own words, rather than showing stale data as current or blank data as\nzero — and the panels beside it stay live.",
          "oneOf": [
            {
              "description": "The source answered; here is the panel.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Service"
                  }
                },
                "panel": {
                  "type": "string",
                  "const": "ready"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            },
            {
              "description": "The source could not be reached, for this stated reason.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "object",
                  "properties": {
                    "reason": {
                      "description": "Why the panel could not be filled, in the operator's terms.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "reason"
                  ]
                },
                "panel": {
                  "type": "string",
                  "const": "unavailable"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            }
          ]
        },
        "Panel6": {
          "description": "A panel's content, or the reason its source could not fill it.\n\nThe difference between \"this panel is up to date\" and \"this panel's source is\nunreachable\" is the whole of degrading honestly: an unavailable panel says so,\nin its own words, rather than showing stale data as current or blank data as\nzero — and the panels beside it stay live.",
          "oneOf": [
            {
              "description": "The source answered; here is the panel.",
              "type": "object",
              "properties": {
                "data": {
                  "$ref": "#/$defs/FrontDoorReport"
                },
                "panel": {
                  "type": "string",
                  "const": "ready"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            },
            {
              "description": "The source could not be reached, for this stated reason.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "object",
                  "properties": {
                    "reason": {
                      "description": "Why the panel could not be filled, in the operator's terms.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "reason"
                  ]
                },
                "panel": {
                  "type": "string",
                  "const": "unavailable"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            }
          ]
        },
        "Panel7": {
          "description": "A panel's content, or the reason its source could not fill it.\n\nThe difference between \"this panel is up to date\" and \"this panel's source is\nunreachable\" is the whole of degrading honestly: an unavailable panel says so,\nin its own words, rather than showing stale data as current or blank data as\nzero — and the panels beside it stay live.",
          "oneOf": [
            {
              "description": "The source answered; here is the panel.",
              "type": "object",
              "properties": {
                "data": {
                  "$ref": "#/$defs/HouseholdReport"
                },
                "panel": {
                  "type": "string",
                  "const": "ready"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            },
            {
              "description": "The source could not be reached, for this stated reason.",
              "type": "object",
              "properties": {
                "data": {
                  "type": "object",
                  "properties": {
                    "reason": {
                      "description": "Why the panel could not be filled, in the operator's terms.",
                      "type": "string"
                    }
                  },
                  "required": [
                    "reason"
                  ]
                },
                "panel": {
                  "type": "string",
                  "const": "unavailable"
                }
              },
              "required": [
                "panel",
                "data"
              ]
            }
          ]
        },
        "Passed": {
          "description": "Where a refusal's words were carried, and when.",
          "type": "object",
          "properties": {
            "at": {
              "description": "When the attempt was made, absent where the clock could not be written down.",
              "type": [
                "string",
                "null"
              ]
            },
            "to": {
              "description": "The services they reached, by the names the member knows them by. Empty where\nthere was nowhere this could send.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "to"
          ]
        },
        "Policy": {
          "description": "What happens to what a household member asks for.\n\nThree, and they are the three the request service can actually be put into. A\nhousehold is in one of them because of two settings taken together — whether requests\narrive unseen, and whether a period limits how many — so the words here are a reading\nof that pair rather than a fourth setting kept beside it.\n\nChoosing per person is not a fourth policy. It is one of these three chosen for one\nmember rather than for the house, which is why what a surface offers is a policy and,\nseparately, who it is for.",
          "oneOf": [
            {
              "description": "Everything asked for arrives, and nothing is counted against anybody.",
              "type": "string",
              "const": "trusted"
            },
            {
              "description": "Everything asked for arrives until the period's limit is spent, and what\nwould go past it is refused at the moment of asking.",
              "type": "string",
              "const": "within-a-limit"
            },
            {
              "description": "Everything asked for waits for the operator to rule on it.",
              "type": "string",
              "const": "everything-waits"
            }
          ]
        },
        "Protocol": {
          "description": "Which protocol a transfer is moving over, since the same download reads\ndifferently on each — a Usenet download has no peers, a torrent has no server.",
          "oneOf": [
            {
              "description": "A Usenet download.",
              "type": "string",
              "const": "usenet"
            },
            {
              "description": "A torrent download.",
              "type": "string",
              "const": "torrent"
            }
          ]
        },
        "Queue": {
          "description": "One `*arr`'s queue, and how much of it is stuck.",
          "type": "object",
          "properties": {
            "depth": {
              "description": "How many items are queued.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "service": {
              "description": "The service whose queue this is.",
              "type": "string"
            },
            "stuck": {
              "description": "How many of them are stuck rather than progressing.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "service",
            "depth",
            "stuck"
          ]
        },
        "Rated": {
          "description": "What one age limit comes to, in the certificates named for it.\n\nBoth sides, because either alone misleads. What is allowed without what is held\nback reads as a limit that stops nothing; what is held back without what is allowed\nreads as a limit that stops everything.",
          "type": "object",
          "properties": {
            "allows": {
              "description": "The certificates at the highest age this limit still lets through.\n\nEmpty where the table names nothing at or below the limit, which is a limit\nthat lets nothing rated through at all.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "fell_back": {
              "description": "Whether these came from lemonfiber's own mapping because the media server's\ntable named no certificates.\n\nCarried rather than hidden: a certificate said to be this household's when it\nis this program's is the kind of claim a parent would act on.",
              "type": "boolean"
            },
            "holds_back": {
              "description": "The certificates at the lowest age this limit holds back.\n\nEmpty where the table names nothing above the limit, which is a limit that\nholds nothing rated back at all.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "allows",
            "holds_back",
            "fell_back"
          ]
        },
        "Reading": {
          "description": "A figure a source reports, kept apart from the two ways it can be missing.\n\nZero is a value a source gave; stale is the last value a source that has since\ngone quiet gave; unknown is a source that never answered at all. Collapsing any\ntwo of them sends an operator after the wrong problem — a stalled download and\na dashboard that simply stopped polling look identical only if the code lets\nthem.",
          "oneOf": [
            {
              "description": "The source answered this refresh with a value — which may legitimately be\nzero.",
              "type": "object",
              "properties": {
                "reading": {
                  "type": "string",
                  "const": "known"
                },
                "value": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                }
              },
              "required": [
                "reading",
                "value"
              ]
            },
            {
              "description": "The source did not answer this refresh; this is the last value it gave.",
              "type": "object",
              "properties": {
                "reading": {
                  "type": "string",
                  "const": "stale"
                },
                "value": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                }
              },
              "required": [
                "reading",
                "value"
              ]
            },
            {
              "description": "The source has never answered, so nothing can be said about it.",
              "type": "object",
              "properties": {
                "reading": {
                  "type": "string",
                  "const": "unknown"
                }
              },
              "required": [
                "reading"
              ]
            }
          ]
        },
        "Refusal": {
          "description": "A named front door that is not one, and why it is not.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why this stack will not send a household there.",
              "type": "string"
            },
            "named": {
              "description": "What the operator recorded, as they wrote it.",
              "type": "string"
            }
          },
          "required": [
            "named",
            "because"
          ]
        },
        "Refused": {
          "description": "What was said when one request was turned down.",
          "type": "object",
          "properties": {
            "at": {
              "description": "When it was turned down, so somebody reading it later knows which answer this\nwas rather than assuming it is the newest.\n\nAbsent where the machine's clock could not be written as a date, which is a\nrefusal worth keeping the words of and not worth losing them over.",
              "type": [
                "string",
                "null"
              ]
            },
            "expired": {
              "description": "Whether nobody ruled on it and the period the household agreed to closed it,\nrather than an operator turning it down.\n\nThe words are then this program's own, which is why the two are told apart here\nand not left to be read off the sentence: an operator scanning for what happened\nwhile they were away is looking for exactly the ones nobody answered, and a\nmember is owed the difference between having been refused and having run out.\n\nAbsent from every record written before a household could arrange this, which is\nthe right reading of them — they are all somebody's own refusals.",
              "type": "boolean",
              "default": false
            },
            "reason": {
              "description": "Why, in the words it was turned down in.",
              "type": "string"
            },
            "told": {
              "description": "Whether the words have been carried to whoever asked, and where to.\n\nAbsent until the one attempt has been made. **This is what makes a second one\nimpossible rather than unlikely**: carrying happens only where this is absent,\nand it is written whether anything was reached or not — so a member with nowhere\nto send to is asked about once, and a household is never told the same thing\ntwice by a run that happened to be started twice.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Passed"
                },
                {
                  "type": "null"
                }
              ],
              "default": null
            }
          },
          "required": [
            "reason"
          ]
        },
        "Restriction": {
          "description": "What one member is held to, in the words a household would use.\n\nThe two restrictions are one decision and two services: the media server decides\nwhat may be *watched* and the request service what may be *asked for*. Setting one\nwithout the other is the hole this vocabulary exists to name — a child who cannot\nwatch something but can pull it into the library has parents who set a limit and got\nhalf of one.",
          "oneOf": [
            {
              "description": "Nothing is held back from them.",
              "type": "string",
              "const": "unrestricted"
            },
            {
              "description": "Held to a highest rating.",
              "type": "string",
              "const": "rating-limited"
            },
            {
              "description": "Held to some of the libraries.",
              "type": "string",
              "const": "library-limited"
            },
            {
              "description": "Held to both a rating and some of the libraries.",
              "type": "string",
              "const": "both"
            },
            {
              "description": "What they may watch is limited and what they may ask for is not.",
              "type": "string",
              "const": "inconsistent"
            }
          ]
        },
        "Service": {
          "description": "One service, as it stands.",
          "type": "object",
          "properties": {
            "criticality": {
              "description": "How much its absence costs, so a summary can weigh it.",
              "$ref": "#/$defs/Criticality"
            },
            "depends_on": {
              "description": "The services it needs before it can work, as the manifest declares them.\nCarried so a failure can be attributed to the thing underneath it rather\nthan counted as one more independent thing wrong.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "exit": {
              "description": "How it exited, where it has exited.",
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "id": {
              "description": "The service's identifier, which is also its Compose service name.",
              "type": "string"
            },
            "name": {
              "description": "What it is called in front of an operator.",
              "type": "string"
            },
            "profile": {
              "description": "The profile that declared it.",
              "type": "string"
            },
            "state": {
              "description": "What it is doing.",
              "$ref": "#/$defs/State"
            }
          },
          "required": [
            "id",
            "name",
            "profile",
            "state",
            "criticality",
            "depends_on"
          ]
        },
        "Severity": {
          "description": "How much a problem matters.\n\nFour levels, deliberately. More would not be applied consistently, and\ninconsistent severity is worse than coarse severity.",
          "oneOf": [
            {
              "description": "Informational; nothing is required.",
              "type": "string",
              "const": "advisory"
            },
            {
              "description": "Degraded or risky, still working.",
              "type": "string",
              "const": "warning"
            },
            {
              "description": "Something is broken.",
              "type": "string",
              "const": "error"
            },
            {
              "description": "Consequences outside the machine, or data at risk.",
              "type": "string",
              "const": "critical"
            }
          ]
        },
        "Snapshot": {
          "description": "Everything the dashboard shows at one moment.\n\nEach source's panel is filled or marked unavailable on its own, so one dead\nsource degrades one region rather than the screen. The surface builds this from\nwhat it gathered; the standing is read from the same facts so it cannot\ndisagree with the panels.",
          "type": "object",
          "properties": {
            "alerts": {
              "description": "What the operator has been told, newest first: what is owed them where a\nchannel is refusing, then what has already been said.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Alert"
              }
            },
            "door": {
              "description": "The one address to hand somebody who lives here.\n\nOn the screen rather than only behind a question, because the operator who\nneeds it is not the one who thought to ask: they have just been asked \"what\ndo I open?\" by somebody in the next room. Built from the same reading as the\npanels beside it, so the screen and `front-door` cannot name different doors.",
              "$ref": "#/$defs/Panel6"
            },
            "health": {
              "description": "The one-line health summary — the same computation every other surface\nuses, so no two of them can grade the same stack differently.\n\nAlways present, unlike the panels: a stack that could not be reached has a\nsummary, and it says `unknown`. An absent summary would leave the operator\nto infer health from a blank space, which is the one reading this must never\nbe open to.",
              "$ref": "#/$defs/Summary"
            },
            "household": {
              "description": "What the household has asked for that is not moving.\n\nOn the screen rather than only behind a question, for the reason the door\nbeside it is: a request waiting on a decision or failed after one is waiting\non the operator, and an operator who has to think to ask is one who finds out\nwhen somebody comes to complain.",
              "$ref": "#/$defs/Panel7"
            },
            "queue": {
              "description": "The per-service queues.",
              "$ref": "#/$defs/Panel3"
            },
            "services": {
              "description": "Every service and what it is doing.",
              "$ref": "#/$defs/Panel5"
            },
            "storage": {
              "description": "The storage picture.",
              "$ref": "#/$defs/Panel4"
            },
            "stuck": {
              "description": "What in the pipeline has stopped, worst first — assessed across the\ndownload clients and the \\*arrs together, because the failure that matters\nmost is invisible inside either.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Stuck"
              }
            },
            "telemetry": {
              "description": "Whether the screen itself can be trusted to be current.",
              "$ref": "#/$defs/Telemetry"
            },
            "transfers": {
              "description": "The active transfers.",
              "$ref": "#/$defs/Panel2"
            },
            "vpn": {
              "description": "The VPN, or `None` where no VPN is configured and the panel is omitted\nrather than shown permanently red.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Panel"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "telemetry",
            "health",
            "transfers",
            "queue",
            "stuck",
            "alerts",
            "storage",
            "services",
            "door",
            "household"
          ]
        },
        "Stall": {
          "description": "Why an item is not moving.\n\nOrdered by how much of the operator's attention each deserves, worst first, so\na summary that leads with the worst category needs no second ranking.",
          "oneOf": [
            {
              "description": "Fetched over and over: import is failing silently and being retried, which\nspends bandwidth and Usenet allowance indefinitely while looking normal.",
              "type": "string",
              "const": "redownload-loop"
            },
            {
              "description": "The same item has failed to import more than once. Structural — it will not\nresolve itself.",
              "type": "string",
              "const": "repeated-import-failure"
            },
            {
              "description": "Downloaded successfully and never imported. The failure nobody owns: the\nclient considers it finished, the \\*arr never picked it up, and from each\nservice's own perspective there is nothing wrong.",
              "type": "string",
              "const": "completed-not-imported"
            },
            {
              "description": "On disk, and no \\*arr knows about it.",
              "type": "string",
              "const": "orphaned"
            },
            {
              "description": "No progress at all beyond the threshold.",
              "type": "string",
              "const": "stalled-download"
            },
            {
              "description": "Monitored, never grabbed — nothing matched, or the indexers are answering\nwith nothing.",
              "type": "string",
              "const": "waiting-indefinitely"
            },
            {
              "description": "Moving, but slowly enough to be worth knowing about. Deliberately its own\ncategory rather than a stall: something still arriving needs patience, not\nintervention, and reporting the two alike is how a queue check gets muted.",
              "type": "string",
              "const": "slow"
            }
          ]
        },
        "Standing": {
          "description": "What the stack amounts to.\n\nOrdered from best to worst, so the worst of several is a `max` and there is no\nsecond place to encode the ranking.",
          "oneOf": [
            {
              "description": "Nothing is wrong.",
              "type": "string",
              "const": "healthy"
            },
            {
              "description": "Nothing is running, and that was on purpose.",
              "type": "string",
              "const": "stopped"
            },
            {
              "description": "Nothing is set up.",
              "type": "string",
              "const": "unconfigured"
            },
            {
              "description": "Worth knowing, nothing to do.",
              "type": "string",
              "const": "advisory"
            },
            {
              "description": "Working, with something wrong.",
              "type": "string",
              "const": "degraded"
            },
            {
              "description": "Something is broken.",
              "type": "string",
              "const": "broken"
            },
            {
              "description": "Something is wrong outside this machine, or data is at risk.",
              "type": "string",
              "const": "critical"
            },
            {
              "description": "It could not be established. Never reported as healthy.",
              "type": "string",
              "const": "unknown"
            }
          ]
        },
        "Standing2": {
          "description": "Where the household's one front door stands.",
          "oneOf": [
            {
              "description": "The door is the request surface, and it is running.",
              "type": "string",
              "const": "established"
            },
            {
              "description": "This stack has no request surface, so the library is the door — there is\nnothing here to ask for.",
              "type": "string",
              "const": "library-only"
            },
            {
              "description": "There is a door and it is not answering.\n\nThe service's own state. What fixes it is starting the service.",
              "type": "string",
              "const": "unreachable"
            },
            {
              "description": "There is a door, it is answering, and nothing here can say where another\ndevice would reach it.\n\nThe other way a door is unreachable, and a different thing to do about it:\nthe service is fine and the way to it is what is missing. Told apart from\n[`Standing::Unreachable`] because the two are fixed at opposite ends — one\nby starting a service, the other by giving this machine an address the\nhousehold's devices can use — and a household member who cannot arrive\nneeds to know which of the two they are looking at before they start\nblaming their own device.",
              "type": "string",
              "const": "stranded"
            },
            {
              "description": "Nothing at all is published to the household: an operator-only configuration.",
              "type": "string",
              "const": "none"
            }
          ]
        },
        "Standing3": {
          "description": "Where one member stands against what a period allows them.\n\nFour, and the one worth having is the middle: somebody told only when they have run\nout has been told too late to do anything but wait, which is the answer this whole\nfeature exists to avoid handing anybody.",
          "oneOf": [
            {
              "description": "Nothing is counted against them.",
              "type": "string",
              "const": "unlimited"
            },
            {
              "description": "There is room, and comfortably.",
              "type": "string",
              "const": "within-quota"
            },
            {
              "description": "A fifth of the period's limit or less is left, and at least one of it.",
              "type": "string",
              "const": "near-quota"
            },
            {
              "description": "Nothing is left, so the next thing they ask for is refused as they ask.",
              "type": "string",
              "const": "quota-exhausted"
            }
          ]
        },
        "State": {
          "description": "What one service is actually doing.\n\nOrdered from worst to best, so a form's condition is the minimum across its\nservices and needs no comparison table. The declaration order is therefore\nload-bearing.",
          "oneOf": [
            {
              "description": "Exited without being asked to, and is not coming back on its own.",
              "type": "string",
              "const": "failed"
            },
            {
              "description": "Exiting and restarting repeatedly.",
              "type": "string",
              "const": "crash-looping"
            },
            {
              "description": "Running, and its own probe says it is not working.",
              "type": "string",
              "const": "unhealthy"
            },
            {
              "description": "No container exists.",
              "type": "string",
              "const": "absent"
            },
            {
              "description": "A container exists and is not running.",
              "type": "string",
              "const": "stopped"
            },
            {
              "description": "Running, and still inside its probe's start period.",
              "type": "string",
              "const": "starting"
            },
            {
              "description": "Running, and declaring no probe by which to know more.\n\nDistinct from healthy on purpose. A service that cannot be asked is not\na service that answered, and rendering the two identically is how a\ndashboard comes to be believed about something it never checked.",
              "type": "string",
              "const": "running"
            },
            {
              "description": "Running and passing its own probe.",
              "type": "string",
              "const": "healthy"
            },
            {
              "description": "The operating system owns this one, not lemonfiber.",
              "type": "string",
              "const": "host-managed"
            }
          ]
        },
        "State2": {
          "description": "Where one request stands, in the words the person who made it would use.\n\nDeliberately coarser than a [`crate::trace::Stage`]: a member does not need to know\nthat a release was grabbed but not imported, only that it is on its way. The trace is\nwhere that detail stays, and a request names the item so it can be asked for.",
          "oneOf": [
            {
              "description": "Asked for, and nobody has approved or refused it yet.",
              "type": "string",
              "const": "waiting-for-approval"
            },
            {
              "description": "Turned down — it will not be fetched.",
              "type": "string",
              "const": "declined"
            },
            {
              "description": "Approved, but the attempt to fetch it failed.",
              "type": "string",
              "const": "failed"
            },
            {
              "description": "Approved and on its way — being searched for, downloaded or imported.",
              "type": "string",
              "const": "getting"
            },
            {
              "description": "Some of it is here: a series with only some of its episodes.",
              "type": "string",
              "const": "partly-here"
            },
            {
              "description": "Here, and playable.",
              "type": "string",
              "const": "here"
            },
            {
              "description": "It was here and has since been removed.",
              "type": "string",
              "const": "gone"
            }
          ]
        },
        "Storage": {
          "description": "The storage picture: what is free, when it runs out, and whether imports link.",
          "type": "object",
          "properties": {
            "exhaustion": {
              "description": "The time until the disk fills at the current rate of the queue draining\nonto it, or `None` where it is not projected to fill.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Duration"
                },
                {
                  "type": "null"
                }
              ]
            },
            "free": {
              "description": "Bytes free on the data volume — a [`Reading`], since a volume that could\nnot be read this refresh must not render as zero free.",
              "$ref": "#/$defs/Reading"
            },
            "hardlink": {
              "description": "Whether imports are linking or copying.",
              "$ref": "#/$defs/Hardlink"
            }
          },
          "required": [
            "free",
            "hardlink"
          ]
        },
        "Stuck": {
          "description": "One thing that is wrong, and why.",
          "type": "object",
          "properties": {
            "blocking": {
              "description": "What the service said was blocking it, in its own words, where it said\nanything. A permission denial from an import log is worth more than any\ninterpretation of it, and it is the difference between \"stuck\" and\nsomething an operator can fix.",
              "type": [
                "string",
                "null"
              ]
            },
            "held_for": {
              "description": "How long it has been that way, in seconds — what turns \"stuck\" into a\nsentence an operator can weigh.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "items": {
              "description": "How many items this stands for. One in the ordinary case; more where they\nshare a cause and the cause is what is wrong — twenty downloads stopped by\na full disk are one thing to fix, and twenty alerts about it are how an\noperator learns to mute the queue check.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "name": {
              "description": "Which item — or, where several share one cause, that cause.",
              "type": "string"
            },
            "stall": {
              "description": "What is wrong with it.",
              "$ref": "#/$defs/Stall"
            }
          },
          "required": [
            "name",
            "stall",
            "held_for",
            "items"
          ]
        },
        "Summary": {
          "description": "The one-line summary, and what it expands to.",
          "type": "object",
          "properties": {
            "affected": {
              "description": "Everything that is wrong, worst first, so the line expands to the affected\nitems and their remedies rather than to a number nobody can act on.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Affected"
              }
            },
            "standing": {
              "description": "The one word.",
              "$ref": "#/$defs/Standing"
            },
            "wanting_attention": {
              "description": "How many things are wrong — root causes, counted once each, so a disk that\nfilled and the nine imports that then failed is one thing and not ten.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "worst": {
              "description": "The worst thing, named, so the line says something rather than only\ngrading. Absent where nothing is wrong.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "standing",
            "wanting_attention",
            "affected"
          ]
        },
        "Telemetry": {
          "description": "How the screen itself is doing, which is a different question from how the\nstack is doing.\n\nThe stack's own verdict is [`crate::health::Standing`]; this is only whether the\npicture can be trusted to be current. Kept apart because they disagree in both\ndirections: a healthy stack can be shown through half-failing telemetry, and a\nperfectly refreshing screen can be reporting a stack that is on fire.",
          "oneOf": [
            {
              "description": "Telemetry is current and refreshing normally.",
              "type": "string",
              "const": "live"
            },
            {
              "description": "Some sources are unavailable; their panels are marked.",
              "type": "string",
              "const": "degraded"
            },
            {
              "description": "The engine cannot be reached, though the CLI path may still work.",
              "type": "string",
              "const": "disconnected"
            },
            {
              "description": "Configured, but nothing is running.",
              "type": "string",
              "const": "no-stack"
            },
            {
              "description": "No configuration; setup is offered instead.",
              "type": "string",
              "const": "unconfigured"
            }
          ]
        },
        "Transfer": {
          "description": "One active download, as the dashboard shows it.",
          "type": "object",
          "properties": {
            "eta": {
              "description": "The time left, or `None` where it is stalled and there is none to give.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Duration"
                },
                {
                  "type": "null"
                }
              ]
            },
            "name": {
              "description": "What is being downloaded.",
              "type": "string"
            },
            "progress": {
              "description": "How far along, as a percentage from zero to a hundred.",
              "type": "integer",
              "format": "uint8",
              "maximum": 255,
              "minimum": 0
            },
            "protocol": {
              "description": "How it is being downloaded.",
              "$ref": "#/$defs/Protocol"
            },
            "speed": {
              "description": "The current speed in bytes per second — a [`Reading`], because a genuine\nzero (stalled) and a source that has gone quiet mean opposite things here,\nand this is the very figure that difference is about.",
              "$ref": "#/$defs/Reading"
            }
          },
          "required": [
            "name",
            "protocol",
            "progress",
            "speed"
          ]
        },
        "Unrated": {
          "description": "What is to happen to content the media server has no rating for.\n\nA choice rather than a default, because a great deal of content carries no rating\nand either answer is wrong for somebody: holding it back makes legitimate content\ninvisible, and letting it through lets through the one thing nobody vetted.\n\nSpelled `HeldBack` and `LetThrough` rather than blocked and allowed, because\n[`Allowed`] is the shape this sits on and a field called `unrated: Allowed` would\nread as the opposite of what it is.\n\nA word rather than a flag on both the read and the write, so the setting is named\nthe same in the answer that reports it as in the call that made it — and so neither\n[`Access`] nor the report built from it becomes a row of four unlabelled booleans.\n\nLetting it through is the default because it is the media server's: a new account\nis made holding nothing back, so that is the state an account is found in rather\nthan a decision anybody took.",
          "oneOf": [
            {
              "description": "Content with no rating is held back.",
              "type": "string",
              "const": "held-back"
            },
            {
              "description": "Content with no rating is let through.",
              "type": "string",
              "const": "let-through"
            }
          ]
        },
        "Vpn": {
          "description": "What the VPN is doing, and whether the download client is actually behind it.",
          "type": "object",
          "properties": {
            "country": {
              "description": "The country that address is in.",
              "type": "string"
            },
            "egress_matches": {
              "description": "Whether the download client's own egress address matches the tunnel's —\nthe one thing that proves traffic is genuinely leaving through it.",
              "type": "boolean"
            },
            "exit_ip": {
              "description": "The tunnel's exit address as the outside world sees it.",
              "type": "string"
            },
            "forwarded_port": {
              "description": "The port the provider forwards, where forwarding is on.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            }
          },
          "required": [
            "exit_ip",
            "country",
            "egress_matches"
          ]
        }
      }
    },
    "doctor": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/DoctorReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Category": {
          "description": "The family a check belongs to, so a run can be narrowed to one of them.\n\nThese are the diagnostic categories the product recognises; the checks that\nfill each one arrive over time, so a category may name more than lemonfiber\ncan yet establish.",
          "oneOf": [
            {
              "description": "Docker present, the daemon reachable, the platform understood.",
              "type": "string",
              "const": "environment"
            },
            {
              "description": "The data root: reachable, writable, one filesystem, room to grow.",
              "type": "string",
              "const": "storage"
            },
            {
              "description": "Ports free, bindings matching policy, services reachable.",
              "type": "string",
              "const": "network"
            },
            {
              "description": "Torrent traffic genuinely leaves through the tunnel.",
              "type": "string",
              "const": "vpn"
            },
            {
              "description": "Each credential still valid.",
              "type": "string",
              "const": "credentials"
            },
            {
              "description": "Health, crash loops, version skew, the wiring between services.",
              "type": "string",
              "const": "services"
            },
            {
              "description": "Provider quota, subscription validity, indexer responsiveness.",
              "type": "string",
              "const": "providers"
            },
            {
              "description": "Stuck items, repeated import failures, orphaned downloads.",
              "type": "string",
              "const": "queue"
            },
            {
              "description": "Drift from lemonfiber-managed state, permissions, manifest validity.",
              "type": "string",
              "const": "config"
            }
          ]
        },
        "Code": {
          "description": "A stable identifier for a kind of problem.\n\nStability is the whole point: an operator who searches for a code should find\nthe same answer a year later. Codes are declared as constants beside the code\nthat raises them, and are never recycled.",
          "type": "string"
        },
        "DoctorReport": {
          "description": "What a diagnostic run found, and what it amounts to.",
          "type": "object",
          "properties": {
            "findings": {
              "description": "Each finding, in the order the checks produced them.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Finding"
              }
            },
            "overall": {
              "description": "What the findings amount to, as one word.",
              "$ref": "#/$defs/Overall"
            }
          },
          "required": [
            "overall",
            "findings"
          ]
        },
        "Finding": {
          "description": "One thing a check established, and how it turned out.",
          "type": "object",
          "properties": {
            "category": {
              "description": "The family this belongs to.",
              "$ref": "#/$defs/Category"
            },
            "caused_by": {
              "description": "The check whose finding explains this one, where another does.\n\nSet after the run rather than by the check itself: a check is independent\nby construction and cannot see what any other found, which is a property\nworth keeping.",
              "type": [
                "string",
                "null"
              ]
            },
            "check": {
              "description": "A stable identifier for the thing checked, such as `vpn.egress-match`.",
              "type": "string"
            },
            "said": {
              "description": "What the service said for itself, lately.\n\nCarried on the finding rather than left for the operator to go and fetch,\nbecause the explanation is almost always in it: a check can say a service is\nnot answering, and only the service can say why. Absent where the finding is\nnot about a service, where the service is fine, or where the engine would not\nsay — an empty section would be a promise of evidence that is not there.\n\nSet after the run, like [`Self::caused_by`], since reading a service's output\nis not the check's own business and a check that did it would be doing two\nthings.",
              "type": [
                "string",
                "null"
              ]
            },
            "service": {
              "description": "The service this is about, where it is about one.\n\nAbsent for the checks that are about the machine rather than about\nsomething running on it — the environment, the filesystem, the operator's\nown choices. Carried so that one service's trouble can be attributed to\nthe service underneath it rather than counted as one more independent\nthing wrong.",
              "type": [
                "string",
                "null"
              ]
            },
            "title": {
              "description": "The one-line summary of what was checked.",
              "type": "string"
            },
            "verdict": {
              "description": "How it turned out.",
              "$ref": "#/$defs/Verdict"
            }
          },
          "required": [
            "check",
            "category",
            "title",
            "verdict"
          ]
        },
        "Overall": {
          "description": "What a run's findings amount to.",
          "oneOf": [
            {
              "description": "Everything that ran passed.",
              "type": "string",
              "const": "healthy"
            },
            {
              "description": "Warnings, but nothing broken.",
              "type": "string",
              "const": "degraded"
            },
            {
              "description": "Something is broken.",
              "type": "string",
              "const": "broken"
            },
            {
              "description": "Something could not be determined, so health is not a settled fact.",
              "type": "string",
              "const": "unknown"
            }
          ]
        },
        "Problem": {
          "description": "Something that went wrong, in the form an operator can act on.",
          "type": "object",
          "properties": {
            "cause": {
              "description": "The problem that produced this one, where several share a root.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Problem"
                },
                {
                  "type": "null"
                }
              ]
            },
            "code": {
              "description": "The stable identifier for this kind of problem.",
              "$ref": "#/$defs/Code"
            },
            "detail": {
              "description": "The underlying technical detail, available but never leading.",
              "type": [
                "string",
                "null"
              ]
            },
            "meaning": {
              "description": "What it means for the operator.",
              "type": "string"
            },
            "remedies": {
              "description": "What to do, most likely first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Remedy"
              }
            },
            "severity": {
              "description": "How much it matters.",
              "$ref": "#/$defs/Severity"
            },
            "state": {
              "description": "Where it stands with respect to being fixed.",
              "$ref": "#/$defs/State"
            },
            "summary": {
              "description": "What happened, in one plain sentence.",
              "type": "string"
            }
          },
          "required": [
            "code",
            "severity",
            "state",
            "summary",
            "meaning",
            "remedies"
          ]
        },
        "Remedy": {
          "description": "One thing the operator can do about a problem.",
          "type": "object",
          "properties": {
            "action": {
              "description": "The action, phrased as something to do rather than something to know.",
              "type": "string"
            },
            "detail": {
              "description": "Where to look, when that helps.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "action"
          ]
        },
        "Severity": {
          "description": "How much a problem matters.\n\nFour levels, deliberately. More would not be applied consistently, and\ninconsistent severity is worse than coarse severity.",
          "oneOf": [
            {
              "description": "Informational; nothing is required.",
              "type": "string",
              "const": "advisory"
            },
            {
              "description": "Degraded or risky, still working.",
              "type": "string",
              "const": "warning"
            },
            {
              "description": "Something is broken.",
              "type": "string",
              "const": "error"
            },
            {
              "description": "Consequences outside the machine, or data at risk.",
              "type": "string",
              "const": "critical"
            }
          ]
        },
        "State": {
          "description": "Where a problem stands with respect to being fixed.",
          "oneOf": [
            {
              "description": "A remedy is available here.",
              "type": "string",
              "const": "actionable"
            },
            {
              "description": "The operator must act, somewhere else.",
              "type": "string",
              "const": "guided"
            },
            {
              "description": "lemonfiber can fix this itself.",
              "type": "string",
              "const": "remediable"
            },
            {
              "description": "No known remedy; escalation is offered instead.",
              "type": "string",
              "const": "unknown"
            },
            {
              "description": "Acknowledged, and not re-shown until it recurs.",
              "type": "string",
              "const": "suppressed"
            }
          ]
        },
        "Verdict": {
          "description": "How a single check turned out.\n\n\"Could not check\" (`Unverified`) is its own variant rather than a level of\nseverity, so a check that could not run can never be mistaken for one that\npassed — the dishonesty this whole subsystem exists to prevent.",
          "oneOf": [
            {
              "description": "Verified working, with the evidence worth showing.",
              "type": "object",
              "properties": {
                "note": {
                  "description": "What was observed, where stating it helps — an address, a port.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "outcome": {
                  "type": "string",
                  "const": "pass"
                }
              },
              "required": [
                "outcome"
              ]
            },
            {
              "description": "Working, but degraded or risky.",
              "type": "object",
              "properties": {
                "cause": {
                  "description": "The problem that produced this one, where several share a root.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/Problem"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "code": {
                  "description": "The stable identifier for this kind of problem.",
                  "$ref": "#/$defs/Code"
                },
                "detail": {
                  "description": "The underlying technical detail, available but never leading.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "meaning": {
                  "description": "What it means for the operator.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "warn"
                },
                "remedies": {
                  "description": "What to do, most likely first.",
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Remedy"
                  }
                },
                "severity": {
                  "description": "How much it matters.",
                  "$ref": "#/$defs/Severity"
                },
                "state": {
                  "description": "Where it stands with respect to being fixed.",
                  "$ref": "#/$defs/State"
                },
                "summary": {
                  "description": "What happened, in one plain sentence.",
                  "type": "string"
                }
              },
              "required": [
                "outcome",
                "code",
                "severity",
                "state",
                "summary",
                "meaning",
                "remedies"
              ]
            },
            {
              "description": "Not working.",
              "type": "object",
              "properties": {
                "cause": {
                  "description": "The problem that produced this one, where several share a root.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/Problem"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "code": {
                  "description": "The stable identifier for this kind of problem.",
                  "$ref": "#/$defs/Code"
                },
                "detail": {
                  "description": "The underlying technical detail, available but never leading.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "meaning": {
                  "description": "What it means for the operator.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "fail"
                },
                "remedies": {
                  "description": "What to do, most likely first.",
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Remedy"
                  }
                },
                "severity": {
                  "description": "How much it matters.",
                  "$ref": "#/$defs/Severity"
                },
                "state": {
                  "description": "Where it stands with respect to being fixed.",
                  "$ref": "#/$defs/State"
                },
                "summary": {
                  "description": "What happened, in one plain sentence.",
                  "type": "string"
                }
              },
              "required": [
                "outcome",
                "code",
                "severity",
                "state",
                "summary",
                "meaning",
                "remedies"
              ]
            },
            {
              "description": "Could not be established. Never a pass.",
              "type": "object",
              "properties": {
                "outcome": {
                  "type": "string",
                  "const": "unverified"
                },
                "reason": {
                  "description": "Why it could not be determined.",
                  "type": "string"
                },
                "remedy": {
                  "description": "What the operator can do to get an answer.",
                  "$ref": "#/$defs/Remedy"
                }
              },
              "required": [
                "outcome",
                "reason",
                "remedy"
              ]
            },
            {
              "description": "A prerequisite was absent, so the check did not apply.",
              "type": "object",
              "properties": {
                "outcome": {
                  "type": "string",
                  "const": "skipped"
                },
                "reason": {
                  "description": "Why the check did not apply.",
                  "type": "string"
                }
              },
              "required": [
                "outcome",
                "reason"
              ]
            }
          ]
        }
      }
    },
    "error": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Problem"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Code": {
          "description": "A stable identifier for a kind of problem.\n\nStability is the whole point: an operator who searches for a code should find\nthe same answer a year later. Codes are declared as constants beside the code\nthat raises them, and are never recycled.",
          "type": "string"
        },
        "Problem": {
          "description": "Something that went wrong, in the form an operator can act on.",
          "type": "object",
          "properties": {
            "cause": {
              "description": "The problem that produced this one, where several share a root.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Problem"
                },
                {
                  "type": "null"
                }
              ]
            },
            "code": {
              "description": "The stable identifier for this kind of problem.",
              "$ref": "#/$defs/Code"
            },
            "detail": {
              "description": "The underlying technical detail, available but never leading.",
              "type": [
                "string",
                "null"
              ]
            },
            "meaning": {
              "description": "What it means for the operator.",
              "type": "string"
            },
            "remedies": {
              "description": "What to do, most likely first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Remedy"
              }
            },
            "severity": {
              "description": "How much it matters.",
              "$ref": "#/$defs/Severity"
            },
            "state": {
              "description": "Where it stands with respect to being fixed.",
              "$ref": "#/$defs/State"
            },
            "summary": {
              "description": "What happened, in one plain sentence.",
              "type": "string"
            }
          },
          "required": [
            "code",
            "severity",
            "state",
            "summary",
            "meaning",
            "remedies"
          ]
        },
        "Remedy": {
          "description": "One thing the operator can do about a problem.",
          "type": "object",
          "properties": {
            "action": {
              "description": "The action, phrased as something to do rather than something to know.",
              "type": "string"
            },
            "detail": {
              "description": "Where to look, when that helps.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "action"
          ]
        },
        "Severity": {
          "description": "How much a problem matters.\n\nFour levels, deliberately. More would not be applied consistently, and\ninconsistent severity is worse than coarse severity.",
          "oneOf": [
            {
              "description": "Informational; nothing is required.",
              "type": "string",
              "const": "advisory"
            },
            {
              "description": "Degraded or risky, still working.",
              "type": "string",
              "const": "warning"
            },
            {
              "description": "Something is broken.",
              "type": "string",
              "const": "error"
            },
            {
              "description": "Consequences outside the machine, or data at risk.",
              "type": "string",
              "const": "critical"
            }
          ]
        },
        "State": {
          "description": "Where a problem stands with respect to being fixed.",
          "oneOf": [
            {
              "description": "A remedy is available here.",
              "type": "string",
              "const": "actionable"
            },
            {
              "description": "The operator must act, somewhere else.",
              "type": "string",
              "const": "guided"
            },
            {
              "description": "lemonfiber can fix this itself.",
              "type": "string",
              "const": "remediable"
            },
            {
              "description": "No known remedy; escalation is offered instead.",
              "type": "string",
              "const": "unknown"
            },
            {
              "description": "Acknowledged, and not re-shown until it recurs.",
              "type": "string",
              "const": "suppressed"
            }
          ]
        }
      }
    },
    "forms": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/FormsReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "FormReport": {
          "description": "One form the stack declares, as a listing shows it.\n\nThe manifest's own words rather than lemonfiber's: forms come from the stack, so a\nstack of somebody's own names and describes them however it likes, and a listing that\nparaphrased would be describing a different stack from the one being run.",
          "type": "object",
          "properties": {
            "composable": {
              "description": "Whether it can be started alongside another form.\n\nWorth saying in the listing rather than only when a combination is refused: an\noperator choosing between two forms is exactly who needs to know they are a choice.",
              "type": "boolean"
            },
            "description": {
              "description": "What it is for, in one line.",
              "type": "string"
            },
            "id": {
              "description": "What to type to start it.",
              "type": "string"
            },
            "name": {
              "description": "What it is called.",
              "type": "string"
            }
          },
          "required": [
            "id",
            "name",
            "description",
            "composable"
          ]
        },
        "FormsReport": {
          "description": "Every form this stack declares.",
          "type": "object",
          "properties": {
            "forms": {
              "description": "The forms, in the order the stack declares them.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/FormReport"
              }
            }
          },
          "required": [
            "forms"
          ]
        }
      }
    },
    "front-door": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/FrontDoorReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Address": {
          "description": "Where the front door is reached, and what is worth knowing about the address.",
          "type": "object",
          "properties": {
            "caution": {
              "description": "What is worth knowing about the address itself, where anything is. Absent\nfor one that keeps working on its own.",
              "type": [
                "string",
                "null"
              ]
            },
            "url": {
              "description": "The whole address, as it would be typed or followed.",
              "type": "string"
            }
          },
          "required": [
            "url"
          ]
        },
        "Beside": {
          "description": "A service the household can reach that is not the front door, and why it is not.\n\nCarried rather than left out, because the decision is the useful part: an operator\nwho can see that the index over every service was considered and refused has been\ntold something, where one shown a single name has only been given an answer.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why it is not somewhere to begin.",
              "type": "string"
            },
            "facing": {
              "description": "What it is to the household.",
              "$ref": "#/$defs/Facing"
            },
            "service": {
              "description": "The service, by the name it shows itself under.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "facing",
            "because"
          ]
        },
        "Chosen": {
          "description": "How the front door came to be the one it is.",
          "oneOf": [
            {
              "description": "Worked out from what the stack declares, which is what a stack whose\noperator has named nothing answers.",
              "type": "object",
              "properties": {
                "chosen": {
                  "type": "string",
                  "const": "derived"
                }
              },
              "required": [
                "chosen"
              ]
            },
            {
              "description": "Named by the operator, by the id the stack declares it under, and it is the\ndoor.",
              "type": "object",
              "properties": {
                "chosen": {
                  "type": "string",
                  "const": "named"
                },
                "door": {
                  "type": "string"
                }
              },
              "required": [
                "chosen",
                "door"
              ]
            },
            {
              "description": "Named by the operator and refused. The worked-out door stands, and this\ncarries what was named and why it is not it.",
              "type": "object",
              "properties": {
                "chosen": {
                  "type": "string",
                  "const": "refused"
                },
                "door": {
                  "$ref": "#/$defs/Refusal"
                }
              },
              "required": [
                "chosen",
                "door"
              ]
            }
          ]
        },
        "Facing": {
          "description": "What a service published to the local network is to the people in the house.",
          "oneOf": [
            {
              "description": "Where asking for something begins, and where what was asked for is followed.\nIt links onward to the library, which is what makes it a place to start\nrather than one more address to be sent.",
              "type": "string",
              "const": "asking"
            },
            {
              "description": "The library, where what arrived is watched. A place to start only where there\nis nothing to ask for, because somebody sent there has no way to ask.",
              "type": "string",
              "const": "watching"
            },
            {
              "description": "One kind of media and no other, reached from the library rather than instead\nof it.",
              "type": "string",
              "const": "shelf"
            },
            {
              "description": "An index over every service, including the ones nobody in the house should\nlearn exist.",
              "type": "string",
              "const": "operators"
            },
            {
              "description": "How the others are reached, rather than one of them.",
              "type": "string",
              "const": "carriage"
            },
            {
              "description": "Published to the household, and nothing here says what it is to them.",
              "type": "string",
              "const": "unstated"
            }
          ]
        },
        "FrontDoorReport": {
          "description": "The household's one front door: which service it is, where it stands, and what\nelse they can reach that is not it.",
          "type": "object",
          "properties": {
            "address": {
              "description": "The address to hand them, read from this machine at the moment of asking\nrather than remembered. Absent where there is no door, and where there is\none on a machine that will say neither what it is called nor where it is.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Address"
                },
                {
                  "type": "null"
                }
              ]
            },
            "beside": {
              "description": "Everything else the household can reach, and why none of it is the door.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Beside"
              }
            },
            "chosen": {
              "description": "How this came to be the door: worked out from what the stack declares, named\nby the operator, or named by them and refused.\n\nCarried as a state rather than left to the sentence beneath it, for the reason\nthe standing is: an operator whose setting was refused reads the sentence, and\na browser, a script or a dashboard reads this.",
              "$ref": "#/$defs/Chosen"
            },
            "facing": {
              "description": "What that service is to them. Absent for the same reason.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Facing"
                },
                {
                  "type": "null"
                }
              ]
            },
            "meaning": {
              "description": "What this comes to, in the words an operator would say it in — including,\nwhere there is no door, that there is none.",
              "type": "string"
            },
            "service": {
              "description": "The service the household begins at, by the name it shows itself under.\nAbsent where this stack publishes nothing they could begin at.",
              "type": [
                "string",
                "null"
              ]
            },
            "standing": {
              "description": "Where the front door stands.",
              "$ref": "#/$defs/Standing"
            }
          },
          "required": [
            "standing",
            "chosen",
            "meaning",
            "beside"
          ]
        },
        "Refusal": {
          "description": "A named front door that is not one, and why it is not.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why this stack will not send a household there.",
              "type": "string"
            },
            "named": {
              "description": "What the operator recorded, as they wrote it.",
              "type": "string"
            }
          },
          "required": [
            "named",
            "because"
          ]
        },
        "Standing": {
          "description": "Where the household's one front door stands.",
          "oneOf": [
            {
              "description": "The door is the request surface, and it is running.",
              "type": "string",
              "const": "established"
            },
            {
              "description": "This stack has no request surface, so the library is the door — there is\nnothing here to ask for.",
              "type": "string",
              "const": "library-only"
            },
            {
              "description": "There is a door and it is not answering.\n\nThe service's own state. What fixes it is starting the service.",
              "type": "string",
              "const": "unreachable"
            },
            {
              "description": "There is a door, it is answering, and nothing here can say where another\ndevice would reach it.\n\nThe other way a door is unreachable, and a different thing to do about it:\nthe service is fine and the way to it is what is missing. Told apart from\n[`Standing::Unreachable`] because the two are fixed at opposite ends — one\nby starting a service, the other by giving this machine an address the\nhousehold's devices can use — and a household member who cannot arrive\nneeds to know which of the two they are looking at before they start\nblaming their own device.",
              "type": "string",
              "const": "stranded"
            },
            {
              "description": "Nothing at all is published to the household: an operator-only configuration.",
              "type": "string",
              "const": "none"
            }
          ]
        }
      }
    },
    "glossary": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Vocabulary"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Term": {
          "description": "A word this product uses, and what somebody meeting it needs to know.",
          "type": "object",
          "properties": {
            "also_called": {
              "description": "What other services in this stack call the same thing.\n\nSonarr and `SABnzbd` do not agree on words, and an operator moving between\ntheir screens should not have to work out that two of them are one.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "deep": {
              "description": "More, for somebody who asks — never needed in order to act.",
              "type": [
                "string",
                "null"
              ]
            },
            "short": {
              "description": "One sentence: what it is for and what it costs or gains.\n\nEnough to act on. Somebody who reads only this should not be stuck.",
              "type": "string"
            },
            "word": {
              "description": "The word as it appears in the interface.",
              "type": "string"
            }
          },
          "required": [
            "word",
            "short",
            "also_called"
          ]
        },
        "Vocabulary": {
          "description": "Every word this product explains, for somebody who asked what there is to ask\nabout.",
          "type": "object",
          "properties": {
            "words": {
              "description": "The words, in the order somebody meets them.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Term"
              }
            }
          },
          "required": [
            "words"
          ]
        }
      }
    },
    "history": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/HistoryReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "ChangeReport": {
          "description": "One change lemonfiber made, and whether it could be put back.",
          "type": "object",
          "properties": {
            "alongside": {
              "description": "How many changes that one operation made, this one among them.\n\nAn operation is the unit an operator agreed to, and undoing half of one leaves a\nmachine in a state nobody chose — so what a single line would take with it is on\nthe line rather than left to be counted off the list.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "at": {
              "description": "When it was made.",
              "type": "string"
            },
            "because": {
              "description": "Why it could not go further, where it could not.",
              "type": [
                "string",
                "null"
              ]
            },
            "did": {
              "description": "What it did, in the operator's terms.",
              "type": "string"
            },
            "instead": {
              "description": "What to do instead, where there is something.",
              "type": [
                "string",
                "null"
              ]
            },
            "operation": {
              "description": "The operation that made it — a seed, a reconfigure, an applied fix — so a\nhistory reads as what happened rather than as bare diffs.",
              "type": "string"
            },
            "reversal": {
              "description": "How far it could be put back: `whole`, `partial`, or `none`.",
              "type": "string"
            },
            "target": {
              "description": "What it was made to.",
              "type": "string"
            }
          },
          "required": [
            "at",
            "operation",
            "target",
            "did",
            "reversal",
            "alongside"
          ]
        },
        "HistoryReport": {
          "description": "Everything lemonfiber changed, most recent first.",
          "type": "object",
          "properties": {
            "changes": {
              "description": "The changes, newest first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/ChangeReport"
              }
            },
            "horizon": {
              "description": "How far back the record goes, in the operator's terms.\n\nStated rather than left to be inferred from the oldest entry: a record that has\nbeen trimmed and one that has always been short look identical from the entries\nalone, and only one of them means something is missing.",
              "type": "string"
            }
          },
          "required": [
            "changes",
            "horizon"
          ]
        }
      }
    },
    "hosting": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/HostingReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Changed": {
          "description": "What one run of this command did to the machine.",
          "type": "object",
          "properties": {
            "installed": {
              "description": "Whether it installed it, rather than took it back.",
              "type": "boolean"
            },
            "name": {
              "description": "The command it acted on.",
              "type": "string"
            },
            "rehearsed": {
              "description": "Whether this was a rehearsal, in which case nothing above happened.",
              "type": "boolean"
            },
            "started": {
              "description": "Whether it started the command, which only installing does.",
              "type": "boolean"
            },
            "touched": {
              "description": "Everything it wrote or removed, so nothing goes unnamed in either direction.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "name",
            "installed",
            "touched",
            "started",
            "rehearsed"
          ]
        },
        "HostedCommand": {
          "description": "One long-running command, and what stands between it and this machine.",
          "type": "object",
          "properties": {
            "command": {
              "description": "How it is typed in a terminal, which is what hosting installs.",
              "type": "string"
            },
            "definition": {
              "description": "The service definition installed for it, where there is one.",
              "type": [
                "string",
                "null"
              ]
            },
            "guarantees": {
              "description": "What it does for as long as it runs, in one sentence.",
              "type": "string"
            },
            "missing": {
              "description": "The program the definition names, where nothing is there any more.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "lemonfiber's own name for it.",
              "type": "string"
            },
            "output": {
              "description": "Where a hosted run writes the words it would have said on a terminal.",
              "type": [
                "string",
                "null"
              ]
            },
            "runs": {
              "description": "The whole command line that definition runs.",
              "type": [
                "string",
                "null"
              ]
            },
            "standing": {
              "description": "What stands between it and the machine.",
              "$ref": "#/$defs/Hosting"
            }
          },
          "required": [
            "name",
            "guarantees",
            "command",
            "standing"
          ]
        },
        "Hosting": {
          "description": "What stands between one long-running command and the machine.\n\nWritten with hyphens because these are the words the operator reads and the\nrequirement names, and a reading that spelled them differently would be a\nsecond vocabulary for one set of facts.",
          "oneOf": [
            {
              "description": "Nothing is installed; it runs only while a terminal holds it.",
              "type": "string",
              "const": "not-hosted"
            },
            {
              "description": "Installed, and the manager confirms it is running.",
              "type": "string",
              "const": "hosted"
            },
            {
              "description": "Installed, and the manager would not say whether it is running.",
              "type": "string",
              "const": "installed-unverified"
            },
            {
              "description": "Installed, and the manager says it is not running.",
              "type": "string",
              "const": "stopped"
            },
            {
              "description": "Installed against a program that is no longer there.",
              "type": "string",
              "const": "orphaned"
            },
            {
              "description": "This platform has no service manager lemonfiber configures.",
              "type": "string",
              "const": "unsupported"
            }
          ]
        },
        "HostingReport": {
          "description": "What this machine keeps running on lemonfiber's behalf.\n\nDefaultable so a test can read one out of a `Result` without a branch it can\nnever take: a closure standing in for the impossible arm is a region no\npassing run enters, and the coverage gate counts those.",
          "type": "object",
          "properties": {
            "caveat": {
              "description": "What is true of this manager and worth knowing before it is relied on.",
              "type": [
                "string",
                "null"
              ]
            },
            "changed": {
              "description": "What this run changed, where it was asked to change something.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Changed"
                },
                {
                  "type": "null"
                }
              ]
            },
            "commands": {
              "description": "Every long-running command there is, hosted or not.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/HostedCommand"
              }
            },
            "instruction": {
              "description": "What to do instead, where lemonfiber cannot configure this platform.",
              "type": [
                "string",
                "null"
              ]
            },
            "manager": {
              "description": "The service manager this platform has, or the absence of one.",
              "$ref": "#/$defs/Manager"
            }
          },
          "required": [
            "manager",
            "commands"
          ]
        },
        "Manager": {
          "description": "The service manager a machine has, or the absence of one lemonfiber configures.\n\nThe absence is the default, because a machine nobody has told is a machine\nnothing is known about, and guessing at a manager is how a report comes to\nclaim a platform it never asked.",
          "oneOf": [
            {
              "description": "macOS, through a launch agent in the operator's own login session.",
              "type": "string",
              "const": "launchd"
            },
            {
              "description": "Linux, through a user service in the operator's own session.",
              "type": "string",
              "const": "systemd"
            },
            {
              "description": "A platform lemonfiber does not configure.",
              "type": "string",
              "const": "unsupported"
            }
          ]
        }
      }
    },
    "household": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/HouseholdReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Counted": {
          "description": "One of the two counts the request service keeps, and what it has left.",
          "type": "object",
          "properties": {
            "limit": {
              "description": "How many the period allows. Absent where nothing limits them, which is a\ndifferent answer from a limit of nought.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "period": {
              "description": "How long the period is, in the words a household says it in — absent where\nthe count runs from the beginning rather than over a window.",
              "type": [
                "string",
                "null"
              ]
            },
            "remaining": {
              "description": "How many more they may ask for. Absent where nothing limits them.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "used": {
              "description": "How many the period has already counted against them.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "used"
          ]
        },
        "Estimate": {
          "description": "About how much room one request will want.\n\nCarried as a number and a word rather than as a rendered string, so a surface can\nput it in a column and still say what it is. The word is not decoration: a figure\nwith nothing hedging it is a promise, and this cannot keep one.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "About how many bytes it will take.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "measured": {
              "description": "Whether this was measured. Always false — nothing here has measured anything,\nand the field is present so that a surface rendering it cannot forget to say so.",
              "type": "boolean"
            }
          },
          "required": [
            "bytes",
            "measured"
          ]
        },
        "HouseholdMember": {
          "description": "One household member: who they are, what they may watch, when they were last\nseen, and everything they have asked for.",
          "type": "object",
          "properties": {
            "access": {
              "description": "What they may watch.",
              "$ref": "#/$defs/MemberAccess"
            },
            "asking": {
              "description": "What they may ask for, and what their period has left of it.\n\nAbsent where the request service holds no account for them, and where it could\nnot be asked — an unread answer is not an unlimited member, and reporting one as\nthe other would tell an operator their quota was never applied.",
              "anyOf": [
                {
                  "$ref": "#/$defs/MemberAsking"
                },
                {
                  "type": "null"
                }
              ]
            },
            "claimed": {
              "description": "Whether somebody has set a password on the account. False is an invitation\nnobody has taken up rather than a member who is not here.",
              "type": "boolean"
            },
            "last_seen": {
              "description": "When the media server last saw them, as it timestamps it. Absent where nobody\nhas ever signed in, which is exactly the unclaimed invitations.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "The member, by the name their account is held under.",
              "type": "string"
            },
            "requests": {
              "description": "What they asked for, newest first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/MemberRequest"
              }
            },
            "to_hand_over": {
              "description": "What this member would be told, in the words they would read it in.\n\nEverything a household member is owed at the moment of asking and cannot be\nshown where they ask: what happens to what they ask for, what their period has\nleft and when it makes room, roughly what a thing costs before they choose one,\nwhat is still waiting on an answer, and what was refused and why. Written to\nthem rather than about them, so it can be handed over as it stands.\n\nEmpty where there is nothing to tell them — a member the request service holds\nno account for has no standing to report and nothing waiting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "name",
            "requests",
            "access",
            "claimed",
            "to_hand_over"
          ]
        },
        "HouseholdReport": {
          "description": "Who is in the household, what each may watch, and what each has asked for.",
          "type": "object",
          "properties": {
            "allows": {
              "description": "What that policy allows in a period, in the words a household says it in.\n\nAbsent where nothing limits the household, which is not the same as a policy\nthat could not be read: that one leaves [`Self::policy`] absent too.",
              "type": [
                "string",
                "null"
              ]
            },
            "available": {
              "description": "Whether the household could be read at all. A false here is why the list is\nempty, and keeps an unread record from being mistaken for an empty house — the\nsame honesty a trace keeps about a silence it did not hear.",
              "type": "boolean"
            },
            "filtering": {
              "description": "What the limits on this household are and are not, where anybody carries one.\n\nAbsent on a household nobody has limited, because there is no claim to be modest\nabout. Present the moment there is one, because a parent who has set a limit is\nexactly the reader who might take it for a lock.",
              "type": [
                "string",
                "null"
              ]
            },
            "findings": {
              "description": "What could not be read, and anything else worth the operator's attention.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "members": {
              "description": "Everybody the media server holds an account for, in name order — including\nthose who have never asked for anything, and the invitations nobody has taken\nup yet.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/HouseholdMember"
              }
            },
            "policy": {
              "description": "What happens to what the household asks for where nobody chose otherwise for\none person. Absent where the request service could not be asked.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Policy"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "members",
            "available",
            "findings"
          ]
        },
        "MemberAccess": {
          "description": "What one member may watch, in the household's own words.\n\nRead off the media server's account rather than kept here: that is where access is\ndecided, and a second copy is a copy able to disagree.",
          "type": "object",
          "properties": {
            "administrator": {
              "description": "Whether the account administers the media server.",
              "type": "boolean"
            },
            "age_limit": {
              "description": "The highest rating they may watch, where the operator set a limit.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "disabled": {
              "description": "Whether the account is switched off — held, but unable to sign in.",
              "type": "boolean"
            },
            "every_library": {
              "description": "Every library, rather than a chosen few. The ordinary case.",
              "type": "boolean"
            },
            "libraries": {
              "description": "The libraries they may watch where it is not every one, by the names the\noperator gave them — or by the server's identifiers where the library list\ncould not be read, which a finding says.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "rated": {
              "description": "What that limit comes to in the certificates this media server names, in the\noperator's own country. Absent where no limit is set.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Rated"
                },
                {
                  "type": "null"
                }
              ]
            },
            "restriction": {
              "description": "What they are held to, in one word — including where what they may watch and\nwhat they may ask for disagree.",
              "$ref": "#/$defs/Restriction"
            },
            "unrated": {
              "description": "What becomes of content the media server has no rating for.\n\nSaid either way, because an unexplained absence is the thing this answers: a\nrestricted member missing half the library is either this setting or a defect,\nand an operator cannot tell which from silence.",
              "$ref": "#/$defs/Unrated"
            }
          },
          "required": [
            "every_library",
            "libraries",
            "unrated",
            "restriction",
            "administrator",
            "disabled"
          ]
        },
        "MemberAsking": {
          "description": "What one member may ask for, where the request service could be asked.\n\nBoth counts, because the service keeps them apart and folding them would report a\nhousehold as within its limit while the half that matters is spent: television is\ncounted a season at a time, so one ask for a six-season series spends six.",
          "type": "object",
          "properties": {
            "films": {
              "description": "Films, counted one to a request.",
              "$ref": "#/$defs/Counted"
            },
            "frees_up": {
              "description": "When the count next lets go of something, so one more becomes possible.\n\nAbsent where nothing limits them, and where the request service's own dates\ncould not be read — an invented one would be a promise about a day on which\nnothing happens. The period is a window that rolls rather than a month that\nends, so this is the moment their earliest counted request ages out.",
              "type": [
                "string",
                "null"
              ]
            },
            "policy": {
              "description": "What happens to what they ask for.",
              "$ref": "#/$defs/Policy"
            },
            "standing": {
              "description": "Where they stand against what their period allows, taken over both counts.",
              "$ref": "#/$defs/Standing"
            },
            "television": {
              "description": "Television, counted one to a season.",
              "$ref": "#/$defs/Counted"
            }
          },
          "required": [
            "policy",
            "standing",
            "films",
            "television"
          ]
        },
        "MemberRequest": {
          "description": "One thing a household member asked for, and where it stands in their words.",
          "type": "object",
          "properties": {
            "estimate": {
              "description": "About how much room it will want, at the quality in force.\n\nA guess and labelled as one — see [`crate::asking::Estimate`]. Absent where the\nrequest service names a kind this build does not know, since there is nothing to\nguess the length of.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Estimate"
                },
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "description": "The number the request service files it under, which is how one is named again\nwhen somebody rules on it.",
              "type": "integer",
              "format": "int64"
            },
            "media": {
              "description": "What kind of thing it is — a series, a film — in the household's own words.\nAbsent where the request service names a kind this build does not know.",
              "type": [
                "string",
                "null"
              ]
            },
            "refused": {
              "description": "Why it was turned down, where it was turned down from here.\n\n**The request service keeps none**, so this is lemonfiber's own record and is\nsaid to be — a reason presented as delivered would end the operator's job at\nexactly the point it begins. Absent on a request nobody has refused, and on one\nrefused in the request service itself, where there are no words to report and\ninventing some would put them in somebody's mouth.\n\nWhether the words were carried to the person who asked is the record's own\n`told`, which is why the two travel together: what an operator does next turns\non it, and a reason read without it is a reason of unknown standing.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Refused"
                },
                {
                  "type": "null"
                }
              ]
            },
            "state": {
              "description": "Where the request stands, or absent where the request service reports a status\nthis build does not know rather than guessing it into the nearest word.",
              "anyOf": [
                {
                  "$ref": "#/$defs/State"
                },
                {
                  "type": "null"
                }
              ]
            },
            "title": {
              "description": "What it is called, where the service filing it has been told about it and its\nlibrary could be read. Absent for a request no service holds yet — one still\nawaiting approval has been handed to nobody, so there is no title to find.",
              "type": [
                "string",
                "null"
              ]
            },
            "waiting_days": {
              "description": "How many whole days it has been waiting on somebody, where it is waiting at all\nand the service's own date could be read.\n\nOnly on the ones nobody has ruled on. A request already answered has not been\nwaiting since it was made, and a figure beside one would be counting the wrong\nthing.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "id"
          ]
        },
        "Passed": {
          "description": "Where a refusal's words were carried, and when.",
          "type": "object",
          "properties": {
            "at": {
              "description": "When the attempt was made, absent where the clock could not be written down.",
              "type": [
                "string",
                "null"
              ]
            },
            "to": {
              "description": "The services they reached, by the names the member knows them by. Empty where\nthere was nowhere this could send.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "to"
          ]
        },
        "Policy": {
          "description": "What happens to what a household member asks for.\n\nThree, and they are the three the request service can actually be put into. A\nhousehold is in one of them because of two settings taken together — whether requests\narrive unseen, and whether a period limits how many — so the words here are a reading\nof that pair rather than a fourth setting kept beside it.\n\nChoosing per person is not a fourth policy. It is one of these three chosen for one\nmember rather than for the house, which is why what a surface offers is a policy and,\nseparately, who it is for.",
          "oneOf": [
            {
              "description": "Everything asked for arrives, and nothing is counted against anybody.",
              "type": "string",
              "const": "trusted"
            },
            {
              "description": "Everything asked for arrives until the period's limit is spent, and what\nwould go past it is refused at the moment of asking.",
              "type": "string",
              "const": "within-a-limit"
            },
            {
              "description": "Everything asked for waits for the operator to rule on it.",
              "type": "string",
              "const": "everything-waits"
            }
          ]
        },
        "Rated": {
          "description": "What one age limit comes to, in the certificates named for it.\n\nBoth sides, because either alone misleads. What is allowed without what is held\nback reads as a limit that stops nothing; what is held back without what is allowed\nreads as a limit that stops everything.",
          "type": "object",
          "properties": {
            "allows": {
              "description": "The certificates at the highest age this limit still lets through.\n\nEmpty where the table names nothing at or below the limit, which is a limit\nthat lets nothing rated through at all.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "fell_back": {
              "description": "Whether these came from lemonfiber's own mapping because the media server's\ntable named no certificates.\n\nCarried rather than hidden: a certificate said to be this household's when it\nis this program's is the kind of claim a parent would act on.",
              "type": "boolean"
            },
            "holds_back": {
              "description": "The certificates at the lowest age this limit holds back.\n\nEmpty where the table names nothing above the limit, which is a limit that\nholds nothing rated back at all.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "allows",
            "holds_back",
            "fell_back"
          ]
        },
        "Refused": {
          "description": "What was said when one request was turned down.",
          "type": "object",
          "properties": {
            "at": {
              "description": "When it was turned down, so somebody reading it later knows which answer this\nwas rather than assuming it is the newest.\n\nAbsent where the machine's clock could not be written as a date, which is a\nrefusal worth keeping the words of and not worth losing them over.",
              "type": [
                "string",
                "null"
              ]
            },
            "expired": {
              "description": "Whether nobody ruled on it and the period the household agreed to closed it,\nrather than an operator turning it down.\n\nThe words are then this program's own, which is why the two are told apart here\nand not left to be read off the sentence: an operator scanning for what happened\nwhile they were away is looking for exactly the ones nobody answered, and a\nmember is owed the difference between having been refused and having run out.\n\nAbsent from every record written before a household could arrange this, which is\nthe right reading of them — they are all somebody's own refusals.",
              "type": "boolean",
              "default": false
            },
            "reason": {
              "description": "Why, in the words it was turned down in.",
              "type": "string"
            },
            "told": {
              "description": "Whether the words have been carried to whoever asked, and where to.\n\nAbsent until the one attempt has been made. **This is what makes a second one\nimpossible rather than unlikely**: carrying happens only where this is absent,\nand it is written whether anything was reached or not — so a member with nowhere\nto send to is asked about once, and a household is never told the same thing\ntwice by a run that happened to be started twice.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Passed"
                },
                {
                  "type": "null"
                }
              ],
              "default": null
            }
          },
          "required": [
            "reason"
          ]
        },
        "Restriction": {
          "description": "What one member is held to, in the words a household would use.\n\nThe two restrictions are one decision and two services: the media server decides\nwhat may be *watched* and the request service what may be *asked for*. Setting one\nwithout the other is the hole this vocabulary exists to name — a child who cannot\nwatch something but can pull it into the library has parents who set a limit and got\nhalf of one.",
          "oneOf": [
            {
              "description": "Nothing is held back from them.",
              "type": "string",
              "const": "unrestricted"
            },
            {
              "description": "Held to a highest rating.",
              "type": "string",
              "const": "rating-limited"
            },
            {
              "description": "Held to some of the libraries.",
              "type": "string",
              "const": "library-limited"
            },
            {
              "description": "Held to both a rating and some of the libraries.",
              "type": "string",
              "const": "both"
            },
            {
              "description": "What they may watch is limited and what they may ask for is not.",
              "type": "string",
              "const": "inconsistent"
            }
          ]
        },
        "Standing": {
          "description": "Where one member stands against what a period allows them.\n\nFour, and the one worth having is the middle: somebody told only when they have run\nout has been told too late to do anything but wait, which is the answer this whole\nfeature exists to avoid handing anybody.",
          "oneOf": [
            {
              "description": "Nothing is counted against them.",
              "type": "string",
              "const": "unlimited"
            },
            {
              "description": "There is room, and comfortably.",
              "type": "string",
              "const": "within-quota"
            },
            {
              "description": "A fifth of the period's limit or less is left, and at least one of it.",
              "type": "string",
              "const": "near-quota"
            },
            {
              "description": "Nothing is left, so the next thing they ask for is refused as they ask.",
              "type": "string",
              "const": "quota-exhausted"
            }
          ]
        },
        "State": {
          "description": "Where one request stands, in the words the person who made it would use.\n\nDeliberately coarser than a [`crate::trace::Stage`]: a member does not need to know\nthat a release was grabbed but not imported, only that it is on its way. The trace is\nwhere that detail stays, and a request names the item so it can be asked for.",
          "oneOf": [
            {
              "description": "Asked for, and nobody has approved or refused it yet.",
              "type": "string",
              "const": "waiting-for-approval"
            },
            {
              "description": "Turned down — it will not be fetched.",
              "type": "string",
              "const": "declined"
            },
            {
              "description": "Approved, but the attempt to fetch it failed.",
              "type": "string",
              "const": "failed"
            },
            {
              "description": "Approved and on its way — being searched for, downloaded or imported.",
              "type": "string",
              "const": "getting"
            },
            {
              "description": "Some of it is here: a series with only some of its episodes.",
              "type": "string",
              "const": "partly-here"
            },
            {
              "description": "Here, and playable.",
              "type": "string",
              "const": "here"
            },
            {
              "description": "It was here and has since been removed.",
              "type": "string",
              "const": "gone"
            }
          ]
        },
        "Unrated": {
          "description": "What is to happen to content the media server has no rating for.\n\nA choice rather than a default, because a great deal of content carries no rating\nand either answer is wrong for somebody: holding it back makes legitimate content\ninvisible, and letting it through lets through the one thing nobody vetted.\n\nSpelled `HeldBack` and `LetThrough` rather than blocked and allowed, because\n[`Allowed`] is the shape this sits on and a field called `unrated: Allowed` would\nread as the opposite of what it is.\n\nA word rather than a flag on both the read and the write, so the setting is named\nthe same in the answer that reports it as in the call that made it — and so neither\n[`Access`] nor the report built from it becomes a row of four unlabelled booleans.\n\nLetting it through is the default because it is the media server's: a new account\nis made holding nothing back, so that is the state an account is found in rather\nthan a decision anybody took.",
          "oneOf": [
            {
              "description": "Content with no rating is held back.",
              "type": "string",
              "const": "held-back"
            },
            {
              "description": "Content with no rating is let through.",
              "type": "string",
              "const": "let-through"
            }
          ]
        }
      }
    },
    "import": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/ImportReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "ImportReport": {
          "description": "What copying an operator's own records across came to, or would come to.",
          "type": "object",
          "properties": {
            "carried": {
              "description": "What was carried across.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/RecordReport"
              }
            },
            "not_carried": {
              "description": "What could not be carried, and why.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/UnsupportedReport"
              }
            },
            "project": {
              "description": "The project the records were read from.",
              "type": [
                "string",
                "null"
              ]
            },
            "refusal": {
              "description": "Why nothing was carried, where nothing was.",
              "type": [
                "string",
                "null"
              ]
            },
            "stance": {
              "description": "Where the act stands.",
              "$ref": "#/$defs/Stance"
            },
            "would_carry": {
              "description": "What would be, where nothing has been yet.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/RecordReport"
              }
            }
          },
          "required": [
            "carried",
            "would_carry",
            "not_carried",
            "stance"
          ]
        },
        "RecordReport": {
          "description": "One record carried across, or that would be.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "What kind of record it is, in the plural a person reads.",
              "type": "string"
            },
            "name": {
              "description": "What it is called.",
              "type": "string"
            },
            "service": {
              "description": "The service it belongs to.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "kind",
            "name"
          ]
        },
        "Stance": {
          "description": "Where a proposed change stands once it has been read against what is in force.",
          "oneOf": [
            {
              "description": "The setting already holds what was asked for. Nothing to apply, nothing to\nconfirm, and the file is not touched — a write of the same value would move\nthe file's own timestamp and read afterwards as an edit somebody made.",
              "type": "string",
              "const": "unchanged"
            },
            {
              "description": "Staged and not applied: nothing has been written. Either it is consequential\nand nobody has said yes to it yet, or it was rehearsed.\n\nThe default, because nothing having happened yet is what every act starts as,\nand a report built and not filled in should say that rather than claim it acted.",
              "type": "string",
              "const": "pending"
            },
            {
              "description": "Turned away. It cannot be applied safely, the file is exactly as it was, and\nthe refusal says why.",
              "type": "string",
              "const": "blocked"
            },
            {
              "description": "Written.",
              "type": "string",
              "const": "applied"
            }
          ]
        },
        "UnsupportedReport": {
          "description": "Something found that lemonfiber cannot take over, named rather than passed over.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why it cannot be adopted, in the operator's terms.",
              "type": "string"
            },
            "what": {
              "description": "What was found, by the name the engine gives it.",
              "type": "string"
            }
          },
          "required": [
            "what",
            "because"
          ]
        }
      }
    },
    "invitation": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Invitation"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Applied": {
          "description": "What an invitation wrote on the account, in the household's own words.\n\n**Said back so an absence later is explicable.** A member held to a rating who\ncannot find half the library is either this working or a defect, and an operator\nwith nothing on record cannot tell which. So the limit, the libraries, what happened\nto content the media server has no rating for, and whether the request service was\nheld to the same decision all travel back on the answer that applied them.\n\nAbsent where the offer set nothing at all, which is not the same as an offer that\nset no restrictions: naming neither a library nor a limit is saying nothing about\naccess, and nothing is what gets written.\n\nSerialised in the field names the household read uses for the same facts, rather\nthan in the invitation's own spelling: one setting named two ways across two shapes\nis two shapes a client has to be told are about the same thing.",
          "type": "object",
          "properties": {
            "filtering": {
              "description": "What a limit here is, and what it is not.\n\nCarried on the answer rather than left to a document, because the reader who\nmost needs it is the parent who has just set one.",
              "type": "string"
            },
            "libraries": {
              "description": "The libraries they may open, as the operator named them. Empty is every one.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "limit": {
              "description": "How far up the ratings they may watch, in the words and the certificates this\nmedia server names in the operator's own country. Absent where no limit was set.",
              "type": [
                "string",
                "null"
              ]
            },
            "requesting": {
              "description": "Whether the request service was held to the same decision.\n\nThe same three answers a link carries, and for the same reason: what somebody\nmay *ask for* is a second service's business, and that service can be down while\nthe media server is not. `NotTried` here is a service with no account for them\nyet — nothing to hold rather than a failure to hold something.",
              "$ref": "#/$defs/Linked"
            },
            "unrated": {
              "description": "What becomes of content the media server has no rating for.\n\nHeld back by default on somebody being narrowed, because a rating limit cannot\ndecide about a thing that carries no rating. The cost is real and is why this is\nreported rather than assumed: some legitimate content becomes invisible to them.",
              "$ref": "#/$defs/Unrated"
            }
          },
          "required": [
            "libraries",
            "unrated",
            "requesting",
            "filtering"
          ]
        },
        "Invitation": {
          "description": "One invitation, as it was just made.\n\nCarries what the operator has to pass on and nothing else — a name to sign in\nwith, one address, and how long it stands. The address is the media server's,\nbecause setting a first password happens there.",
          "type": "object",
          "properties": {
            "address": {
              "description": "The one address to send them.\n\nWhere a *person* reaches the media server: built from what this machine is\ncalled on the network, not from either of the hosts the stack wires itself\nwith — those resolve only on this machine or inside the stack, and an\ninvitation carrying one sends somebody an address that cannot open.",
              "type": "string"
            },
            "applied": {
              "description": "What this offer wrote on the account, where it wrote anything.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Applied"
                },
                {
                  "type": "null"
                }
              ]
            },
            "caution": {
              "description": "What is worth knowing about the address itself, where anything is.\n\nAn address that is a number is one a router can hand elsewhere, so a bookmark\nmade from it stops working with nothing here having changed. Carried on the\ninvitation because that is the copy somebody keeps.",
              "type": [
                "string",
                "null"
              ]
            },
            "hours": {
              "description": "How many hours it stands before it is withdrawn.\n\nCounted from when it was *offered*, which for an account whose password was\ntaken off is the moment of the reset rather than when the account was made.\nWhat happens at the end is withdrawal, and withdrawal removes the account.",
              "type": "integer",
              "format": "int64"
            },
            "linked": {
              "description": "Whether the request service knows about the household yet.\n\nSeparate from `standing`, which is about the media-server account alone. The\ntwo can disagree — an account made while the request service was unreachable\nis `Made` and `NotYet` — and that disagreement is the state this reports.",
              "$ref": "#/$defs/Linked"
            },
            "name": {
              "description": "The name they sign in as.",
              "type": "string"
            },
            "rehearsed": {
              "description": "Whether the account was made, or only described.\n\nA rehearsal can say the whole answer without writing any of it — the name is\nthe one asked for, the address is the stack's, and what has run out has just\nbeen read — so the only thing separating it from the real run is this.",
              "type": "boolean"
            },
            "standing": {
              "description": "What was found where this was going.",
              "$ref": "#/$defs/InvitationStanding"
            },
            "withdrawn": {
              "description": "Invitations nobody claimed in time, taken back on the way past.\n\nReported rather than done quietly: an operator who invited somebody last\nweek and hears nothing would otherwise have no way to learn the account is\ngone. On a rehearsal these are the ones that *would* be taken back.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "name",
            "address",
            "hours",
            "withdrawn",
            "rehearsed",
            "standing",
            "linked"
          ]
        },
        "InvitationStanding": {
          "description": "What was found where the invitation was going.\n\nOffering somebody an account twice is a thing operators do — they forget, or the\nfirst message went unanswered — and it is not a mistake to be refused. Each of\nthese is an answer, and which one it is decides what there is to say rather than\nwhether anything worked.",
          "oneOf": [
            {
              "description": "The account did not exist, and was made.",
              "type": "string",
              "const": "made"
            },
            {
              "description": "An invitation was already out for them, and it still stands.\n\nThe same account, offered again: nothing is made, and the message to send is\nthe one that was already true.",
              "type": "string",
              "const": "waiting"
            },
            {
              "description": "They have set a password, so they are already in the house.\n\nNothing to claim and nothing to send. An account is not made, because the one\nthey have is theirs and a second under a nearly identical name is how a\nhousehold ends up with two of somebody.",
              "type": "string",
              "const": "joined"
            },
            {
              "description": "The account was theirs already, and its password has been taken off.\n\n**Told apart from [`Made`](Self::Made) because what the person needs to hear is\ndifferent.** Nobody is being invited: they are already in the household, and the\nnews is that the password they had has stopped working. It runs out on the same\nwindow an offer does, counted from the reset — and what is at stake if it does is\nlarger, because the account being withdrawn is one they have watched on.",
              "type": "string",
              "const": "reset"
            }
          ]
        },
        "Linked": {
          "description": "Whether the request service has been given an account for the household yet.\n\nThe account somebody watches with is made on the media server, and it stands on its\nown from that moment — nothing has to be running for them to claim it. Being able\nto *ask* for something is a second account, on a second service, and that service\ncan be down while the first is not.\n\nSo this is reported rather than made a condition of the invitation: an operator who\ninvites somebody during an outage has still invited them, and what they cannot do\nyet is worth one line rather than a refusal.",
          "oneOf": [
            {
              "description": "It holds an account for everybody the media server does.",
              "type": "string",
              "const": "made"
            },
            {
              "description": "It could not be told. The media-server account stands, and the next run tells\nit — the service skips anybody it already knows, so nothing has to be\nremembered in between.",
              "type": "string",
              "const": "not-yet"
            },
            {
              "description": "Nothing was tried: a rehearsal, or a stack with no request service at all.\n\nNot a failure either way, and nothing a later run would put right — which is\nwhat separates it from `NotYet`.",
              "type": "string",
              "const": "not-tried"
            }
          ]
        },
        "Unrated": {
          "description": "What is to happen to content the media server has no rating for.\n\nA choice rather than a default, because a great deal of content carries no rating\nand either answer is wrong for somebody: holding it back makes legitimate content\ninvisible, and letting it through lets through the one thing nobody vetted.\n\nSpelled `HeldBack` and `LetThrough` rather than blocked and allowed, because\n[`Allowed`] is the shape this sits on and a field called `unrated: Allowed` would\nread as the opposite of what it is.\n\nA word rather than a flag on both the read and the write, so the setting is named\nthe same in the answer that reports it as in the call that made it — and so neither\n[`Access`] nor the report built from it becomes a row of four unlabelled booleans.\n\nLetting it through is the default because it is the media server's: a new account\nis made holding nothing back, so that is the state an account is found in rather\nthan a decision anybody took.",
          "oneOf": [
            {
              "description": "Content with no rating is held back.",
              "type": "string",
              "const": "held-back"
            },
            {
              "description": "Content with no rating is let through.",
              "type": "string",
              "const": "let-through"
            }
          ]
        }
      }
    },
    "job": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Started"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Started": {
          "description": "Work that outlives the request that started it: its name, and what it was.\n\nThe action is carried beside the name because a client holding several has to\ntell them apart, and asking it to remember which name it gave which request is\nasking it to keep a second copy of what this already knows.",
          "type": "object",
          "properties": {
            "action": {
              "description": "The action that was asked for, as it was named.",
              "type": "string"
            },
            "job": {
              "description": "The name to ask what became of this work by.",
              "type": "string"
            }
          },
          "required": [
            "job",
            "action"
          ]
        }
      }
    },
    "lifecycle": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/LifecycleReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Condition": {
          "description": "What a whole set of services amounts to.",
          "oneOf": [
            {
              "description": "Nothing is running.",
              "type": "string",
              "const": "inactive"
            },
            {
              "description": "Running, with at least one service in a state that wants attention.",
              "type": "string",
              "const": "degraded"
            },
            {
              "description": "Some services are up and others are not.",
              "type": "string",
              "const": "partial"
            },
            {
              "description": "Everything expected is up and answering.",
              "type": "string",
              "const": "active"
            }
          ]
        },
        "Criticality": {
          "description": "How much a service's absence costs.\n\nSerialisable as well as readable, because it reaches an operator: a status\nreport that says a service is down without saying whether that matters\nleaves them to guess, and the manifest already holds the answer.",
          "oneOf": [
            {
              "description": "Its failure has consequences outside the machine.",
              "type": "string",
              "const": "critical"
            },
            {
              "description": "The form does not work without it.",
              "type": "string",
              "const": "core"
            },
            {
              "description": "The form works, badly.",
              "type": "string",
              "const": "important"
            },
            {
              "description": "Makes things better.",
              "type": "string",
              "const": "enhancing"
            },
            {
              "description": "Nice to have.",
              "type": "string",
              "const": "optional"
            }
          ]
        },
        "Dropped": {
          "description": "A profile left out of a closure, and what it would have needed.\n\nThe provider travels with the profile because a name on its own sends the\noperator looking for a fault. What they have is a stack not configured for\none of the two ways of downloading, which is a sentence rather than a word.",
          "type": "object",
          "properties": {
            "needs": {
              "description": "The provider it cannot run without.",
              "$ref": "#/$defs/Protocol"
            },
            "profile": {
              "description": "The profile that will not run.",
              "type": "string"
            }
          },
          "required": [
            "profile",
            "needs"
          ]
        },
        "LifecycleReport": {
          "description": "What a lifecycle command did, or would have done.",
          "type": "object",
          "properties": {
            "action": {
              "description": "The Compose subcommand that was run.",
              "type": "string"
            },
            "command": {
              "description": "The exact command, so what happened is never a matter of trust.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "condition": {
              "description": "What those services amount to, as one word.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Condition"
                },
                {
                  "type": "null"
                }
              ]
            },
            "forwarding": {
              "description": "What starting the stack did about the VPN's forwarded port, where it did\nanything. Absent in the ordinary case — the client was already on it, or\nthere is no tunnel to forward through — and a sentence where the client was\nmoved, or could not be.",
              "type": [
                "string",
                "null"
              ]
            },
            "plan": {
              "description": "What the named forms came to: the profiles, the services they hold, and\nwhat the configuration left out.\n\nThe resolved plan itself rather than a copy of its parts, because it is\nstated to the operator before the command runs and read out of the\nreport afterwards — two accounts of one run, and a second shape for it\nwould be a way for them to differ.",
              "$ref": "#/$defs/Plan"
            },
            "rehearsed": {
              "description": "Whether this was a rehearsal.",
              "type": "boolean"
            },
            "services": {
              "description": "What each service ended up doing, where the action waited to find out.\n\nEmpty for actions that do not wait. Stopping is finished when Compose\nsays it is, and surveying afterwards would only report the absence it\nwas asked to produce.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Service"
              }
            },
            "stack_edits": {
              "description": "Stack files the operator has edited, left as they set them rather than\noverwritten with lemonfiber's own. Empty in the ordinary case; a named entry\nwarns that an upgrade would change a file they changed, and shows the diff.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/StackEdit"
              }
            },
            "status": {
              "description": "The exit status, absent for a rehearsal or a signalled process.",
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "switched": {
              "description": "What narrowing moved, where the command was a switch. Absent for every\nother action, which is what tells a reader that nothing was left running\non purpose.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Switched"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "action",
            "plan",
            "command",
            "rehearsed",
            "services",
            "stack_edits"
          ]
        },
        "Plan": {
          "description": "What will be run, and what was left out.\n\nSerialisable because it is an answer in its own right: asking what a form\nwould do is a question a script asks as readily as a person, and the plan a\nlifecycle report carries is this same value rather than a retelling of it.",
          "type": "object",
          "properties": {
            "dropped": {
              "description": "Profiles the closure asked for that the configuration does not support.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Dropped"
              }
            },
            "forms": {
              "description": "The forms the operator named, in the order they named them.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "profiles": {
              "description": "The profiles to activate, sorted so the command is reproducible.",
              "type": "array",
              "items": {
                "type": "string"
              },
              "uniqueItems": true
            },
            "services": {
              "description": "The services those profiles start, in the order the stack declares them.\n\nA service belongs to exactly one profile, so a service two named forms\nboth reach is here once. That is a property of the manifest rather than\nof a pass over this list: the union is over profiles, and a service\nappearing twice is not a state this can hold.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "forms",
            "profiles",
            "services",
            "dropped"
          ]
        },
        "Protocol": {
          "description": "A download provider a profile can depend on.\n\nSerialisable as well as readable, for the same reason [`Criticality`] is: it\nreaches an operator. A profile left out of a closure is only half reported\nwithout the provider it wanted.",
          "oneOf": [
            {
              "description": "Needs a Usenet provider.",
              "type": "string",
              "const": "usenet"
            },
            {
              "description": "Needs a VPN and a torrent client.",
              "type": "string",
              "const": "torrent"
            }
          ]
        },
        "Service": {
          "description": "One service, as it stands.",
          "type": "object",
          "properties": {
            "criticality": {
              "description": "How much its absence costs, so a summary can weigh it.",
              "$ref": "#/$defs/Criticality"
            },
            "depends_on": {
              "description": "The services it needs before it can work, as the manifest declares them.\nCarried so a failure can be attributed to the thing underneath it rather\nthan counted as one more independent thing wrong.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "exit": {
              "description": "How it exited, where it has exited.",
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "id": {
              "description": "The service's identifier, which is also its Compose service name.",
              "type": "string"
            },
            "name": {
              "description": "What it is called in front of an operator.",
              "type": "string"
            },
            "profile": {
              "description": "The profile that declared it.",
              "type": "string"
            },
            "state": {
              "description": "What it is doing.",
              "$ref": "#/$defs/State"
            }
          },
          "required": [
            "id",
            "name",
            "profile",
            "state",
            "criticality",
            "depends_on"
          ]
        },
        "StackEdit": {
          "description": "A stack file the operator edited, preserved rather than overwritten, with the\nchange an upgrade would make shown against it.",
          "type": "object",
          "properties": {
            "diff": {
              "description": "The lines that differ between the operator's file and what lemonfiber would\nwrite — theirs marked `-`, lemonfiber's `+`, the matching head and tail left\nout. Empty where the two differ only in ways `lines` does not see.",
              "type": "string"
            },
            "path": {
              "description": "The file's path within the stack directory.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "diff"
          ]
        },
        "State": {
          "description": "What one service is actually doing.\n\nOrdered from worst to best, so a form's condition is the minimum across its\nservices and needs no comparison table. The declaration order is therefore\nload-bearing.",
          "oneOf": [
            {
              "description": "Exited without being asked to, and is not coming back on its own.",
              "type": "string",
              "const": "failed"
            },
            {
              "description": "Exiting and restarting repeatedly.",
              "type": "string",
              "const": "crash-looping"
            },
            {
              "description": "Running, and its own probe says it is not working.",
              "type": "string",
              "const": "unhealthy"
            },
            {
              "description": "No container exists.",
              "type": "string",
              "const": "absent"
            },
            {
              "description": "A container exists and is not running.",
              "type": "string",
              "const": "stopped"
            },
            {
              "description": "Running, and still inside its probe's start period.",
              "type": "string",
              "const": "starting"
            },
            {
              "description": "Running, and declaring no probe by which to know more.\n\nDistinct from healthy on purpose. A service that cannot be asked is not\na service that answered, and rendering the two identically is how a\ndashboard comes to be believed about something it never checked.",
              "type": "string",
              "const": "running"
            },
            {
              "description": "Running and passing its own probe.",
              "type": "string",
              "const": "healthy"
            },
            {
              "description": "The operating system owns this one, not lemonfiber.",
              "type": "string",
              "const": "host-managed"
            }
          ]
        },
        "Switched": {
          "description": "What narrowing the active set moved.\n\nThree lists rather than a before and an after, because the operator's question\nis not \"what is running now\" — they can ask that — but \"what did that do\". The\nmiddle list is the one that makes the verb worth having: it is the promise that\na download in flight was not interrupted to change the shape of the stack\naround it.",
          "type": "object",
          "properties": {
            "kept": {
              "description": "Left running: the new closure holds them too, so nothing here asked them to\nstop. Not a promise that nothing touched them — Compose recreates a container\nwhose configuration changed — but a promise that narrowing did not.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "started": {
              "description": "Started, because the new closure holds them and they were not up.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "stop_command": {
              "description": "The exact Compose invocation that stopped what fell outside, so a switch is\nno more a matter of trust than any other action. Absent where nothing had\nto stop.",
              "type": [
                "array",
                "null"
              ],
              "items": {
                "type": "string"
              }
            },
            "stopped": {
              "description": "Stopped, because the new closure does not hold them.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "stopped",
            "started",
            "kept"
          ]
        }
      }
    },
    "log": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/LogLine"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "LogLine": {
          "description": "One line of output from one service.\n\nSerialisable because a log stream is part of the machine-readable contract:\n`--json` renders one envelope per line, since a stream has no last element\nto close a document with.",
          "type": "object",
          "properties": {
            "at": {
              "description": "When the container itself says it wrote the line, where it said so.\n\nKept verbatim and unparsed. Containers disagree with the host clock and\nwith each other, and the only defensible ordering is each container's own\naccount of itself — which a reader can only apply if it is carried\nrather than replaced by an arrival time.",
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "description": "The line, without its trailing newline.",
              "type": "string"
            },
            "service": {
              "description": "The Compose service it came from.",
              "type": "string"
            },
            "stream": {
              "description": "Which stream it arrived on.",
              "$ref": "#/$defs/Stream"
            }
          },
          "required": [
            "service",
            "stream",
            "line"
          ]
        },
        "Stream": {
          "description": "Which stream a log line arrived on.",
          "oneOf": [
            {
              "description": "Standard output.",
              "type": "string",
              "const": "stdout"
            },
            {
              "description": "Standard error.",
              "type": "string",
              "const": "stderr"
            }
          ]
        }
      }
    },
    "migration": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/MigrationReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "CarryingReport": {
          "description": "What adopting one existing service would come to.",
          "type": "object",
          "properties": {
            "backup_first": {
              "description": "Whether its database must be backed up before lemonfiber opens it.",
              "type": "boolean"
            },
            "because": {
              "description": "What that means for this service's data, in the operator's terms.",
              "type": "string"
            },
            "existing": {
              "description": "The version standing here now.",
              "type": "string"
            },
            "ours": {
              "description": "The version lemonfiber pins.",
              "type": "string"
            },
            "refused": {
              "description": "Whether lemonfiber will not do this at all.",
              "type": "boolean"
            },
            "service": {
              "description": "The service, by the name lemonfiber runs it under.",
              "type": "string"
            },
            "verdict": {
              "description": "Which of the two is the later, in one word.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "existing",
            "ours",
            "verdict",
            "because",
            "backup_first",
            "refused"
          ]
        },
        "ConflictReport": {
          "description": "A port lemonfiber wants for a service that something else already answers on.",
          "type": "object",
          "properties": {
            "held_by": {
              "description": "The project already holding it.",
              "type": "string"
            },
            "port": {
              "description": "The host port both want.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "wanted_by": {
              "description": "The lemonfiber service that would publish it.",
              "type": "string"
            }
          },
          "required": [
            "port",
            "wanted_by",
            "held_by"
          ]
        },
        "LinkingReport": {
          "description": "What an existing layout costs, where it cannot hold a hardlink.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why they cannot, naming the filesystems it is about.",
              "type": "string"
            },
            "cost": {
              "description": "What that costs, in room rather than in adjectives.",
              "type": "string"
            },
            "filesystems": {
              "description": "The filesystems the existing setup keeps its data on.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "forced": {
              "description": "Whether lemonfiber will do it. Always false: the layout and the library in it\nare the operator's, and correctness does not outrank their data.",
              "type": "boolean"
            },
            "links": {
              "description": "Whether imports can be hardlinks across this layout. False whenever this is\nreported at all, since a layout that links is not reported.",
              "type": "boolean"
            },
            "remedy": {
              "description": "What would fix it, offered.",
              "type": "string"
            }
          },
          "required": [
            "links",
            "because",
            "cost",
            "remedy",
            "forced",
            "filesystems"
          ]
        },
        "MigrationReport": {
          "description": "What is already on this machine, before anything is proposed.",
          "type": "object",
          "properties": {
            "beside": {
              "description": "Where each service would listen to run beside the existing setup.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/MovedReport"
              }
            },
            "carrying": {
              "description": "What adopting each recognised service would come to, by service name.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/CarryingReport"
              }
            },
            "conflicts": {
              "description": "Ports wanted by lemonfiber that an existing service already holds.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/ConflictReport"
              }
            },
            "linking": {
              "description": "What the existing layout costs where it cannot hold a hardlink, absent where\nit can.",
              "anyOf": [
                {
                  "$ref": "#/$defs/LinkingReport"
                },
                {
                  "type": "null"
                }
              ]
            },
            "modes": {
              "description": "What may be done about what was found, least destructive first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/ModeReport"
              }
            },
            "not_carried": {
              "description": "What no migration carries across, whatever mode it runs in.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/UnsupportedReport"
              }
            },
            "read": {
              "description": "Whether the engine answered at all.\n\nFalse means the survey found nothing because it could not look, which is a\ndifferent answer from finding nothing, and the only one that must never be\nread as an empty machine.",
              "type": "boolean"
            },
            "standing": {
              "description": "Existing projects, by project name.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/StandingReport"
              }
            },
            "unsupported": {
              "description": "What was found and cannot be adopted.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/UnsupportedReport"
              }
            }
          },
          "required": [
            "read",
            "standing",
            "conflicts",
            "unsupported",
            "carrying",
            "not_carried",
            "modes",
            "beside"
          ]
        },
        "ModeReport": {
          "description": "One thing an operator may do about a setup already here.",
          "type": "object",
          "properties": {
            "disturbs": {
              "description": "Whether carrying it out stops or alters what is already running.",
              "type": "boolean"
            },
            "mode": {
              "description": "The word an operator types for it.",
              "type": "string"
            },
            "preselected": {
              "description": "Whether it is offered already chosen. Only adopting is.",
              "type": "boolean"
            },
            "what": {
              "description": "What choosing it would come to, in the operator's terms.",
              "type": "string"
            }
          },
          "required": [
            "mode",
            "preselected",
            "what",
            "disturbs"
          ]
        },
        "MovedReport": {
          "description": "Where one service would listen to run beside what is already here.",
          "type": "object",
          "properties": {
            "from": {
              "description": "The port it would ordinarily take.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "service": {
              "description": "The lemonfiber service being moved.",
              "type": "string"
            },
            "to": {
              "description": "The port it would take instead.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            }
          },
          "required": [
            "service",
            "from",
            "to"
          ]
        },
        "OccupantReport": {
          "description": "One container of somebody else's stack, as the engine reports it.",
          "type": "object",
          "properties": {
            "adoptable": {
              "description": "Whether lemonfiber knows this service and could take it over as it stands.",
              "type": "boolean"
            },
            "ports": {
              "description": "Every host port it publishes, lowest first.",
              "type": "array",
              "items": {
                "type": "integer",
                "format": "uint16",
                "maximum": 65535,
                "minimum": 0
              }
            },
            "running": {
              "description": "Whether it is running now, as against present but stopped.",
              "type": "boolean"
            },
            "service": {
              "description": "The Compose service name it answers to.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "running",
            "ports",
            "adoptable"
          ]
        },
        "StandingReport": {
          "description": "One Compose project on this machine that is not lemonfiber's.",
          "type": "object",
          "properties": {
            "project": {
              "description": "The Compose project name.",
              "type": "string"
            },
            "services": {
              "description": "Its containers, by service name.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/OccupantReport"
              }
            }
          },
          "required": [
            "project",
            "services"
          ]
        },
        "UnsupportedReport": {
          "description": "Something found that lemonfiber cannot take over, named rather than passed over.",
          "type": "object",
          "properties": {
            "because": {
              "description": "Why it cannot be adopted, in the operator's terms.",
              "type": "string"
            },
            "what": {
              "description": "What was found, by the name the engine gives it.",
              "type": "string"
            }
          },
          "required": [
            "what",
            "because"
          ]
        }
      }
    },
    "music": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/MusicReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Disposition": {
          "description": "What a quality command did to the stored choice.",
          "oneOf": [
            {
              "description": "The choice was only shown; nothing was asked to change.",
              "type": "string",
              "const": "shown"
            },
            {
              "description": "The choice was recorded.",
              "type": "string",
              "const": "recorded"
            },
            {
              "description": "The choice would be recorded; this was a rehearsal, so it was not.",
              "type": "string",
              "const": "rehearsed"
            },
            {
              "description": "The choice needs transcoding this host cannot do well, so it was held\nrather than recorded without explicit confirmation.",
              "type": "string",
              "const": "held"
            },
            {
              "description": "The recorded preset was re-asserted over the Recyclarr config, overwriting\na hand-edit where an ordinary run would have preserved it.",
              "type": "string",
              "const": "reapplied"
            },
            {
              "description": "A re-assert that was a rehearsal: it reports whether it would overwrite a\nhand-edit, and writes nothing.",
              "type": "string",
              "const": "would-reapply"
            }
          ]
        },
        "MusicChoice": {
          "description": "One audio-format choice in force, for media that has no resolution — the same\nquestion as a [`PresetChoice`], answered in format terms rather than resolution.",
          "type": "object",
          "properties": {
            "format": {
              "description": "The format's plain-language name.",
              "type": "string"
            },
            "means": {
              "description": "What it means, in the operator's terms.",
              "type": "string"
            },
            "note": {
              "description": "The practical caveat worth knowing — playing it, or finding it.",
              "type": "string"
            },
            "scope": {
              "description": "What this applies to — `music`.",
              "type": "string"
            },
            "size_per_hour": {
              "description": "Roughly how much disk an hour of it takes.",
              "type": "string"
            },
            "targets": {
              "description": "The audio format it targets, in plain terms.",
              "type": "string"
            }
          },
          "required": [
            "scope",
            "format",
            "means",
            "targets",
            "size_per_hour",
            "note"
          ]
        },
        "MusicReport": {
          "description": "What choosing an audio format for music did: the choice, whether it was recorded\nor only rehearsed, and — once recorded — what became of applying it to the music\nservice.\n\nMusic has no resolution and no community profile to lean on, so unlike a resolution\npreset the choice is carried straight to the service through its API. The choice is\nstill recorded first, so it is remembered even when the service cannot be reached.",
          "type": "object",
          "properties": {
            "choice": {
              "description": "The format chosen, what it means, and what it costs.",
              "$ref": "#/$defs/MusicChoice"
            },
            "disposition": {
              "description": "Whether the choice was recorded, or only rehearsed.",
              "$ref": "#/$defs/Disposition"
            },
            "outcome": {
              "description": "What became of applying it to the music service, or `None` for a rehearsal\nthat applied nothing.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Triggered"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "choice",
            "disposition"
          ]
        },
        "Triggered": {
          "description": "What became of asking one service to re-search its existing content.",
          "oneOf": [
            {
              "description": "The re-search was accepted and now runs in the service's background.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "started"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "The service had not finished starting — no key yet — so nothing was asked of\nit; running the upgrade again once it is up will reach it.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "not-started"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "The service refused the command or could not be reached.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "The service's own account of why.",
                  "type": "string"
                },
                "state": {
                  "type": "string",
                  "const": "failed"
                }
              },
              "required": [
                "state",
                "detail"
              ]
            }
          ]
        }
      }
    },
    "outbound": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Leaving"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Elsewhere": {
          "description": "A request one of the stack's services makes, which is not lemonfiber's.",
          "type": "object",
          "properties": {
            "destination": {
              "description": "Where its requests go, in the terms an operator would recognise.",
              "type": "string"
            },
            "purpose": {
              "description": "What it asks for.",
              "type": "string"
            },
            "service": {
              "description": "The service, by the id the stack declares it under.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "destination",
            "purpose"
          ]
        },
        "Leaving": {
          "description": "Everything that leaves this machine: lemonfiber's own requests, and the stack's.",
          "type": "object",
          "properties": {
            "ours": {
              "description": "Every request lemonfiber makes on its own account, in a fixed order.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Outbound"
              }
            },
            "theirs": {
              "description": "The requests made by services this stack runs, attributed to them.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Elsewhere"
              }
            }
          },
          "required": [
            "ours",
            "theirs"
          ]
        },
        "Outbound": {
          "description": "One request lemonfiber makes, where it goes, and what refusing it costs.",
          "type": "object",
          "properties": {
            "allowed": {
              "description": "Whether this machine's settings allow it.",
              "type": "boolean"
            },
            "cost": {
              "description": "What stops working once it is off.",
              "type": "string"
            },
            "destination": {
              "description": "Where it goes as this machine is configured. Empty where nothing is\nconfigured to reach, which is not the same as switched off.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "purpose": {
              "description": "Why lemonfiber asks.",
              "type": "string"
            },
            "reach": {
              "description": "Which request this is.",
              "$ref": "#/$defs/Reach"
            },
            "sends": {
              "description": "Exactly what travels in the request.",
              "type": "string"
            },
            "switch": {
              "description": "The setting that switches it off.",
              "type": "string"
            }
          },
          "required": [
            "reach",
            "destination",
            "purpose",
            "sends",
            "allowed",
            "switch",
            "cost"
          ]
        },
        "Reach": {
          "description": "One of the requests lemonfiber makes on its own account.\n\nSeven, and the closed set is the claim. An eighth is a decision somebody makes by\nadding a variant here and answering four questions about it, rather than one\nthat happens by somebody building a request.\n\nThe sixth is the one that carries somebody's words rather than a credential or a\nname, and it was added deliberately and late: five of these prove or fetch\nsomething and could say what travels in a phrase, and this one travels to a\nperson. What that costs is the same four answers as the rest, and one more thing\nthe others do not owe — it is the only entry whose destination is somebody else's\nchoice, so the list names the two services it can reach and the sender is handed\nthem rather than holding addresses of its own.\n\nThe seventh is the only one this program makes about *itself*, and it is the one\nwhose absence used to be the claim. What it costs to allow is the shortest answer\non the list: it carries nothing at all, so the only thing switching it off keeps\nfrom anybody is the knowledge that a version came out.",
          "oneOf": [
            {
              "description": "Fetching the service images the stack runs.",
              "type": "string",
              "const": "registry"
            },
            {
              "description": "Probing the source the community quality guides are synced from.",
              "type": "string",
              "const": "guides"
            },
            {
              "description": "Asking what public address this machine's traffic comes out of.",
              "type": "string",
              "const": "echo"
            },
            {
              "description": "Proving an indexer key against the indexer.",
              "type": "string",
              "const": "indexer"
            },
            {
              "description": "Proving a Usenet login against the provider.",
              "type": "string",
              "const": "usenet"
            },
            {
              "description": "Telling a household member the one thing the request service cannot carry.",
              "type": "string",
              "const": "household"
            },
            {
              "description": "Asking which version of lemonfiber itself has been released.",
              "type": "string",
              "const": "updates"
            }
          ]
        }
      }
    },
    "preview": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Plan"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Dropped": {
          "description": "A profile left out of a closure, and what it would have needed.\n\nThe provider travels with the profile because a name on its own sends the\noperator looking for a fault. What they have is a stack not configured for\none of the two ways of downloading, which is a sentence rather than a word.",
          "type": "object",
          "properties": {
            "needs": {
              "description": "The provider it cannot run without.",
              "$ref": "#/$defs/Protocol"
            },
            "profile": {
              "description": "The profile that will not run.",
              "type": "string"
            }
          },
          "required": [
            "profile",
            "needs"
          ]
        },
        "Plan": {
          "description": "What will be run, and what was left out.\n\nSerialisable because it is an answer in its own right: asking what a form\nwould do is a question a script asks as readily as a person, and the plan a\nlifecycle report carries is this same value rather than a retelling of it.",
          "type": "object",
          "properties": {
            "dropped": {
              "description": "Profiles the closure asked for that the configuration does not support.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Dropped"
              }
            },
            "forms": {
              "description": "The forms the operator named, in the order they named them.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "profiles": {
              "description": "The profiles to activate, sorted so the command is reproducible.",
              "type": "array",
              "items": {
                "type": "string"
              },
              "uniqueItems": true
            },
            "services": {
              "description": "The services those profiles start, in the order the stack declares them.\n\nA service belongs to exactly one profile, so a service two named forms\nboth reach is here once. That is a property of the manifest rather than\nof a pass over this list: the union is over profiles, and a service\nappearing twice is not a state this can hold.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "forms",
            "profiles",
            "services",
            "dropped"
          ]
        },
        "Protocol": {
          "description": "A download provider a profile can depend on.\n\nSerialisable as well as readable, for the same reason [`Criticality`] is: it\nreaches an operator. A profile left out of a closure is only half reported\nwithout the provider it wanted.",
          "oneOf": [
            {
              "description": "Needs a Usenet provider.",
              "type": "string",
              "const": "usenet"
            },
            {
              "description": "Needs a VPN and a torrent client.",
              "type": "string",
              "const": "torrent"
            }
          ]
        }
      }
    },
    "pull": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "type": "string"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ]
    },
    "quality": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/QualityReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Disposition": {
          "description": "What a quality command did to the stored choice.",
          "oneOf": [
            {
              "description": "The choice was only shown; nothing was asked to change.",
              "type": "string",
              "const": "shown"
            },
            {
              "description": "The choice was recorded.",
              "type": "string",
              "const": "recorded"
            },
            {
              "description": "The choice would be recorded; this was a rehearsal, so it was not.",
              "type": "string",
              "const": "rehearsed"
            },
            {
              "description": "The choice needs transcoding this host cannot do well, so it was held\nrather than recorded without explicit confirmation.",
              "type": "string",
              "const": "held"
            },
            {
              "description": "The recorded preset was re-asserted over the Recyclarr config, overwriting\na hand-edit where an ordinary run would have preserved it.",
              "type": "string",
              "const": "reapplied"
            },
            {
              "description": "A re-assert that was a rehearsal: it reports whether it would overwrite a\nhand-edit, and writes nothing.",
              "type": "string",
              "const": "would-reapply"
            }
          ]
        },
        "MusicChoice": {
          "description": "One audio-format choice in force, for media that has no resolution — the same\nquestion as a [`PresetChoice`], answered in format terms rather than resolution.",
          "type": "object",
          "properties": {
            "format": {
              "description": "The format's plain-language name.",
              "type": "string"
            },
            "means": {
              "description": "What it means, in the operator's terms.",
              "type": "string"
            },
            "note": {
              "description": "The practical caveat worth knowing — playing it, or finding it.",
              "type": "string"
            },
            "scope": {
              "description": "What this applies to — `music`.",
              "type": "string"
            },
            "size_per_hour": {
              "description": "Roughly how much disk an hour of it takes.",
              "type": "string"
            },
            "targets": {
              "description": "The audio format it targets, in plain terms.",
              "type": "string"
            }
          },
          "required": [
            "scope",
            "format",
            "means",
            "targets",
            "size_per_hour",
            "note"
          ]
        },
        "PresetChoice": {
          "description": "One preset in force, and what it means for the media it applies to — the\noperator's question answered in their own terms, with no scoring vocabulary.",
          "type": "object",
          "properties": {
            "means": {
              "description": "What it means, in the operator's terms rather than the tool's.",
              "type": "string"
            },
            "needs_transcoding_here": {
              "description": "Whether this host would have to transcode it in software — the caution\nstated before a choice a household cannot smoothly play.",
              "type": "boolean"
            },
            "preset": {
              "description": "The preset's plain-language name.",
              "type": "string"
            },
            "resolution": {
              "description": "The resolution and encode it targets.",
              "type": "string"
            },
            "scope": {
              "description": "What this applies to: `everything`, or a specific media type.",
              "type": "string"
            },
            "size_per_hour": {
              "description": "Roughly how much disk an hour of it takes.",
              "type": "string"
            },
            "transcoding": {
              "description": "What playback costs, in plain terms.",
              "type": "string"
            }
          },
          "required": [
            "scope",
            "preset",
            "means",
            "resolution",
            "size_per_hour",
            "transcoding",
            "needs_transcoding_here"
          ]
        },
        "QualityReport": {
          "description": "The operator's quality choice, what each preset means, and what the command\ndid with it.",
          "type": "object",
          "properties": {
            "choices": {
              "description": "The global choice first, then each media type set apart from it.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/PresetChoice"
              }
            },
            "customised": {
              "description": "Whether the Recyclarr config has been hand-edited since lemonfiber wrote it —\nthe `customised` state, in which the preset is no longer authoritative until\nit is deliberately re-asserted. For a reapply, whether an edit was overwritten.",
              "type": "boolean"
            },
            "disposition": {
              "description": "What became of the choice.",
              "$ref": "#/$defs/Disposition"
            },
            "music": {
              "description": "The audio-format choice for music, where one is set — media that has no\nresolution, so it is reported apart from the resolution presets rather than\nforced into their shape.",
              "anyOf": [
                {
                  "$ref": "#/$defs/MusicChoice"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "choices",
            "customised",
            "disposition"
          ]
        }
      }
    },
    "removal": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/HouseholdRemoval"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "HouseholdRemoval": {
          "description": "What removing somebody costs, and what it did.\n\nRead before anything is written: the whole point of the unconfirmed run is that every\nfigure here is knowable without removing anybody.",
          "type": "object",
          "properties": {
            "asks-through-the-request-service": {
              "description": "Whether the request service holds an account for them at all.\n\nFalse where they never signed in there, which is nothing to revoke rather than a\nrevocation that failed.",
              "type": "boolean"
            },
            "confirmed": {
              "description": "Whether it was carried out, or only described pending confirmation.",
              "type": "boolean"
            },
            "findings": {
              "description": "What could not be done, and anything else worth the operator's attention.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "name": {
              "description": "The name their account is held under, as the media server spells it rather than\nas the operator typed it.",
              "type": "string"
            },
            "requests": {
              "description": "How many of their requests go with them.\n\n**They are destroyed, not reassigned.** The request service removes them by hand\nso that a title still waiting goes back to being unrequested rather than pointing\nat nobody — so this is a count of things that will stop existing.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "revoked": {
              "description": "How far it got.",
              "$ref": "#/$defs/Revoked"
            }
          },
          "required": [
            "name",
            "confirmed",
            "requests",
            "asks-through-the-request-service",
            "revoked",
            "findings"
          ]
        },
        "Revoked": {
          "description": "How far a removal got, across the two services a household member exists on.\n\nThe media server is removed first and the request service second, because the\nrequest service authenticates *through* the media server — so once the first is gone\nthey can do nothing either way, and a failure at the second leaves an account that\ncannot sign in rather than somebody who can still watch.",
          "oneOf": [
            {
              "description": "Both, which is what removal means.",
              "type": "string",
              "const": "everywhere"
            },
            {
              "description": "The media server only, and the request service still holds an account.\n\nThey can neither watch nor ask — the account left behind signs in through the one\nthat is gone — but something is there that should not be, and the next run of this\ncommand takes it.",
              "type": "string",
              "const": "media-server-only"
            },
            {
              "description": "Nothing was removed, because nothing was confirmed.",
              "type": "string",
              "const": "nothing"
            }
          ]
        }
      }
    },
    "repair": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Report"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Beyond": {
          "description": "A repair that has run out of chances, and where to go instead.",
          "type": "object",
          "properties": {
            "check": {
              "description": "The check whose fault has outlasted every attempt at it.",
              "type": "string"
            },
            "remedy": {
              "description": "What to do about it now that lemonfiber has stopped offering to.",
              "$ref": "#/$defs/Remedy"
            }
          },
          "required": [
            "check",
            "remedy"
          ]
        },
        "Mended": {
          "description": "One repair, and what became of it.",
          "type": "object",
          "properties": {
            "outcome": {
              "description": "How it turned out, once the check was asked again.",
              "$ref": "#/$defs/Outcome"
            },
            "repair": {
              "description": "What was proposed.",
              "$ref": "#/$defs/Repair"
            }
          },
          "required": [
            "repair",
            "outcome"
          ]
        },
        "Outcome": {
          "description": "How a repair turned out, once the check that raised the finding has been asked again.\n\nDeliberately not a boolean. \"It ran\" and \"it worked\" are different claims, and a model\nthat cannot tell them apart will eventually report the first as the second.",
          "oneOf": [
            {
              "description": "It ran, and the check now passes.",
              "type": "object",
              "properties": {
                "outcome": {
                  "type": "string",
                  "const": "fixed"
                }
              },
              "required": [
                "outcome"
              ]
            },
            {
              "description": "It ran, and the check still fails.",
              "type": "object",
              "properties": {
                "outcome": {
                  "type": "string",
                  "const": "fix_failed"
                }
              },
              "required": [
                "outcome"
              ]
            },
            {
              "description": "It stopped partway, leaving this.\n\nNamed precisely rather than as \"failed\": a half-applied change is a different\nstate to be in from an unchanged one, and the operator has to know which they are\nlooking at before they try anything else.",
              "type": "object",
              "properties": {
                "leaving": {
                  "description": "What the machine is now in, said plainly.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "stopped"
                }
              },
              "required": [
                "outcome",
                "leaving"
              ]
            },
            {
              "description": "Not carried out, because the operator said no.",
              "type": "object",
              "properties": {
                "outcome": {
                  "type": "string",
                  "const": "declined"
                }
              },
              "required": [
                "outcome"
              ]
            },
            {
              "description": "Refused, because it would have written over something changed by hand.",
              "type": "object",
              "properties": {
                "outcome": {
                  "type": "string",
                  "const": "would_overwrite"
                }
              },
              "required": [
                "outcome"
              ]
            }
          ]
        },
        "Remedy": {
          "description": "One thing the operator can do about a problem.",
          "type": "object",
          "properties": {
            "action": {
              "description": "The action, phrased as something to do rather than something to know.",
              "type": "string"
            },
            "detail": {
              "description": "Where to look, when that helps.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "action"
          ]
        },
        "Repair": {
          "description": "One repair lemonfiber could carry out.",
          "type": "object",
          "properties": {
            "check": {
              "description": "The check whose finding this answers, as the finding names it.",
              "type": "string"
            },
            "does": {
              "description": "What it would do, in the words the operator will read before confirming.",
              "type": "string"
            },
            "effects": {
              "description": "What else changes if it does.\n\nStated before it is confirmed and never afterwards, because an effect an operator\nlearns about after the fact is not something they agreed to. Empty where a repair\ntouches nothing but the thing it names.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "reversible": {
              "description": "Whether carrying it out is recorded well enough to be undone.\n\nA repair that cannot be reversed is still worth offering — restarting a container\nis not undoable and is usually right — but the operator confirming one deserves to\nknow which kind they are agreeing to.",
              "type": "boolean"
            }
          },
          "required": [
            "check",
            "does",
            "effects",
            "reversible"
          ]
        },
        "Report": {
          "description": "What a repairing run offered, and what it did.",
          "type": "object",
          "properties": {
            "acted": {
              "description": "Whether this run was allowed to act at all.",
              "type": "boolean"
            },
            "agreement": {
              "description": "What this offer is, so consent given for it can name which offer it read.\n\nCarried on every report rather than only on the ones that offer something: a\nsurface that has to look for it is a surface that can fail to find it, and an\noffer of nothing is still an offer somebody may agree to nothing of.",
              "type": "string"
            },
            "beyond": {
              "description": "What has been tried too often to keep offering.\n\nSaid rather than passed over. A repair that quietly stopped being offered leaves\nthe operator watching a fault nobody mentions any more, which is worse than being\ntold plainly that this is past what lemonfiber can work out.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Beyond"
              }
            },
            "mended": {
              "description": "What was carried out, in the order it was.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Mended"
              }
            },
            "offered": {
              "description": "What could be put right, whether or not it was.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Repair"
              }
            }
          },
          "required": [
            "offered",
            "agreement",
            "mended",
            "beyond",
            "acted"
          ]
        }
      }
    },
    "replacement": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/ReplaceReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "ReplaceReport": {
          "description": "What standing in place of a setup already here came to, or would come to.",
          "type": "object",
          "properties": {
            "project": {
              "description": "The project that would be stood in place of.",
              "type": [
                "string",
                "null"
              ]
            },
            "refusal": {
              "description": "Why nothing was stopped, where nothing was.",
              "type": [
                "string",
                "null"
              ]
            },
            "stance": {
              "description": "Where the act stands.",
              "$ref": "#/$defs/Stance"
            },
            "still_running": {
              "description": "The services that would not stop and are still up.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "stopped": {
              "description": "The services that were stopped.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "would_stop": {
              "description": "The services that would be stopped, by name.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "would_stop",
            "stopped",
            "still_running",
            "stance"
          ]
        },
        "Stance": {
          "description": "Where a proposed change stands once it has been read against what is in force.",
          "oneOf": [
            {
              "description": "The setting already holds what was asked for. Nothing to apply, nothing to\nconfirm, and the file is not touched — a write of the same value would move\nthe file's own timestamp and read afterwards as an edit somebody made.",
              "type": "string",
              "const": "unchanged"
            },
            {
              "description": "Staged and not applied: nothing has been written. Either it is consequential\nand nobody has said yes to it yet, or it was rehearsed.\n\nThe default, because nothing having happened yet is what every act starts as,\nand a report built and not filled in should say that rather than claim it acted.",
              "type": "string",
              "const": "pending"
            },
            {
              "description": "Turned away. It cannot be applied safely, the file is exactly as it was, and\nthe refusal says why.",
              "type": "string",
              "const": "blocked"
            },
            {
              "description": "Written.",
              "type": "string",
              "const": "applied"
            }
          ]
        }
      }
    },
    "reset": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/ResetReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "ResetReport": {
          "description": "What a full reset did, or — until it is confirmed — would do: the operator edits it\nreverts back to lemonfiber's own state, and whether it was carried out or only shown.",
          "type": "object",
          "properties": {
            "confirmed": {
              "description": "Whether the reset was carried out, or only previewed pending confirmation.",
              "type": "boolean"
            },
            "reverted": {
              "description": "The operator's edits that were reverted — or, unconfirmed, that a reset would\nrevert — each with the diff of what is lost against what lemonfiber restores.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/StackEdit"
              }
            },
            "reverted_connections": {
              "description": "The service connections whose drifted value was reverted to lemonfiber's — or,\nunconfirmed, would be — each named as it reads in a seed report.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "reverted",
            "reverted_connections",
            "confirmed"
          ]
        },
        "StackEdit": {
          "description": "A stack file the operator edited, preserved rather than overwritten, with the\nchange an upgrade would make shown against it.",
          "type": "object",
          "properties": {
            "diff": {
              "description": "The lines that differ between the operator's file and what lemonfiber would\nwrite — theirs marked `-`, lemonfiber's `+`, the matching head and tail left\nout. Empty where the two differ only in ways `lines` does not see.",
              "type": "string"
            },
            "path": {
              "description": "The file's path within the stack directory.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "diff"
          ]
        }
      }
    },
    "restore": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Restoration"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Manifest": {
          "description": "The record written inside an archive, and read back to decide a restore.\n\nEverything a restore needs to know before it overwrites anything: what made\nthe archive, when, what data root it was taken against, what it covers, whether\nit is sensitive, and the contents to list. Round-trips through JSON so the same\nvalue the capture wrote is the value the restore reads.",
          "type": "object",
          "properties": {
            "created_at": {
              "description": "When it was taken. Opaque here; the surface stamps it from the clock.",
              "type": "string"
            },
            "data_root": {
              "description": "The data root it was taken against, to notice a restore to a different one.",
              "type": "string"
            },
            "members": {
              "description": "What is inside, for a listing shown before anything is overwritten.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Member"
              }
            },
            "product_version": {
              "description": "The lemonfiber version that wrote it, checked against the one restoring.",
              "type": "string"
            },
            "schema": {
              "description": "The archive format, checked before anything inside is trusted.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "scope": {
              "description": "What it covers.",
              "$ref": "#/$defs/Scope"
            },
            "sensitive": {
              "description": "Whether it carries credentials, and so must be handled as sensitive.",
              "type": "boolean"
            }
          },
          "required": [
            "schema",
            "product_version",
            "created_at",
            "data_root",
            "scope",
            "sensitive",
            "members"
          ]
        },
        "Member": {
          "description": "One entry in an archive's contents listing.",
          "type": "object",
          "properties": {
            "archive_path": {
              "description": "Where it sits inside the archive.",
              "type": "string"
            },
            "label": {
              "description": "What it is, in the operator's terms.",
              "type": "string"
            }
          },
          "required": [
            "archive_path",
            "label"
          ]
        },
        "Preview": {
          "description": "What a restore would do, shown before anything is overwritten.",
          "type": "object",
          "properties": {
            "agreement": {
              "description": "What this listing is, so consent given for it can name which listing it read.\n\nCarried on every listing rather than only on the ones that would re-point\nsomething: a surface that has to look for it is a surface that can fail to\nfind it, and a restore that would overwrite the same configuration in place\nis still one somebody may agree to.",
              "type": "string"
            },
            "downgrade": {
              "description": "Whether the archive is old enough that a compatibility warning applies.",
              "type": "boolean"
            },
            "manifest": {
              "description": "The archive's own account of itself — its scope, version and contents.",
              "$ref": "#/$defs/Manifest"
            },
            "relocation": {
              "description": "The data-root difference, where the archive was taken against another one.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Relocation"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "manifest",
            "downgrade",
            "agreement"
          ]
        },
        "Relocation": {
          "description": "A restore whose archive was taken against a different data root than the one\nconfigured now, so its stored paths would land where nothing exists.",
          "type": "object",
          "properties": {
            "now": {
              "description": "The data root configured now.",
              "type": "string"
            },
            "was": {
              "description": "The data root the archive was taken against.",
              "type": "string"
            }
          },
          "required": [
            "was",
            "now"
          ]
        },
        "Report": {
          "description": "What a restore did.",
          "type": "object",
          "properties": {
            "from_version": {
              "description": "The lemonfiber version the archive was written by.",
              "type": "string"
            },
            "relocated": {
              "description": "The data root that was re-pointed, where the restore accepted one.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Relocation"
                },
                {
                  "type": "null"
                }
              ]
            },
            "scope": {
              "description": "What was restored.",
              "$ref": "#/$defs/Scope"
            }
          },
          "required": [
            "scope",
            "from_version"
          ]
        },
        "Restoration": {
          "description": "What a restore said: what it would overwrite, and whether it did.\n\nThe listing is present either way, and that is the point of the shape. It is not\na separate request a surface may or may not make — it is the half of a restore\nthat happens before anything is overwritten, so every answer carries it and an\nanswer that overwrote nothing is one whose `done` is absent.",
          "type": "object",
          "properties": {
            "done": {
              "description": "What was put back, or nothing where nothing was.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Report"
                },
                {
                  "type": "null"
                }
              ]
            },
            "would": {
              "description": "What the archive holds and what restoring it would come to, read before\nanything was touched.",
              "$ref": "#/$defs/Preview"
            }
          },
          "required": [
            "would"
          ]
        },
        "Scope": {
          "description": "How much of the stack a backup covers.\n\nWhole-stack is the common case, but restoring one service is often what is\nactually wanted — one \\*arr's configuration mangled while the rest is fine —\nso the scope is recorded in the archive and honoured on the way back.",
          "oneOf": [
            {
              "description": "Every service's configuration, plus lemonfiber's own and the stack.",
              "type": "object",
              "properties": {
                "scope": {
                  "type": "string",
                  "const": "whole_stack"
                }
              },
              "required": [
                "scope"
              ]
            },
            {
              "description": "One named service's configuration alone.",
              "type": "object",
              "properties": {
                "name": {
                  "description": "The service whose configuration this covers.",
                  "type": "string"
                },
                "scope": {
                  "type": "string",
                  "const": "service"
                }
              },
              "required": [
                "scope",
                "name"
              ]
            }
          ]
        }
      }
    },
    "seed": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Report"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Assessment": {
          "description": "Whether a pass could assess drift — whether it had the record of what lemonfiber\nlast wrote to compare against.",
          "oneOf": [
            {
              "description": "The expected-state record was read, or genuinely absent as on a first seed,\nso each connection was judged against it.",
              "type": "string",
              "const": "assessed"
            },
            {
              "description": "The expected-state record was there but could not be read — lost. Drift could\nnot be judged this pass, and re-baselining from the current state is offered.",
              "type": "string",
              "const": "unassessable"
            }
          ]
        },
        "Report": {
          "description": "What a seed pass amounted to.",
          "type": "object",
          "properties": {
            "assessment": {
              "description": "Whether drift could be assessed, or the expected-state record was lost.",
              "$ref": "#/$defs/Assessment"
            },
            "wirings": {
              "description": "Every connection attempted, and how each turned out.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Wiring"
              }
            }
          },
          "required": [
            "wirings",
            "assessment"
          ]
        },
        "Severity": {
          "description": "How serious a reported connection is.\n\nDrift is normal and usually the operator's own harmless edit, so it is reported\nas information rather than a failure. It escalates to a warning only when the\ndrift breaks the stack — a root folder pointing where nothing exists, a download\nclient that no longer answers — and a warning that cannot be acted on is noise,\nso a warning always names both what broke and what to do about it.",
          "oneOf": [
            {
              "description": "Nothing is broken: the connection is settled, or its drift is the operator's\nown edit that still works.",
              "type": "object",
              "properties": {
                "severity": {
                  "type": "string",
                  "const": "informational"
                }
              },
              "required": [
                "severity"
              ]
            },
            {
              "description": "The connection breaks the stack. Both the breakage and a remediation are\nnamed, because a warning the operator cannot act on is noise.",
              "type": "object",
              "properties": {
                "breakage": {
                  "description": "What is broken, in the operator's terms.",
                  "type": "string"
                },
                "remediation": {
                  "description": "What to do about it.",
                  "type": "string"
                },
                "severity": {
                  "type": "string",
                  "const": "warning"
                }
              },
              "required": [
                "severity",
                "breakage",
                "remediation"
              ]
            }
          ]
        },
        "State": {
          "description": "How one connection turned out after a seed pass.",
          "oneOf": [
            {
              "description": "Written and read back.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "wired"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "Present and correct; nothing was done.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "already-wired"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "Present but operator-changed; preserved.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "drifted"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "Present and still lemonfiber's own value, but behind lemonfiber's intent —\nit should be brought up to date. Reported until an update path applies it,\nand never overwritten in the meantime.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "stale"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "Both the service's value and lemonfiber's intent moved away from the\nbaseline. The conflict is presented — the value the operator set beside the\none lemonfiber would write — and the value left as it is; lemonfiber does not\nresolve it on its own.\n\nBoth values are shown in the report and serialized with it, so a\nsecret-bearing field must not report a conflict through this variant: a\nconflict in a secret is to be reported without either value on show, and\nwants a masked shape of its own rather than this one.",
              "type": "object",
              "properties": {
                "ours": {
                  "description": "The value lemonfiber would write in its place.",
                  "type": "string"
                },
                "state": {
                  "type": "string",
                  "const": "conflicted"
                },
                "yours": {
                  "description": "The value the service now holds, as the operator set it; `None` where\nthey cleared it.",
                  "type": [
                    "string",
                    "null"
                  ]
                }
              },
              "required": [
                "state",
                "ours"
              ]
            },
            {
              "description": "An operator's edit adopted as the accepted state, kept across seeds and\nrestores. Settled: lemonfiber leaves it as it is.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "adopted"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "A value the service already held that lemonfiber never wrote — the operator's\nown, pre-existing. Adopted as the baseline this run rather than reported as\ndrift, so an existing setup is taken on instead of flagged wholesale. Its\nvalue is not shown, so a secret among the adopted is never put on display.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "unmanaged"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "Prerequisite unavailable; a later run will complete it.",
              "type": "object",
              "properties": {
                "reason": {
                  "description": "Why it could not be attempted.",
                  "type": "string"
                },
                "state": {
                  "type": "string",
                  "const": "skipped"
                }
              },
              "required": [
                "state",
                "reason"
              ]
            },
            {
              "description": "Attempted and rejected, carrying the service's own words.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "What the service said.",
                  "type": "string"
                },
                "state": {
                  "type": "string",
                  "const": "failed"
                }
              },
              "required": [
                "state",
                "detail"
              ]
            },
            {
              "description": "Refused by lemonfiber's own policy, carrying the reason a re-run will not\nresolve — such as two \\*arrs pointed at one root folder, or a service that\ndoes not serve the API version this build speaks. Either way nothing it\nnames was written, whether it was refused before the write or the write\nitself found nothing to land in.",
              "type": "object",
              "properties": {
                "reason": {
                  "description": "Why it was refused, in lemonfiber's own words.",
                  "type": "string"
                },
                "state": {
                  "type": "string",
                  "const": "refused"
                }
              },
              "required": [
                "state",
                "reason"
              ]
            }
          ]
        },
        "Wiring": {
          "description": "One connection, and how it turned out.",
          "type": "object",
          "properties": {
            "connection": {
              "description": "What was being connected, such as `SABnzbd into Sonarr`.",
              "type": "string"
            },
            "severity": {
              "description": "How serious the outcome is — information by default, a warning where the\nconnection breaks the stack.",
              "$ref": "#/$defs/Severity"
            },
            "state": {
              "description": "How it turned out.",
              "$ref": "#/$defs/State"
            }
          },
          "required": [
            "connection",
            "state",
            "severity"
          ]
        }
      }
    },
    "self-update": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/UpdateReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Installed": {
          "description": "How this copy of lemonfiber got onto the machine.\n\nNothing known is the default. Every other answer is a claim about somebody's\nmachine, and a value that arrived by nobody filling it in has established none of\nthem.",
          "oneOf": [
            {
              "description": "Homebrew put it here, on macOS or on Linux.",
              "type": "string",
              "const": "homebrew"
            },
            {
              "description": "Scoop put it here.",
              "type": "string",
              "const": "scoop"
            },
            {
              "description": "winget put it here.",
              "type": "string",
              "const": "winget"
            },
            {
              "description": "`cargo install` built and placed it.",
              "type": "string",
              "const": "cargo"
            },
            {
              "description": "A distribution's own package manager placed it.",
              "type": "string",
              "const": "distribution"
            },
            {
              "description": "The shell installer this project publishes wrote it.",
              "type": "string",
              "const": "installer"
            },
            {
              "description": "Downloaded, unpacked or built by hand, and owned by nobody but the operator.",
              "type": "string",
              "const": "elsewhere"
            },
            {
              "description": "This machine would not say where the running binary is, so none of the above\ncan be told apart. Not a failure, and not a licence to guess.",
              "type": "string",
              "const": "untellable"
            }
          ]
        },
        "Standing": {
          "description": "Where a copy of lemonfiber stands, in the words the specification uses.\n\nNothing known is the default, and it is the right one: a report built before\nanything has been read has not established that this copy is current, and a\ndefault that said so would be a claim made by an empty value.",
          "oneOf": [
            {
              "description": "The version running is the newest known.",
              "type": "string",
              "const": "current"
            },
            {
              "description": "A newer version exists, and replacing this copy is lemonfiber's own to do.",
              "type": "string",
              "const": "update-available"
            },
            {
              "description": "A newer version exists and a package manager owns this copy, so the update\nis that tool's to make.",
              "type": "string",
              "const": "managed-externally"
            },
            {
              "description": "Availability could not be determined, which is not a fault and stops nothing.",
              "type": "string",
              "const": "check-failed"
            }
          ]
        },
        "UpdateReport": {
          "description": "Where this copy of lemonfiber stands, and what moving it would come to.",
          "type": "object",
          "properties": {
            "afterwards": {
              "description": "What updating leaves alone, and what it needs afterwards.",
              "type": "string"
            },
            "asked": {
              "description": "The version the operator asked to move to, where they asked for one.",
              "type": [
                "string",
                "null"
              ]
            },
            "at": {
              "description": "Where the running binary is, with any link followed, or nothing where this\nmachine would not say.\n\nThe answer to which of several copies on a search path is the one that ran, so\na version somebody quotes can be attributed to a file rather than to a name.",
              "type": [
                "string",
                "null"
              ]
            },
            "carries": {
              "description": "What a release brings besides the program, and when any of it is fetched.",
              "type": "string"
            },
            "command": {
              "description": "Exactly what to type, where there is something exact to type.",
              "type": [
                "string",
                "null"
              ]
            },
            "configuration": {
              "description": "Whether the version named can read the configuration on this machine.\n\nOnly where a version was named, since it is the question a downgrade asks and\nnothing else does.",
              "type": [
                "string",
                "null"
              ]
            },
            "installed": {
              "description": "How this copy got onto the machine.",
              "$ref": "#/$defs/Installed"
            },
            "instead": {
              "description": "Why there is nothing exact to type, where there is not.",
              "type": [
                "string",
                "null"
              ]
            },
            "offered": {
              "description": "The newest version released, where the check could read one.",
              "type": [
                "string",
                "null"
              ]
            },
            "owner": {
              "description": "The tool that owns this copy, where one does.",
              "type": [
                "string",
                "null"
              ]
            },
            "replaceable": {
              "description": "Whether the directory holding the running binary can be written to.\n\nNothing where it was not asked, which is every copy a package manager owns —\nreplacing one of those is that tool's business and not this one's. Asked by\ntrying rather than by reading permission bits, and reported rather than acted\non: a copy this operator cannot replace is a thing to say with the path, never\na reason to go looking for a way to become somebody else.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "running": {
              "description": "The version running now.",
              "type": "string"
            },
            "standing": {
              "description": "Which of the states this is.",
              "$ref": "#/$defs/Standing"
            },
            "untold": {
              "description": "Why availability could not be told, where it could not.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "standing",
            "running",
            "installed",
            "afterwards",
            "carries"
          ]
        }
      }
    },
    "setup": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/SetupReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Protocols": {
          "description": "Which download protocols the operator actually has accounts for.\n\nA form names both, because a form describes what it *does* rather than what\nthis operator has paid for. Narrowing happens afterwards, so a tunnel is\nnever started with credentials that were never supplied.",
          "type": "object",
          "properties": {
            "torrent": {
              "description": "A VPN and torrent client are configured.",
              "type": "boolean"
            },
            "usenet": {
              "description": "A Usenet provider is configured.",
              "type": "boolean"
            }
          },
          "required": [
            "usenet",
            "torrent"
          ]
        },
        "SetupOutcome": {
          "description": "How a setup run ended.",
          "oneOf": [
            {
              "description": "The reviewed answers were written.",
              "type": "string",
              "const": "applied"
            },
            {
              "description": "The plan was seen and not applied; nothing was written.",
              "type": "string",
              "const": "abandoned"
            },
            {
              "description": "Nothing was asked, because this machine was already set up.",
              "type": "string",
              "const": "already-set-up"
            }
          ]
        },
        "SetupReport": {
          "description": "What a setup run came to, and what it settled on.\n\n**Deliberately not the settings themselves.** Setup writes an indexer key and a\nservice password among them, and a report a script can read is a report a script\ncan log — into a file, a CI transcript, somebody's terminal history. So this says\nwhat was *decided* and never what was *entered*, and the fields are chosen one at\na time rather than by serialising a struct that might later gain a secret.\n\nThe indexer's address is left out for that reason rather than because it is\nitself a secret: it is entered beside its key, and the two travel together in\nevery place an operator copies them from.",
          "type": "object",
          "properties": {
            "data_root": {
              "description": "Where the library was put, where a location was chosen.",
              "type": [
                "string",
                "null"
              ]
            },
            "outcome": {
              "description": "How the run ended.",
              "$ref": "#/$defs/SetupOutcome"
            },
            "protocols": {
              "description": "Which ways of downloading the stack was set up for.",
              "$ref": "#/$defs/Protocols"
            },
            "service_user": {
              "description": "The user the services run as, as `uid:gid`, where one was set.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "outcome",
            "protocols"
          ]
        }
      }
    },
    "space": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Reckoning"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Candidate": {
          "description": "One completed download, and what reclaiming it would come to.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "What it occupies.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "consequence": {
              "description": "What removing it costs, where it costs anything.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "What both sides call it.",
              "type": "string"
            },
            "standing": {
              "description": "Where it stands.",
              "$ref": "#/$defs/Standing"
            }
          },
          "required": [
            "name",
            "bytes",
            "standing"
          ]
        },
        "Category": {
          "description": "What one line of the accounting is about.",
          "oneOf": [
            {
              "description": "One directory beneath the data root, named as the operator named it.\n\nPer directory rather than one figure for the library, because several\nlibraries commonly share a volume and \"the library is large\" tells nobody\nwhich of them is growing.",
              "type": "object",
              "properties": {
                "name": {
                  "type": "string"
                },
                "of": {
                  "type": "string",
                  "const": "tree"
                }
              },
              "required": [
                "of",
                "name"
              ]
            },
            {
              "description": "What the download clients still have to write.",
              "type": "object",
              "properties": {
                "of": {
                  "type": "string",
                  "const": "landing"
                }
              },
              "required": [
                "of"
              ]
            },
            {
              "description": "Completed downloads the client is still seeding.",
              "type": "object",
              "properties": {
                "of": {
                  "type": "string",
                  "const": "seeding"
                }
              },
              "required": [
                "of"
              ]
            },
            {
              "description": "Downloads on disk that no service ever took.",
              "type": "object",
              "properties": {
                "of": {
                  "type": "string",
                  "const": "orphaned"
                }
              },
              "required": [
                "of"
              ]
            },
            {
              "description": "Archives whose extracted contents sit beside them.",
              "type": "object",
              "properties": {
                "of": {
                  "type": "string",
                  "const": "extracted"
                }
              },
              "required": [
                "of"
              ]
            },
            {
              "description": "The services' own configuration and databases.",
              "type": "object",
              "properties": {
                "of": {
                  "type": "string",
                  "const": "services"
                }
              },
              "required": [
                "of"
              ]
            },
            {
              "description": "What the operator said to leave alone.",
              "type": "object",
              "properties": {
                "of": {
                  "type": "string",
                  "const": "unmanaged"
                }
              },
              "required": [
                "of"
              ]
            }
          ]
        },
        "Consumption": {
          "description": "One line of the accounting.",
          "type": "object",
          "properties": {
            "category": {
              "description": "What it is about.",
              "$ref": "#/$defs/Category"
            },
            "reclaim": {
              "description": "What getting it back would cost.",
              "$ref": "#/$defs/Reclaim"
            },
            "tally": {
              "description": "What it occupies, counted both ways.",
              "$ref": "#/$defs/Tally"
            }
          },
          "required": [
            "category",
            "tally",
            "reclaim"
          ]
        },
        "Freshness": {
          "description": "How much a reading can be relied on.",
          "oneOf": [
            {
              "description": "Read off a local disk, so it is true as of now.",
              "type": "object",
              "properties": {
                "as": {
                  "type": "string",
                  "const": "live"
                }
              },
              "required": [
                "as"
              ]
            },
            {
              "description": "Read across a network share, which answers with what it was last told —\ncarrying the moment it was taken, in seconds since the epoch, so a figure\nnobody can refresh is at least dated.",
              "type": "object",
              "properties": {
                "as": {
                  "type": "string",
                  "const": "as_of"
                },
                "at": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                }
              },
              "required": [
                "as",
                "at"
              ]
            }
          ]
        },
        "Interrupted": {
          "description": "An import that stopped part-way, in the words of whatever stopped it.",
          "type": "object",
          "properties": {
            "name": {
              "description": "What the service calls it.",
              "type": "string"
            },
            "partial": {
              "description": "What is on disk for it already, where the walk could find it.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "said": {
              "description": "What the service said, verbatim.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "partial",
            "said"
          ]
        },
        "Left": {
          "description": "Something a cleanup could not take.",
          "type": "object",
          "properties": {
            "at": {
              "description": "Where it is.",
              "type": "string"
            },
            "why": {
              "description": "What the platform said, verbatim.",
              "type": "string"
            }
          },
          "required": [
            "at",
            "why"
          ]
        },
        "Level": {
          "description": "Where a volume stands.",
          "oneOf": [
            {
              "description": "Usage cannot be determined.\n\nOrdered first so that it never wins a comparison against a level that was\nactually measured: a volume nobody could read must not make a stack that is\nvisibly full read as merely unknown.",
              "type": "string",
              "const": "unknown"
            },
            {
              "description": "Comfortable headroom.",
              "type": "string",
              "const": "ample"
            },
            {
              "description": "Below comfortable, not urgent.",
              "type": "string",
              "const": "advisory"
            },
            {
              "description": "Projected to exhaust within the horizon.",
              "type": "string",
              "const": "warning"
            },
            {
              "description": "Nearly exhausted.",
              "type": "string",
              "const": "critical"
            },
            {
              "description": "Full; acquisitions halted.",
              "type": "string",
              "const": "exhausted"
            }
          ]
        },
        "Outsized": {
          "description": "One file far larger than the rest.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "What it occupies.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "path": {
              "description": "Where it is.",
              "type": "string"
            },
            "times_typical": {
              "description": "How many times the middle file of this walk it is.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "path",
            "bytes",
            "times_typical"
          ]
        },
        "Reckoning": {
          "description": "Where the disk stands, what is on it, and what could be got back.",
          "type": "object",
          "properties": {
            "agreement": {
              "description": "What this offer names itself, so an answer to it can say which offer it was\nanswering.",
              "type": "string"
            },
            "candidates": {
              "description": "The completed downloads, each with where it stands and what removing it\nwould cost.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Candidate"
              }
            },
            "consumption": {
              "description": "Where the room went, one line per tree plus the services' own files, and\none line for what is committed but has not landed yet.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Consumption"
              }
            },
            "halted": {
              "description": "Whether new acquisitions are halted to keep the services writable.",
              "type": "boolean"
            },
            "interrupted": {
              "description": "The imports that stopped part-way, with what is on disk for each.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Interrupted"
              }
            },
            "level": {
              "description": "Where the stack stands, which is where its worst volume stands.",
              "$ref": "#/$defs/Level"
            },
            "outsized": {
              "description": "The files far enough out of line with the rest to be worth pointing at.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Outsized"
              }
            },
            "reclaimable": {
              "description": "What of that room could be got back, and what each would cost.\n\nA second reading of bytes already counted above rather than more of them: a\nseeding torrent's file is in the tree it lives in *and* here. Summing the\ntwo lists together would double what is on the disk, which is the mistake\nthis whole module is arranged to avoid.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Consumption"
              }
            },
            "reclaimed": {
              "description": "What became of a confirmed cleanup, where one was asked for.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Reclaimed"
                },
                {
                  "type": "null"
                }
              ]
            },
            "volumes": {
              "description": "The volumes watched. Either filling stops the stack, so both are reported\nwhether or not they are the same drive.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Volume"
              }
            }
          },
          "required": [
            "volumes",
            "level",
            "halted",
            "consumption",
            "reclaimable",
            "candidates",
            "outsized",
            "interrupted",
            "agreement"
          ]
        },
        "Reclaim": {
          "description": "What getting a line's room back costs.",
          "oneOf": [
            {
              "description": "Only by deciding to lose something you chose to keep.",
              "type": "string",
              "const": "by_losing_content"
            },
            {
              "description": "Nothing to reclaim: it is being written right now.",
              "type": "string",
              "const": "in_progress"
            },
            {
              "description": "Yes, and it costs standing with the trackers it came from.",
              "type": "string",
              "const": "at_the_cost_of_ratio"
            },
            {
              "description": "Yes, and it costs nothing — the usual easy win.",
              "type": "string",
              "const": "the_easy_win"
            },
            {
              "description": "Yes: the unpacked copy is the one being used.",
              "type": "string",
              "const": "already_have_it"
            },
            {
              "description": "A little, and rarely worth it.",
              "type": "string",
              "const": "marginally"
            },
            {
              "description": "No, because you said so.",
              "type": "string",
              "const": "you_said_not"
            }
          ]
        },
        "Reclaimed": {
          "description": "What became of a confirmed cleanup.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "What they occupied.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "gone": {
              "description": "The paths that were taken.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "left": {
              "description": "What could not be taken, and what the platform said about it.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Left"
              }
            }
          },
          "required": [
            "gone",
            "bytes",
            "left"
          ]
        },
        "Role": {
          "description": "Which of the two volumes a reading is about.",
          "oneOf": [
            {
              "description": "Where the media and the downloads live.",
              "type": "string",
              "const": "data"
            },
            {
              "description": "Where the services keep their own configuration and databases.",
              "type": "string",
              "const": "services"
            }
          ]
        },
        "Standing": {
          "description": "Where one completed download stands.",
          "oneOf": [
            {
              "description": "Nothing ever linked it into a library: it was never imported, and removing\nit loses nothing.",
              "type": "object",
              "properties": {
                "standing": {
                  "type": "string",
                  "const": "never_imported"
                }
              },
              "required": [
                "standing"
              ]
            },
            {
              "description": "It was imported and is still seeding, so removing it has a consequence\noutside this machine.",
              "type": "object",
              "properties": {
                "ratio": {
                  "description": "What it has uploaded against what it downloaded, in hundredths, as the\nclient reports it.\n\nA whole number rather than a fraction because every report this product\nmakes is compared for equality somewhere, and a fraction cannot be —\ntwo figures a client would call the same would not be. The hundredth is\nfiner than any decision made on a ratio.",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0
                },
                "standing": {
                  "type": "string",
                  "const": "seeding"
                }
              },
              "required": [
                "standing",
                "ratio"
              ]
            },
            {
              "description": "The operator asked for this one to be left alone.",
              "type": "object",
              "properties": {
                "standing": {
                  "type": "string",
                  "const": "left_alone"
                }
              },
              "required": [
                "standing"
              ]
            }
          ]
        },
        "Tally": {
          "description": "What a set of files occupies, counted both ways.",
          "type": "object",
          "properties": {
            "files": {
              "description": "How many names were counted.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "logical": {
              "description": "The bytes the names add up to — what this would take with nothing shared.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "physical": {
              "description": "The bytes the underlying files add up to — what the volume has actually\nlost to them.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "shared": {
              "description": "How many of those names pointed at a file already counted.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "logical",
            "physical",
            "files",
            "shared"
          ]
        },
        "Volume": {
          "description": "One volume, as one run measured it.",
          "type": "object",
          "properties": {
            "at": {
              "description": "The path that was measured.",
              "type": "string"
            },
            "committed": {
              "description": "The bytes already committed to landing here.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "free": {
              "description": "Bytes free, or nothing where the volume could not be read.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "level": {
              "description": "Where it stands.",
              "$ref": "#/$defs/Level"
            },
            "limit": {
              "description": "The effective limit — the mount's own size, which on a dataset given a\nquota is the quota rather than the device beneath it.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "point": {
              "description": "Where the volume holding it is mounted, which is what the limit belongs to.",
              "type": "string"
            },
            "projected": {
              "description": "What would be free once the committed content has landed.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "reading": {
              "description": "What the reading is worth.",
              "$ref": "#/$defs/Freshness"
            },
            "role": {
              "description": "Which of the two this is.",
              "$ref": "#/$defs/Role"
            }
          },
          "required": [
            "role",
            "at",
            "point",
            "committed",
            "level",
            "reading"
          ]
        }
      }
    },
    "start": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "type": "string"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ]
    },
    "status": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/StatusReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Condition": {
          "description": "What a whole set of services amounts to.",
          "oneOf": [
            {
              "description": "Nothing is running.",
              "type": "string",
              "const": "inactive"
            },
            {
              "description": "Running, with at least one service in a state that wants attention.",
              "type": "string",
              "const": "degraded"
            },
            {
              "description": "Some services are up and others are not.",
              "type": "string",
              "const": "partial"
            },
            {
              "description": "Everything expected is up and answering.",
              "type": "string",
              "const": "active"
            }
          ]
        },
        "Criticality": {
          "description": "How much a service's absence costs.\n\nSerialisable as well as readable, because it reaches an operator: a status\nreport that says a service is down without saying whether that matters\nleaves them to guess, and the manifest already holds the answer.",
          "oneOf": [
            {
              "description": "Its failure has consequences outside the machine.",
              "type": "string",
              "const": "critical"
            },
            {
              "description": "The form does not work without it.",
              "type": "string",
              "const": "core"
            },
            {
              "description": "The form works, badly.",
              "type": "string",
              "const": "important"
            },
            {
              "description": "Makes things better.",
              "type": "string",
              "const": "enhancing"
            },
            {
              "description": "Nice to have.",
              "type": "string",
              "const": "optional"
            }
          ]
        },
        "Service": {
          "description": "One service, as it stands.",
          "type": "object",
          "properties": {
            "criticality": {
              "description": "How much its absence costs, so a summary can weigh it.",
              "$ref": "#/$defs/Criticality"
            },
            "depends_on": {
              "description": "The services it needs before it can work, as the manifest declares them.\nCarried so a failure can be attributed to the thing underneath it rather\nthan counted as one more independent thing wrong.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "exit": {
              "description": "How it exited, where it has exited.",
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "id": {
              "description": "The service's identifier, which is also its Compose service name.",
              "type": "string"
            },
            "name": {
              "description": "What it is called in front of an operator.",
              "type": "string"
            },
            "profile": {
              "description": "The profile that declared it.",
              "type": "string"
            },
            "state": {
              "description": "What it is doing.",
              "$ref": "#/$defs/State"
            }
          },
          "required": [
            "id",
            "name",
            "profile",
            "state",
            "criticality",
            "depends_on"
          ]
        },
        "State": {
          "description": "What one service is actually doing.\n\nOrdered from worst to best, so a form's condition is the minimum across its\nservices and needs no comparison table. The declaration order is therefore\nload-bearing.",
          "oneOf": [
            {
              "description": "Exited without being asked to, and is not coming back on its own.",
              "type": "string",
              "const": "failed"
            },
            {
              "description": "Exiting and restarting repeatedly.",
              "type": "string",
              "const": "crash-looping"
            },
            {
              "description": "Running, and its own probe says it is not working.",
              "type": "string",
              "const": "unhealthy"
            },
            {
              "description": "No container exists.",
              "type": "string",
              "const": "absent"
            },
            {
              "description": "A container exists and is not running.",
              "type": "string",
              "const": "stopped"
            },
            {
              "description": "Running, and still inside its probe's start period.",
              "type": "string",
              "const": "starting"
            },
            {
              "description": "Running, and declaring no probe by which to know more.\n\nDistinct from healthy on purpose. A service that cannot be asked is not\na service that answered, and rendering the two identically is how a\ndashboard comes to be believed about something it never checked.",
              "type": "string",
              "const": "running"
            },
            {
              "description": "Running and passing its own probe.",
              "type": "string",
              "const": "healthy"
            },
            {
              "description": "The operating system owns this one, not lemonfiber.",
              "type": "string",
              "const": "host-managed"
            }
          ]
        },
        "StatusReport": {
          "description": "What each service is doing, and what that adds up to.",
          "type": "object",
          "properties": {
            "condition": {
              "description": "What the services amount to, as one word.",
              "$ref": "#/$defs/Condition"
            },
            "forms": {
              "description": "The forms asked about; empty means the whole stack was.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "services": {
              "description": "Each service, worst first.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Service"
              }
            }
          },
          "required": [
            "forms",
            "condition",
            "services"
          ]
        }
      }
    },
    "step": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Line"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Line": {
          "description": "One narrated line: a step, and what was specifically true of it.",
          "type": "object",
          "properties": {
            "detail": {
              "description": "What was specifically true — the evidence that makes the line worth reading\nrather than a spinner. Empty where there is nothing particular to say.",
              "type": "string"
            },
            "said": {
              "description": "What it is doing, in plain language.",
              "type": "string"
            },
            "step": {
              "description": "The step being narrated.",
              "$ref": "#/$defs/Step"
            }
          },
          "required": [
            "step",
            "said",
            "detail"
          ]
        },
        "Step": {
          "description": "One step of the walk, ordered from picking something to watching it play.",
          "oneOf": [
            {
              "description": "Picking something to add, and confirming it is not already here.",
              "type": "string",
              "const": "choosing"
            },
            {
              "description": "The indexers are being searched for releases.",
              "type": "string",
              "const": "searching"
            },
            {
              "description": "A release is being sent to the download client.",
              "type": "string",
              "const": "grabbing"
            },
            {
              "description": "The download is running.",
              "type": "string",
              "const": "downloading"
            },
            {
              "description": "The \\*arr is moving the finished download into the library.",
              "type": "string",
              "const": "importing"
            },
            {
              "description": "The media server is being told to look at what arrived.",
              "type": "string",
              "const": "scanning"
            },
            {
              "description": "It is in the library and playable.",
              "type": "string",
              "const": "available"
            }
          ]
        }
      }
    },
    "stop-seeding": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Letting"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Candidate": {
          "description": "One completed download, and what reclaiming it would come to.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "What it occupies.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "consequence": {
              "description": "What removing it costs, where it costs anything.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "What both sides call it.",
              "type": "string"
            },
            "standing": {
              "description": "Where it stands.",
              "$ref": "#/$defs/Standing"
            }
          },
          "required": [
            "name",
            "bytes",
            "standing"
          ]
        },
        "Gone": {
          "description": "What became of a download the client was asked to let go.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "What it occupied, as the client reported it.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "name": {
              "description": "What the client is no longer holding.",
              "type": "string"
            },
            "rehearsed": {
              "description": "Whether this was a rehearsal, which asks the client for nothing.",
              "type": "boolean"
            }
          },
          "required": [
            "name",
            "bytes",
            "rehearsed"
          ]
        },
        "Letting": {
          "description": "One completed download, what letting it go would cost, and what became of it.",
          "type": "object",
          "properties": {
            "agreement": {
              "description": "What this offer names itself, so an answer to it can say which offer it\nanswered.",
              "type": "string"
            },
            "download": {
              "description": "The download, in the same words the account names it in: where it stands, what\nit occupies, and what removing it costs.",
              "$ref": "#/$defs/Candidate"
            },
            "goes": {
              "description": "What goes with it, carried rather than left for a surface to remember.",
              "type": "string"
            },
            "gone": {
              "description": "What became of an answered offer, and nothing where the offer is all this is.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Gone"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "download",
            "goes",
            "agreement"
          ]
        },
        "Standing": {
          "description": "Where one completed download stands.",
          "oneOf": [
            {
              "description": "Nothing ever linked it into a library: it was never imported, and removing\nit loses nothing.",
              "type": "object",
              "properties": {
                "standing": {
                  "type": "string",
                  "const": "never_imported"
                }
              },
              "required": [
                "standing"
              ]
            },
            {
              "description": "It was imported and is still seeding, so removing it has a consequence\noutside this machine.",
              "type": "object",
              "properties": {
                "ratio": {
                  "description": "What it has uploaded against what it downloaded, in hundredths, as the\nclient reports it.\n\nA whole number rather than a fraction because every report this product\nmakes is compared for equality somewhere, and a fraction cannot be —\ntwo figures a client would call the same would not be. The hundredth is\nfiner than any decision made on a ratio.",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0
                },
                "standing": {
                  "type": "string",
                  "const": "seeding"
                }
              },
              "required": [
                "standing",
                "ratio"
              ]
            },
            {
              "description": "The operator asked for this one to be left alone.",
              "type": "object",
              "properties": {
                "standing": {
                  "type": "string",
                  "const": "left_alone"
                }
              },
              "required": [
                "standing"
              ]
            }
          ]
        }
      }
    },
    "stored": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Stored"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Beside": {
          "description": "Something on this machine that lemonfiber neither keeps nor removes.",
          "type": "object",
          "properties": {
            "what": {
              "description": "What it is.",
              "type": "string"
            },
            "why": {
              "description": "Whose it is, and why it is not lemonfiber's to take away.",
              "type": "string"
            }
          },
          "required": [
            "what",
            "why"
          ]
        },
        "Kept": {
          "description": "One thing lemonfiber keeps on this machine.",
          "type": "object",
          "properties": {
            "at": {
              "description": "Where it is, in full.",
              "type": "string"
            },
            "secret": {
              "description": "Whether it holds a credential, which is what decides how carefully a copy of\nit has to be treated.",
              "type": "boolean"
            },
            "what": {
              "description": "What it is, in the operator's words.",
              "type": "string"
            },
            "why": {
              "description": "Why it is kept.",
              "type": "string"
            }
          },
          "required": [
            "what",
            "at",
            "why",
            "secret"
          ]
        },
        "Left": {
          "description": "Something a removal could not take away.",
          "type": "object",
          "properties": {
            "at": {
              "description": "The path that is still there.",
              "type": "string"
            },
            "why": {
              "description": "What the machine said about it, so it can be finished by hand.",
              "type": "string"
            }
          },
          "required": [
            "at",
            "why"
          ]
        },
        "Removal": {
          "description": "Whether anything was removed on this run, and what became of it.",
          "oneOf": [
            {
              "description": "Nobody asked. This is a listing.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "not-asked"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "Asked for without the agreement it takes, so nothing was touched.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "unconfirmed"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "Carried out.",
              "type": "object",
              "properties": {
                "gone": {
                  "description": "The directories that are gone.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "left": {
                  "description": "What could not be removed, each with the reason.",
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Left"
                  }
                },
                "state": {
                  "type": "string",
                  "const": "done"
                }
              },
              "required": [
                "state",
                "gone",
                "left"
              ]
            }
          ]
        },
        "Root": {
          "description": "A directory everything lemonfiber keeps sits under.",
          "type": "object",
          "properties": {
            "at": {
              "description": "The directory itself.",
              "type": "string"
            },
            "what": {
              "description": "What lives under it, and what losing it would cost.",
              "type": "string"
            }
          },
          "required": [
            "at",
            "what"
          ]
        },
        "Stored": {
          "description": "Everything lemonfiber keeps on this machine, and what became of it.",
          "type": "object",
          "properties": {
            "beside": {
              "description": "What is on this machine that is not lemonfiber's to keep or remove.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Beside"
              }
            },
            "kept": {
              "description": "Each thing kept, configuration first and then what can be made again.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Kept"
              }
            },
            "removal": {
              "description": "Whether this run removed any of it.",
              "$ref": "#/$defs/Removal"
            },
            "roots": {
              "description": "The two directories all of it lives under.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Root"
              }
            }
          },
          "required": [
            "roots",
            "kept",
            "beside",
            "removal"
          ]
        }
      }
    },
    "stuck": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/StuckReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Stage": {
          "description": "A stage in an item's journey, ordered from \"nobody asked for it\" to \"playable\". The\ndeclaration order is the pipeline order, so one stage compares less than a later one.",
          "oneOf": [
            {
              "description": "Nobody has asked for it — no \\*arr is monitoring it.",
              "type": "string",
              "const": "not-monitored"
            },
            {
              "description": "Monitored, waiting to be searched for.",
              "type": "string",
              "const": "monitored"
            },
            {
              "description": "A search is running.",
              "type": "string",
              "const": "searching"
            },
            {
              "description": "Releases were found by an indexer.",
              "type": "string",
              "const": "found"
            },
            {
              "description": "A release was sent to the download client.",
              "type": "string",
              "const": "grabbed"
            },
            {
              "description": "The download is in progress.",
              "type": "string",
              "const": "downloading"
            },
            {
              "description": "The download finished.",
              "type": "string",
              "const": "downloaded"
            },
            {
              "description": "The \\*arr is importing it to the library.",
              "type": "string",
              "const": "importing"
            },
            {
              "description": "It was imported to the library on disk.",
              "type": "string",
              "const": "imported"
            },
            {
              "description": "It is visible and playable in the media server.",
              "type": "string",
              "const": "available"
            }
          ]
        },
        "StuckEntry": {
          "description": "One stuck item queue health found, named so it links straight to its own trace.",
          "type": "object",
          "properties": {
            "service": {
              "description": "The \\*arr whose queue is holding it.",
              "type": "string"
            },
            "stage": {
              "description": "The stage its download is stuck at.",
              "$ref": "#/$defs/Stage"
            },
            "title": {
              "description": "The item's title — the term a `trace` searches by.",
              "type": "string"
            }
          },
          "required": [
            "title",
            "service",
            "stage"
          ]
        },
        "StuckReport": {
          "description": "The items whose downloads are stuck, across the \\*arrs — the landing point for \"N\nitems stuck\" that queue health reports, each entry naming the item so the operator\ngoes straight to its per-item trace rather than to a count to investigate.",
          "type": "object",
          "properties": {
            "incomplete": {
              "description": "Whether an \\*arr's queue could not be read, so the list may be short — reported\nrather than read as \"nothing stuck\", the same honesty a trace keeps.",
              "type": "boolean"
            },
            "items": {
              "description": "The stuck items, each linkable to its trace.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/StuckEntry"
              }
            }
          },
          "required": [
            "items",
            "incomplete"
          ]
        }
      }
    },
    "trace": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/TraceReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Confidence": {
          "description": "How sure the correlation behind a trace is — a release renamed between services can\nonly be matched fuzzily, and a guess presented as fact is worse than a marked one.",
          "oneOf": [
            {
              "description": "Joined on identifiers the services agree on.",
              "type": "string",
              "const": "certain"
            },
            {
              "description": "Joined by fuzzy matching; the trace may not be the item asked for.",
              "type": "string",
              "const": "uncertain"
            }
          ]
        },
        "Coverage": {
          "description": "How much of a traced series is here, season by season — the aggregate that turns a\nsingle furthest stage into an answer about the whole. The counts are of parts someone\nasked for; what nobody asked for is reported beside them, never folded in.",
          "type": "object",
          "properties": {
            "have": {
              "description": "How many wanted parts are here, across every season.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "seasons": {
              "description": "Each season, in order.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/SeasonCoverage"
              }
            },
            "unmonitored": {
              "description": "How many parts nobody asked for, across every season.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "wanted": {
              "description": "How many parts were asked for, across every season.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "seasons",
            "have",
            "wanted",
            "unmonitored"
          ]
        },
        "Outcome": {
          "description": "A notable thing that happened to an item, as an \\*arr's history records it. Where the\nfurthest stage answers \"how far did it get?\", the sequence of outcomes answers \"what\nhas been tried?\" — a release grabbed more than once, a download that failed and was\ntried again, a file imported and later removed. Repeated failed grabs are a pattern\nworth seeing, not something a single furthest-stage reading can show.",
          "oneOf": [
            {
              "description": "A release was sent to the download client.",
              "type": "string",
              "const": "grabbed"
            },
            {
              "description": "The download client failed the release it was handed.",
              "type": "string",
              "const": "download-failed"
            },
            {
              "description": "The item was imported to the library on disk.",
              "type": "string",
              "const": "imported"
            },
            {
              "description": "The item's file was removed.",
              "type": "string",
              "const": "removed"
            }
          ]
        },
        "Part": {
          "description": "One part of a traced item — an episode of a series. A film has no parts: the item is\nthe whole, and a trace of it says all there is to say. A series does not, which is the\ngap this closes: \"the show is imported\" is true the moment one episode lands, and reads\nas done while nine are still missing.",
          "type": "object",
          "properties": {
            "number": {
              "description": "Its number within that season.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "season": {
              "description": "Which season it belongs to.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "stage": {
              "description": "How far this one part got, on the same scale as the item as a whole.",
              "$ref": "#/$defs/Stage"
            },
            "title": {
              "description": "Its title, as a person would name it.",
              "type": "string"
            }
          },
          "required": [
            "season",
            "number",
            "title",
            "stage"
          ]
        },
        "SeasonCoverage": {
          "description": "How much of one season is actually here, and what is outstanding — the season-level\nanswer, which for a series is the one an operator can act on.",
          "type": "object",
          "properties": {
            "have": {
              "description": "How many of the wanted parts are here.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "outstanding": {
              "description": "The wanted parts that are not here yet, each carrying the stage it rests at, so\none that stalled is told apart from one still downloading.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Part"
              }
            },
            "season": {
              "description": "The season number. Season zero is where a service files specials.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "unmonitored": {
              "description": "How many parts nobody asked for — unmonitored and not on disk.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "wanted": {
              "description": "How many parts were asked for, or are already here — the denominator. Parts\nnobody asked for are counted separately rather than inflating this, so a season\nwith every wanted episode present reads as complete even where specials are not.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "season",
            "have",
            "wanted",
            "unmonitored",
            "outstanding"
          ]
        },
        "Stage": {
          "description": "A stage in an item's journey, ordered from \"nobody asked for it\" to \"playable\". The\ndeclaration order is the pipeline order, so one stage compares less than a later one.",
          "oneOf": [
            {
              "description": "Nobody has asked for it — no \\*arr is monitoring it.",
              "type": "string",
              "const": "not-monitored"
            },
            {
              "description": "Monitored, waiting to be searched for.",
              "type": "string",
              "const": "monitored"
            },
            {
              "description": "A search is running.",
              "type": "string",
              "const": "searching"
            },
            {
              "description": "Releases were found by an indexer.",
              "type": "string",
              "const": "found"
            },
            {
              "description": "A release was sent to the download client.",
              "type": "string",
              "const": "grabbed"
            },
            {
              "description": "The download is in progress.",
              "type": "string",
              "const": "downloading"
            },
            {
              "description": "The download finished.",
              "type": "string",
              "const": "downloaded"
            },
            {
              "description": "The \\*arr is importing it to the library.",
              "type": "string",
              "const": "importing"
            },
            {
              "description": "It was imported to the library on disk.",
              "type": "string",
              "const": "imported"
            },
            {
              "description": "It is visible and playable in the media server.",
              "type": "string",
              "const": "available"
            }
          ]
        },
        "TraceMoment": {
          "description": "One moment in a traced item's history: what happened and when. Where [`TraceStage`]\nis the linear progress, this is the log an \\*arr kept — the grabs, the failed\ndownloads, the import and any later removal — so a repeated attempt is seen as the\npattern it is rather than flattened to a single furthest stage.",
          "type": "object",
          "properties": {
            "at": {
              "description": "When the service reported it.",
              "type": "string"
            },
            "outcome": {
              "description": "What happened.",
              "$ref": "#/$defs/Outcome"
            }
          },
          "required": [
            "outcome",
            "at"
          ]
        },
        "TraceReport": {
          "description": "Where one item is in the pipeline: how far it got, why it stopped if it did, and the\nstages it passed through — the answer to \"where is my show?\".",
          "type": "object",
          "properties": {
            "confidence": {
              "description": "How sure the trace is of the item it followed.",
              "$ref": "#/$defs/Confidence"
            },
            "coverage": {
              "description": "How much of the item is actually here, season by season — present for an item\nmade of parts, absent for a film, which is the whole item and has none.\n\nThe furthest stage alone cannot answer this: a series is \"imported\" the moment one\nepisode lands, which reads as done while the rest are missing.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Coverage"
                },
                {
                  "type": "null"
                }
              ]
            },
            "findings": {
              "description": "Disagreements between the services about this item, each in plain language — a\nmedia server holding what no service is monitoring, and the like. Orthogonal to\nthe linear pipeline: not where the item got to, but where two services' views of\nit contradict, surfaced rather than silently reconciled.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "furthest": {
              "description": "The furthest stage the item reached.",
              "$ref": "#/$defs/Stage"
            },
            "history": {
              "description": "The notable events in its history, oldest first — the grabs, failed downloads,\nimports and removals. Repeated attempts show here as the pattern they are, which\nthe single furthest stage cannot.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/TraceMoment"
              }
            },
            "item": {
              "description": "The term the item was searched for by.",
              "type": "string"
            },
            "matched": {
              "description": "Whether a monitored item matched the term at all — a false here is itself the\nanswer: nobody asked for it.",
              "type": "boolean"
            },
            "stages": {
              "description": "The stages it passed through, in order.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/TraceStage"
              }
            },
            "stall": {
              "description": "Why it stopped, where it plainly has — or absent where it is progressing or done.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "item",
            "matched",
            "furthest",
            "stages",
            "history",
            "confidence",
            "findings"
          ]
        },
        "TraceStage": {
          "description": "One stage a traced item reached, named as the operator would read it: the stage,\nthe service that recorded it, and when.",
          "type": "object",
          "properties": {
            "at": {
              "description": "When it happened, as the service reported it — absent for a stage inferred\nrather than timed, such as being monitored.",
              "type": [
                "string",
                "null"
              ]
            },
            "service": {
              "description": "The service that recorded it.",
              "type": "string"
            },
            "stage": {
              "description": "The stage reached.",
              "$ref": "#/$defs/Stage"
            }
          },
          "required": [
            "stage",
            "service"
          ]
        }
      }
    },
    "undo": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Reversal"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Action": {
          "description": "What an undo does.\n\nTagged by what it does rather than by the field it sits in, so a reader parsing\none branches on a word rather than on which keys are present.",
          "oneOf": [
            {
              "description": "Remove the resource that was created.",
              "type": "object",
              "properties": {
                "does": {
                  "type": "string",
                  "const": "remove"
                },
                "id": {
                  "description": "The identifier to remove.",
                  "type": "string"
                },
                "resource": {
                  "description": "The kind of resource.",
                  "type": "string"
                }
              },
              "required": [
                "does",
                "resource",
                "id"
              ]
            },
            {
              "description": "Restore a value, or remove it where there was none before (`None`).",
              "type": "object",
              "properties": {
                "does": {
                  "type": "string",
                  "const": "restore"
                },
                "key": {
                  "description": "The setting to restore.",
                  "type": "string"
                },
                "value": {
                  "description": "What to restore it to, or `None` to remove it.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "wrote": {
                  "description": "What lemonfiber put there, which has to still be there for putting the\nold value back to be putting anything back.\n\nCarried so that a reversal can ask whether it is undoing its own work.\nWithout it a reversal knows only what it would like the setting to say,\nand a setting the operator has since chosen for themselves reads exactly\nlike one nobody has touched.",
                  "type": "string"
                }
              },
              "required": [
                "does",
                "key",
                "wrote"
              ]
            },
            {
              "description": "Remove a path that was created.",
              "type": "object",
              "properties": {
                "does": {
                  "type": "string",
                  "const": "delete"
                },
                "path": {
                  "description": "The path to remove.",
                  "type": "string"
                }
              },
              "required": [
                "does",
                "path"
              ]
            },
            {
              "description": "Put one field of a service's own resource back to what it held.\n\nThe only reversal that needs the service itself: the value lives inside it,\nand nothing on the host can write it. A reversal that cannot reach the\nservice says so rather than reporting the field restored.",
              "type": "object",
              "properties": {
                "does": {
                  "type": "string",
                  "const": "reconfigure"
                },
                "field": {
                  "description": "The field to put back.",
                  "type": "string"
                },
                "id": {
                  "description": "The identifier to change.",
                  "type": "string"
                },
                "resource": {
                  "description": "The kind of resource.",
                  "type": "string"
                },
                "value": {
                  "description": "What to put back, or `None` where it held nothing.",
                  "type": [
                    "string",
                    "null"
                  ]
                }
              },
              "required": [
                "does",
                "resource",
                "id",
                "field"
              ]
            }
          ]
        },
        "Reversal": {
          "description": "What putting back the last repair came to.\n\nA report rather than a bare list, because it is what an envelope carries and an\nenvelope carries a document. The list is the whole of it: what went back, and\nagainst what.",
          "type": "object",
          "properties": {
            "reversed": {
              "description": "What was put back, in the order it was.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Undo"
              }
            }
          },
          "required": [
            "reversed"
          ]
        },
        "Undo": {
          "description": "A single reversal, for the surface to carry out.",
          "type": "object",
          "properties": {
            "action": {
              "description": "What reversing it does.",
              "$ref": "#/$defs/Action"
            },
            "target": {
              "description": "The service or file to reverse it against.",
              "type": "string"
            }
          },
          "required": [
            "target",
            "action"
          ]
        }
      }
    },
    "uninstall": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Uninstall"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Coming": {
          "description": "One download still coming down when the removal was asked for.",
          "type": "object",
          "properties": {
            "name": {
              "description": "What it is, as the client names it.",
              "type": "string"
            },
            "progress": {
              "description": "How far along, from zero to a hundred.",
              "type": "integer",
              "format": "uint8",
              "maximum": 255,
              "minimum": 0
            }
          },
          "required": [
            "name",
            "progress"
          ]
        },
        "Confidence": {
          "description": "How much of a manifest was read and how much stood in for what could not be.",
          "type": "object",
          "properties": {
            "complete": {
              "description": "Whether every source this tier needed answered.",
              "type": "boolean"
            },
            "unread": {
              "description": "What could not be read, each in the words of whatever refused.\n\nThe point of the field: a manifest that is short says so and says why, rather\nthan reading as a machine with less on it than it has.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "complete",
            "unread"
          ]
        },
        "Foreign": {
          "description": "Something beneath the data location that the stack did not put there.",
          "type": "object",
          "properties": {
            "at": {
              "description": "The directory it is in, relative to the data location — or the file itself,\nwhere it sits directly in the data location.",
              "type": "string"
            },
            "bytes": {
              "description": "What they occupy.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "files": {
              "description": "How many files were found under it.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "at",
            "files",
            "bytes"
          ]
        },
        "Item": {
          "description": "One thing a removal reaches, said to be going or said to be kept.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "What it occupies, where that is knowable. Absent for a container or a network,\nwhose room is the image's rather than their own.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "kept": {
              "description": "Why it is being kept rather than removed, where it is being kept.\n\n`None` is the ordinary case: this line is going. A reason here is the whole of\nhow an image shared with another project, or a path this run could not\nconfirm, stays on the list without being taken.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "What it is called — a container name, an image reference, or a full path.",
              "type": "string"
            },
            "secret": {
              "description": "Whether it holds a credential, so a report can say what destroying it destroys.",
              "type": "boolean"
            },
            "sort": {
              "description": "Which of the four sorts of thing it is.",
              "$ref": "#/$defs/Sort"
            },
            "what": {
              "description": "What it is, in the operator's words.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "sort",
            "what",
            "secret"
          ]
        },
        "Left": {
          "description": "Something a removal could not take.",
          "type": "object",
          "properties": {
            "by_hand": {
              "description": "How to finish it by hand.",
              "type": "string"
            },
            "name": {
              "description": "What is still there.",
              "type": "string"
            },
            "why": {
              "description": "What the machine said about it, verbatim.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "why",
            "by_hand"
          ]
        },
        "Manifest": {
          "description": "What removing would come to, shown before anything is removed.",
          "type": "object",
          "properties": {
            "agreement": {
              "description": "What this reading names itself, so an answer says which reading it answered.",
              "type": "string"
            },
            "backup": {
              "description": "Whether a backup was offered before configuration is destroyed, and how.",
              "type": [
                "string",
                "null"
              ]
            },
            "bytes": {
              "description": "What the lines that are going occupy, where that is knowable.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "coming": {
              "description": "What is still coming down, which stopping would interrupt.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Coming"
              }
            },
            "confidence": {
              "description": "How much of this was read, and what could not be.",
              "$ref": "#/$defs/Confidence"
            },
            "foreign": {
              "description": "What is beneath the data location that the stack did not put there.\n\nNot a warning. While this is non-empty the data location is never removed as\none tree, and only the stack's own directories beneath it are offered.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Foreign"
              }
            },
            "items": {
              "description": "Every line it reaches, each said to be going or said to be kept.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Item"
              }
            },
            "keeps": {
              "description": "What it leaves alone, in the operator's words.",
              "type": "string"
            },
            "outside": {
              "description": "What lemonfiber cannot remove, each with how to remove it by hand.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Outside"
              }
            },
            "removes": {
              "description": "What it takes, in the operator's words.",
              "type": "string"
            },
            "tier": {
              "description": "Which removal this is.",
              "$ref": "#/$defs/Tier"
            },
            "volume": {
              "description": "Whether the data location is on a network share or a drive that unplugs.\n\nSaid where it is, so removing across a mount an operator forgot was a mount is\nsomething they read before agreeing rather than after.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "tier",
            "removes",
            "keeps",
            "items",
            "bytes",
            "foreign",
            "coming",
            "outside",
            "confidence",
            "agreement"
          ]
        },
        "Outside": {
          "description": "Something an uninstall leaves behind, and how to remove it by hand.",
          "type": "object",
          "properties": {
            "by_hand": {
              "description": "How to remove it on this platform, as the operator would type or do it.",
              "type": "string"
            },
            "found": {
              "description": "Whether this machine was found to have it.\n\nA survey that could not look says nothing was found rather than that nothing\nis there, which is why the entry is listed either way and this field carries\nthe difference.",
              "type": "boolean"
            },
            "what": {
              "description": "What it is.",
              "type": "string"
            },
            "why": {
              "description": "Why it is not lemonfiber's to take away.",
              "type": "string"
            }
          },
          "required": [
            "what",
            "why",
            "by_hand",
            "found"
          ]
        },
        "Removal": {
          "description": "Whether anything was removed on this run, and what became of it.\n\nFour states rather than the five a removal passes through. `removing` is the\ninterval between the last two and is said through the narrator as it happens — a\nvalue returned at the end cannot be the state a run is in while it runs, and a\nvariant nothing could ever answer with would be a state that is documentation\npretending to be a value.",
          "oneOf": [
            {
              "description": "Everything is enumerated with its size, and nothing has been removed.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "surveyed"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "The tier and the manifest were agreed to, and this run changes nothing — the\nstate a rehearsal ends in.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "confirmed"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "Everything the manifest named as going is gone.",
              "type": "object",
              "properties": {
                "credentials": {
                  "description": "The credentials this destroyed, said rather than left to be inferred.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "gone": {
                  "description": "What went, by the name the manifest gave it.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "state": {
                  "type": "string",
                  "const": "complete"
                }
              },
              "required": [
                "state",
                "gone",
                "credentials"
              ]
            },
            {
              "description": "Some of it could not be removed, and each of those is named with how to\nfinish it by hand.",
              "type": "object",
              "properties": {
                "credentials": {
                  "description": "The credentials this destroyed.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "gone": {
                  "description": "What went, by the name the manifest gave it.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "left": {
                  "description": "What is still there, and how to remove it.",
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Left"
                  }
                },
                "state": {
                  "type": "string",
                  "const": "partial"
                }
              },
              "required": [
                "state",
                "gone",
                "credentials",
                "left"
              ]
            }
          ]
        },
        "Sort": {
          "description": "What sort of thing one line of a manifest is.",
          "oneOf": [
            {
              "description": "A container the engine is holding.",
              "type": "string",
              "const": "container"
            },
            {
              "description": "A network the containers were on.",
              "type": "string",
              "const": "network"
            },
            {
              "description": "An image that was pulled.",
              "type": "string",
              "const": "image"
            },
            {
              "description": "A directory or file on this machine.",
              "type": "string",
              "const": "path"
            }
          ]
        },
        "Tier": {
          "description": "Which of the four removals was asked for.",
          "oneOf": [
            {
              "description": "Nothing removed. The services are stopped and everything stays where it is.",
              "type": "string",
              "const": "stop"
            },
            {
              "description": "The containers, the networks between them, and the images that were pulled.",
              "type": "string",
              "const": "services"
            },
            {
              "description": "Each service's own configuration, lemonfiber's own state, and the credentials\nin both.",
              "type": "string",
              "const": "configuration"
            },
            {
              "description": "The library and the downloads.",
              "type": "string",
              "const": "media"
            }
          ]
        },
        "Uninstall": {
          "description": "A removal, before or after it happened.",
          "type": "object",
          "properties": {
            "manifest": {
              "description": "What removing would come to, or what it came to.",
              "$ref": "#/$defs/Manifest"
            },
            "removal": {
              "description": "Whether anything was removed on this run.",
              "$ref": "#/$defs/Removal"
            }
          },
          "required": [
            "manifest",
            "removal"
          ]
        }
      }
    },
    "update": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Report"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Applied": {
          "description": "What one service's update came to.",
          "type": "object",
          "properties": {
            "detail": {
              "description": "What went wrong, where anything did.",
              "type": [
                "string",
                "null"
              ]
            },
            "ending": {
              "description": "How it ended.",
              "$ref": "#/$defs/Ending"
            },
            "from": {
              "description": "The version it was standing on before the run.",
              "type": "string"
            },
            "reversal": {
              "description": "How it could be put back, given how it ended.",
              "$ref": "#/$defs/Reversal"
            },
            "service": {
              "description": "The service it is about.",
              "type": "string"
            },
            "to": {
              "description": "The version the run was moving it to.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "from",
            "to",
            "ending",
            "reversal"
          ]
        },
        "Change": {
          "description": "What updating one service would change.",
          "type": "object",
          "properties": {
            "because": {
              "description": "What the step means, in the words an operator decides on.",
              "type": "string"
            },
            "current": {
              "description": "The version it is standing on now.",
              "type": "string"
            },
            "irreversible": {
              "description": "Whether taking it is a step nothing walks back.",
              "type": "boolean"
            },
            "jump": {
              "description": "How large the step between them is.",
              "$ref": "#/$defs/Jump"
            },
            "refused": {
              "description": "Whether lemonfiber refuses to take it at all.",
              "type": "boolean"
            },
            "service": {
              "description": "The service, by its manifest id, which is also its Compose service name.",
              "type": "string"
            },
            "target": {
              "description": "The version this build pins for it.",
              "type": "string"
            }
          },
          "required": [
            "service",
            "current",
            "target",
            "jump",
            "irreversible",
            "refused",
            "because"
          ]
        },
        "Ending": {
          "description": "How one service's update ended.",
          "oneOf": [
            {
              "description": "Updated, and answering again afterwards.",
              "type": "string",
              "const": "updated"
            },
            {
              "description": "The newer image was never fetched, so nothing about this service changed.",
              "type": "string",
              "const": "not-fetched"
            },
            {
              "description": "It was started on the newer image and did not come back.",
              "type": "string",
              "const": "not-started"
            },
            {
              "description": "The run halted before reaching it, so it is standing where it was.",
              "type": "string",
              "const": "not-reached"
            }
          ]
        },
        "Jump": {
          "description": "How large a step from one version to another is.\n\nNamed after the part of the version that moved rather than after a size, because\nthat is the fact an operator weighs: a first-number change is where a project puts\nthe work that breaks configurations, and the two behind it are where it puts the\nwork that does not.",
          "oneOf": [
            {
              "description": "The first number moved.",
              "type": "string",
              "const": "major"
            },
            {
              "description": "The second number moved, and the first did not.",
              "type": "string",
              "const": "minor"
            },
            {
              "description": "Nothing before the third number moved.",
              "type": "string",
              "const": "patch"
            },
            {
              "description": "One of the tags is not a run of numbers, so the size of the step is unknown.",
              "type": "string",
              "const": "untellable"
            }
          ]
        },
        "Report": {
          "description": "What updating the stack would change, or what a run of it came to.",
          "type": "object",
          "properties": {
            "applied": {
              "description": "What became of each service the run reached, in the order it reached them.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Applied"
              }
            },
            "backup": {
              "description": "Where the backup taken before anything moved was written.",
              "type": [
                "string",
                "null"
              ]
            },
            "changes": {
              "description": "What would move, and what taking each step means.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Change"
              }
            },
            "confirmed": {
              "description": "Whether the steps were agreed to, or only shown.",
              "type": "boolean"
            },
            "halted": {
              "description": "Why the run stopped where it did, where it stopped early.",
              "type": [
                "string",
                "null"
              ]
            },
            "in_flight": {
              "description": "What the download clients are still working on, named so an operator can\ntell whether the thing they have been waiting for is among them.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "stack_edits": {
              "description": "Stack files the operator had edited, left as they set them rather than\noverwritten with this build's own, each with the change that was held back.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/StackEdit"
              }
            },
            "state": {
              "description": "The one word the run comes to.",
              "$ref": "#/$defs/State"
            }
          },
          "required": [
            "state",
            "changes",
            "in_flight",
            "confirmed",
            "stack_edits",
            "applied"
          ]
        },
        "Reversal": {
          "description": "How a service could be put back the way it was.\n\nThe distinction is the whole of why this is reported rather than left to be\nworked out: pinning the previous image again is a minute's work, and restoring a\nbackup is an evening. Offering the first where only the second can succeed is\nworse than offering nothing, because it is acted on.",
          "oneOf": [
            {
              "description": "Nothing opened its state on the newer image, so the previous one runs again\nexactly as it did.",
              "type": "string",
              "const": "rollback"
            },
            {
              "description": "The newer image was started, so its state may already have been migrated and\nthe backup is the only way back.",
              "type": "string",
              "const": "restore"
            }
          ]
        },
        "StackEdit": {
          "description": "A stack file the operator edited, preserved rather than overwritten, with the\nchange an upgrade would make shown against it.",
          "type": "object",
          "properties": {
            "diff": {
              "description": "The lines that differ between the operator's file and what lemonfiber would\nwrite — theirs marked `-`, lemonfiber's `+`, the matching head and tail left\nout. Empty where the two differ only in ways `lines` does not see.",
              "type": "string"
            },
            "path": {
              "description": "The file's path within the stack directory.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "diff"
          ]
        },
        "State": {
          "description": "Where the stack stands against the versions this build pins.\n\nExactly one of these is true of a run at a time. A surface that had to say\n\"updates available, and also partly applied\" would be reporting the question\nrather than the answer.",
          "oneOf": [
            {
              "description": "Every service is standing on the version pinned for it.",
              "type": "string",
              "const": "current"
            },
            {
              "description": "Newer versions are pinned than what is running, and nothing has been applied.",
              "type": "string",
              "const": "updates-available"
            },
            {
              "description": "Everything that had an update took it.",
              "type": "string",
              "const": "updated"
            },
            {
              "description": "Some were updated and one was not; the run stopped there.",
              "type": "string",
              "const": "partial"
            },
            {
              "description": "Nothing was updated, and the run stopped where it did.",
              "type": "string",
              "const": "failed"
            }
          ]
        }
      }
    },
    "upgrade": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/UpgradeReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Triggered": {
          "description": "What became of asking one service to re-search its existing content.",
          "oneOf": [
            {
              "description": "The re-search was accepted and now runs in the service's background.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "started"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "The service had not finished starting — no key yet — so nothing was asked of\nit; running the upgrade again once it is up will reach it.",
              "type": "object",
              "properties": {
                "state": {
                  "type": "string",
                  "const": "not-started"
                }
              },
              "required": [
                "state"
              ]
            },
            {
              "description": "The service refused the command or could not be reached.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "The service's own account of why.",
                  "type": "string"
                },
                "state": {
                  "type": "string",
                  "const": "failed"
                }
              },
              "required": [
                "state",
                "detail"
              ]
            }
          ]
        },
        "UpgradeMedia": {
          "description": "One media type an upgrade covers: its chosen quality, that quality's cost, and —\nonce confirmed — what became of asking its service to re-search.\n\nReported per media type rather than as one figure, because each type carries its\nown preset and so its own cost: film at maximum and television at space-saving are\nupgraded to different bars, and a single number would misstate one of them.",
          "type": "object",
          "properties": {
            "media_type": {
              "description": "The media type — `tv` or `movies`.",
              "type": "string"
            },
            "outcome": {
              "description": "What became of the re-search, or `None` where the upgrade was not confirmed\nand only the cost was stated.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Triggered"
                },
                {
                  "type": "null"
                }
              ]
            },
            "preset": {
              "description": "The preset in force for it.",
              "type": "string"
            },
            "size_per_hour": {
              "description": "Roughly what an hour of it costs at that preset.",
              "type": "string"
            }
          },
          "required": [
            "media_type",
            "preset",
            "size_per_hour"
          ]
        },
        "UpgradeReport": {
          "description": "What upgrading existing content did, or — unconfirmed — would do.\n\nUpgrading re-acquires the existing library at the chosen quality, which is a\nlarge, bandwidth-expensive operation, so it is a separate explicit action whose\ncost is stated before it runs and which does nothing until confirmed. Each *arr\nre-searches against its own current cutoff, so the report speaks per media type\nrather than asserting one preset across the library.",
          "type": "object",
          "properties": {
            "confirmed": {
              "description": "Whether the operator confirmed; without it nothing was triggered, only the\ncost stated.",
              "type": "boolean"
            },
            "media": {
              "description": "Per media type: its preset, that preset's cost, and — confirmed — the outcome.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/UpgradeMedia"
              }
            }
          },
          "required": [
            "confirmed",
            "media"
          ]
        }
      }
    },
    "version": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/VersionReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "VersionReport": {
          "description": "What versions are in play: the binary, and the stack it can operate.",
          "type": "object",
          "properties": {
            "binary": {
              "description": "The running binary's version.",
              "type": "string"
            },
            "compose": {
              "description": "What the container engine reports, when it could be asked.",
              "type": [
                "string",
                "null"
              ]
            },
            "stack": {
              "description": "The version of the stack this build operates.",
              "type": "string"
            },
            "supported_schema": {
              "description": "The manifest schema generations this build reads.",
              "type": "array",
              "items": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0
              }
            }
          },
          "required": [
            "binary",
            "supported_schema",
            "stack"
          ]
        }
      }
    },
    "walkthrough": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/WalkthroughReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Handover": {
          "description": "Where a finished walkthrough leaves the operator.",
          "type": "object",
          "properties": {
            "next": {
              "description": "What to do next, in order.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Next"
              }
            }
          },
          "required": [
            "next"
          ]
        },
        "Line": {
          "description": "One narrated line: a step, and what was specifically true of it.",
          "type": "object",
          "properties": {
            "detail": {
              "description": "What was specifically true — the evidence that makes the line worth reading\nrather than a spinner. Empty where there is nothing particular to say.",
              "type": "string"
            },
            "said": {
              "description": "What it is doing, in plain language.",
              "type": "string"
            },
            "step": {
              "description": "The step being narrated.",
              "$ref": "#/$defs/Step"
            }
          },
          "required": [
            "step",
            "said",
            "detail"
          ]
        },
        "Link": {
          "description": "What the import did with the finished download — the difference between one copy of a\nfile and two.",
          "oneOf": [
            {
              "description": "The library entry and the download are the same file under two names, which is\nwhat a correctly-shared volume allows and what costs no extra disk.",
              "type": "string",
              "const": "hardlinked"
            },
            {
              "description": "The file was copied, so it now exists twice. Works, but every import costs its own\nsize again and seeding a torrent holds the second copy for as long as it seeds.",
              "type": "string",
              "const": "copied"
            }
          ]
        },
        "Next": {
          "description": "One thing to do next.",
          "oneOf": [
            {
              "description": "Add more content, now that the shape of it is understood.",
              "type": "string",
              "const": "more-content"
            },
            {
              "description": "Let the rest of the household ask for things themselves.",
              "type": "string",
              "const": "household"
            },
            {
              "description": "Watch it somewhere other than the machine it is running on.",
              "type": "string",
              "const": "client-apps"
            }
          ]
        },
        "Reason": {
          "description": "Why a walkthrough could not go on.",
          "oneOf": [
            {
              "description": "No indexer is configured, so there is nothing to search. Not a failure of the\nwalk — a reason not to offer it at all.",
              "type": "string",
              "const": "no-indexers"
            },
            {
              "description": "The search could not be run: the indexers, or the service holding them, would not\nanswer.",
              "type": "string",
              "const": "indexers-failed"
            },
            {
              "description": "The search ran cleanly and matched nothing. An entirely different problem from\nthe one above, and the one operators most often mistake for it.",
              "type": "string",
              "const": "nothing-matched"
            },
            {
              "description": "Releases exist, but none meets the chosen quality preset. The stack is working;\nthe preset and what is out there disagree.",
              "type": "string",
              "const": "none-met-the-preset"
            },
            {
              "description": "Torrents are in play and the tunnel is not verified up. Nothing is grabbed.",
              "type": "string",
              "const": "tunnel-down"
            },
            {
              "description": "The release was never handed to a download client.",
              "type": "string",
              "const": "not-grabbed"
            },
            {
              "description": "The download stopped making progress.",
              "type": "string",
              "const": "stalled"
            },
            {
              "description": "The download finished and the library manager would not take it.",
              "type": "string",
              "const": "import-failed"
            },
            {
              "description": "There is no media server in the running form to make it playable.",
              "type": "string",
              "const": "no-media-server"
            },
            {
              "description": "It was imported, the media server was told, and it still cannot be found there.",
              "type": "string",
              "const": "not-visible"
            }
          ]
        },
        "Shape": {
          "description": "Which walkthrough this stack is offered.",
          "oneOf": [
            {
              "description": "The full walk: search, grab, download, import, and see it in the library.",
              "type": "string",
              "const": "pipeline"
            },
            {
              "description": "The walk for a stack that acquires nothing: point at media already on disk and\nconfirm the media server can see it.",
              "type": "string",
              "const": "library-only"
            }
          ]
        },
        "State": {
          "description": "What has become of a walkthrough.",
          "oneOf": [
            {
              "description": "Presented at the end of setup, not yet answered.",
              "type": "string",
              "const": "offered"
            },
            {
              "description": "Declined. Available later, and carrying no penalty — a stack that was set up is\nset up whether or not anyone watched it fetch something.",
              "type": "string",
              "const": "skipped"
            },
            {
              "description": "Looking for releases.",
              "type": "string",
              "const": "searching"
            },
            {
              "description": "Sending a release to the download client.",
              "type": "string",
              "const": "grabbing"
            },
            {
              "description": "The download is running.",
              "type": "string",
              "const": "downloading"
            },
            {
              "description": "Moving the finished download into the library.",
              "type": "string",
              "const": "importing"
            },
            {
              "description": "It is in the library and playable.",
              "type": "string",
              "const": "complete"
            },
            {
              "description": "Stopped at a named step, with a diagnosis.",
              "type": "string",
              "const": "failed"
            },
            {
              "description": "The operator left part-way. Whatever was in flight stays in flight.",
              "type": "string",
              "const": "abandoned"
            }
          ]
        },
        "Step": {
          "description": "One step of the walk, ordered from picking something to watching it play.",
          "oneOf": [
            {
              "description": "Picking something to add, and confirming it is not already here.",
              "type": "string",
              "const": "choosing"
            },
            {
              "description": "The indexers are being searched for releases.",
              "type": "string",
              "const": "searching"
            },
            {
              "description": "A release is being sent to the download client.",
              "type": "string",
              "const": "grabbing"
            },
            {
              "description": "The download is running.",
              "type": "string",
              "const": "downloading"
            },
            {
              "description": "The \\*arr is moving the finished download into the library.",
              "type": "string",
              "const": "importing"
            },
            {
              "description": "The media server is being told to look at what arrived.",
              "type": "string",
              "const": "scanning"
            },
            {
              "description": "It is in the library and playable.",
              "type": "string",
              "const": "available"
            }
          ]
        },
        "Stopped": {
          "description": "A walkthrough that stopped: where, why, what the services were saying, and what to do.",
          "type": "object",
          "properties": {
            "logs": {
              "description": "What the services involved were saying at the time, shown inline rather than left\nfor the operator to go and find — a fault report they have to research is a fault\nreport they abandon.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "reason": {
              "description": "Why.",
              "$ref": "#/$defs/Reason"
            },
            "remedy": {
              "description": "The one thing to try.",
              "type": "string"
            },
            "step": {
              "description": "The step it stopped at.",
              "$ref": "#/$defs/Step"
            }
          },
          "required": [
            "step",
            "reason",
            "logs",
            "remedy"
          ]
        },
        "WalkthroughReport": {
          "description": "What a first-content walkthrough did — the whole of it, narrated line by line as it\nhappened and gathered here so the ending can be rendered, serialised and exited on.",
          "type": "object",
          "properties": {
            "already_here": {
              "description": "Whether what was asked for was already here, and so was not acquired again.",
              "type": "boolean"
            },
            "handover": {
              "description": "Where it leaves the operator, where it worked.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Handover"
                },
                {
                  "type": "null"
                }
              ]
            },
            "in_background": {
              "description": "Whether the download was handed to the background rather than waited out.",
              "type": "boolean"
            },
            "item": {
              "description": "What it walked, where it got as far as choosing something.",
              "type": [
                "string",
                "null"
              ]
            },
            "lines": {
              "description": "Every line it said, in order — the same lines the operator watched arrive, kept so\na machine-readable run is not a silent one.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Line"
              }
            },
            "link": {
              "description": "What the import did with the file, where it got that far.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Link"
                },
                {
                  "type": "null"
                }
              ]
            },
            "proves": {
              "description": "What it set out to prove, said so the operator knows what they watched.",
              "type": "string"
            },
            "shape": {
              "description": "Which walk this was.",
              "$ref": "#/$defs/Shape"
            },
            "state": {
              "description": "Where it ended up.",
              "$ref": "#/$defs/State"
            },
            "stopped": {
              "description": "Where and why it stopped, where it did.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Stopped"
                },
                {
                  "type": "null"
                }
              ]
            },
            "suggestions": {
              "description": "What could have been walked instead, where nothing was chosen — the safe first\nattempts, so an operator with an empty library is not left guessing.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "shape",
            "state",
            "proves",
            "lines",
            "suggestions",
            "in_background",
            "already_here"
          ]
        }
      }
    },
    "watch": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/SupervisionReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "SupervisionReport": {
          "description": "What a watch saw, once the data root it was guarding was lost.",
          "type": "object",
          "properties": {
            "forms": {
              "description": "The forms that were being watched, and are now stopped.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "reason": {
              "description": "Why the watch ended: the data root vanished, or a different volume took\nits place.",
              "type": "string"
            },
            "stopped": {
              "description": "Whether stopping the services succeeded.",
              "type": "boolean"
            }
          },
          "required": [
            "forms",
            "reason",
            "stopped"
          ]
        }
      }
    },
    "wizard": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/WizardReport"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Phase": {
          "description": "Where an in-flight or finished setup stands in its lifecycle.\n\nThe persisted marker a later run reads to tell answers still being gathered\nfrom a half-written apply. Only these four are ever written: the two states a\nrun infers instead of storing — no setup at all, and an apply that stopped\nmid-write — are read off the world rather than trusted from a file (see\n[`Status`]).",
          "oneOf": [
            {
              "description": "Still gathering answers. Resumable, and nothing has touched disk.",
              "type": "string",
              "const": "in-progress"
            },
            {
              "description": "Every applicable question answered, awaiting the operator's confirmation.",
              "type": "string",
              "const": "reviewing"
            },
            {
              "description": "Writing configuration and starting services — the one non-atomic phase,\nand so the only marker whose persistence signals an interrupted run.",
              "type": "string",
              "const": "applying"
            },
            {
              "description": "Configuration written and valid.",
              "type": "string",
              "const": "applied"
            }
          ]
        },
        "SettingReport": {
          "description": "One setting, as it is safe to show.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The setting's name.",
              "type": "string"
            },
            "secret": {
              "description": "Whether the value was withheld.",
              "type": "boolean"
            },
            "value": {
              "description": "Its value, or a note that it is set and withheld.",
              "type": "string"
            }
          },
          "required": [
            "key",
            "value",
            "secret"
          ]
        },
        "Step": {
          "description": "A step of setup, in the order the operator meets it.\n\nSome steps only inform (they detect and state, and the operator acknowledges);\nothers ask a question whose answer the wizard records. The apply-and-onward\nsteps — writing config, pulling images, wiring services — are not modelled\nhere yet: they arrive with the features they drive, and this machine covers\nthe read-only phase that precedes them.",
          "oneOf": [
            {
              "description": "States what is about to happen and roughly how long it takes. Informs.",
              "type": "string",
              "const": "welcome"
            },
            {
              "description": "Detects the environment: OS, Docker, Compose, daemon reachability. Informs.",
              "type": "string",
              "const": "preflight"
            },
            {
              "description": "The account checklist derived from the protocol choice. Informs.",
              "type": "string",
              "const": "prerequisites"
            },
            {
              "description": "Usenet, torrents, both, or neither.",
              "type": "string",
              "const": "protocols"
            },
            {
              "description": "Whether a VPN carries the torrent traffic. Asked only where torrents were\nchosen, and after the checklist has explained what one is for.",
              "type": "string",
              "const": "vpn"
            },
            {
              "description": "Where downloads and the library are kept.",
              "type": "string",
              "const": "data-location"
            },
            {
              "description": "The indexer credential, tested against the live service. Asked only where a\ndownload protocol was chosen.",
              "type": "string",
              "const": "credentials"
            },
            {
              "description": "The Usenet provider login, tested over NNTP. Asked only where Usenet was\nchosen.",
              "type": "string",
              "const": "provider"
            },
            {
              "description": "The user and group the containers run as. Asked only where it is visible.",
              "type": "string",
              "const": "service-user"
            },
            {
              "description": "Whether to run Jellyfin, and if so how.",
              "type": "string",
              "const": "library"
            },
            {
              "description": "Whether others in the home will use it.",
              "type": "string",
              "const": "household"
            },
            {
              "description": "How much the operator wants to be told about — one question, three presets.",
              "type": "string",
              "const": "notifications"
            },
            {
              "description": "Whether to start on boot.",
              "type": "string",
              "const": "autostart"
            },
            {
              "description": "The complete summary, before anything is written. Informs.",
              "type": "string",
              "const": "review"
            }
          ]
        },
        "Validation": {
          "description": "What proving a credential against its live service established — never the\ninput, only the outcome.\n\nRead back as well as built. A surface that is not in this process asks setup to\nprove a credential and is told what came of it, so the four outcomes are tagged\nby name rather than distinguished by which field is present — the same reason an\nanswer carries the step it belongs to.",
          "oneOf": [
            {
              "description": "Proven working, carrying the capability observed while proving it.",
              "type": "object",
              "properties": {
                "observed": {
                  "description": "The observed fact — what the service did, not that it merely answered.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "valid"
                }
              },
              "required": [
                "outcome",
                "observed"
              ]
            },
            {
              "description": "The service answered and refused: the credential is wrong for it.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "What the service said, in terms the operator can act on.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "rejected"
                }
              },
              "required": [
                "outcome",
                "detail"
              ]
            },
            {
              "description": "Nothing usable answered, so nothing can be concluded about the credential.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "Why nothing usable came back.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "unreachable"
                }
              },
              "required": [
                "outcome",
                "detail"
              ]
            },
            {
              "description": "It authenticated, but cannot do the job it is for — exhausted, limited, or\notherwise unable.",
              "type": "object",
              "properties": {
                "detail": {
                  "description": "What it can no longer do, and why where the service says.",
                  "type": "string"
                },
                "outcome": {
                  "type": "string",
                  "const": "degraded"
                }
              },
              "required": [
                "outcome",
                "detail"
              ]
            }
          ]
        },
        "WizardReport": {
          "description": "Where a setup run stands, and what it is still asking for.\n\nThe answer to every step of setup driven from outside this process: a surface\nasks where the walk is, submits one answer, and is told where the walk is now.\nNothing here is a copy of the wizard's own state — it is read off the wizard\neach time, so a surface cannot hold a stale one and act on it.\n\n**The answers themselves are never in it.** Setup gathers an indexer key and a\nprovider password, and this report is one a script can log, so it says what was\n*decided* and never what was *entered* — the same line [`SetupReport`] holds.\nWhat will be written is in `plan`, with every credential withheld exactly as\n`config show` withholds one.",
          "type": "object",
          "properties": {
            "asks": {
              "description": "Whether that step asks a question, as opposed to only informing.",
              "type": "boolean"
            },
            "at": {
              "description": "The step the operator is on.",
              "$ref": "#/$defs/Step"
            },
            "offered": {
              "description": "Whether this machine has setup left to do. False once configuration\nexists and nothing is part-way through, which is when a surface directs\nthe operator to reconfiguration instead of asking the first question again.",
              "type": "boolean"
            },
            "phase": {
              "description": "Where this run stands in its lifecycle. `applying` read back here means an\napply stopped part-way, because an apply that is still running is one this\nanswer is waiting on.",
              "$ref": "#/$defs/Phase"
            },
            "plan": {
              "description": "What applying will write, in the order it will be written, with any value\nnobody has argued for showing withheld.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/SettingReport"
              }
            },
            "proof": {
              "description": "What proving the credential just given came to, where one was given.\n\nSetup tests an indexer key and a Usenet login against their live services as\nthey are entered, and this is what the service answered — never what was\nentered. Absent for every other answer, and for a step that gave none.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Validation"
                },
                {
                  "type": "null"
                }
              ]
            },
            "ready_for_review": {
              "description": "Whether every applicable question is answered, so the plan can be applied.",
              "type": "boolean"
            },
            "unanswered": {
              "description": "Every question that applies on this machine and has no answer yet, in the\norder they are put.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/Step"
              }
            },
            "written": {
              "description": "What an apply that stopped part-way had already written, each said plainly.\n\nThe partial state a recovery is chosen about, so whoever chooses has seen it.\nEmpty for every other phase, and empty too for an apply that stopped before\nit wrote anything.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "offered",
            "phase",
            "at",
            "asks",
            "unanswered",
            "ready_for_review",
            "plan",
            "written"
          ]
        }
      }
    },
    "word": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Envelope",
      "description": "The wrapper every machine-readable payload arrives in.",
      "type": "object",
      "properties": {
        "api_version": {
          "description": "The output contract's version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "data": {
          "description": "The payload.",
          "$ref": "#/$defs/Term"
        },
        "kind": {
          "description": "Which payload this is, so a consumer can branch before parsing `data`.",
          "type": "string"
        }
      },
      "required": [
        "api_version",
        "kind",
        "data"
      ],
      "$defs": {
        "Term": {
          "description": "A word this product uses, and what somebody meeting it needs to know.",
          "type": "object",
          "properties": {
            "also_called": {
              "description": "What other services in this stack call the same thing.\n\nSonarr and `SABnzbd` do not agree on words, and an operator moving between\ntheir screens should not have to work out that two of them are one.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "deep": {
              "description": "More, for somebody who asks — never needed in order to act.",
              "type": [
                "string",
                "null"
              ]
            },
            "short": {
              "description": "One sentence: what it is for and what it costs or gains.\n\nEnough to act on. Somebody who reads only this should not be stuck.",
              "type": "string"
            },
            "word": {
              "description": "The word as it appears in the interface.",
              "type": "string"
            }
          },
          "required": [
            "word",
            "short",
            "also_called"
          ]
        }
      }
    }
  }
}
